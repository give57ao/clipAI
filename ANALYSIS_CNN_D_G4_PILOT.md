# CNN-D 게이트 재정의(G4) + 파일럿 (2026-07-24)

## 발단

사용자 질문 "6,9는 cnn말고 기존 ocr하면 되는 거 아니야?"가 기존 G3(D슬롯 CNN
게이트) 측정 방법론의 결함을 짚었다.

## 결함 확인

`hud_kda._read_digit_group`의 CNN-D 폴백(`hud_kda.py:237`)은
`_CNN_V2_D and raw_patches is None and d is None`일 때만 발동 — 즉 **OCR/템플릿이
이미 실패한(d=None) 프레임에서만** CNN이 관여한다. 그런데 기존 G3는
`_build_digit_dataset_v2.py`의 `d_claimed`(OCR이 이미 성공한 안정런에서 뽑은
데이터)로 CNN 정확도를 쟀다 — **CNN-D가 실전에서 절대 켜지지 않는 population을
재고 있었다.**

## 조치 1 — 진짜 population 라벨링 + G4 게이트 신설

- `_harvest_d_hardneg.py` 신규: K슬롯 `hardneg_times`와 동일 원리(안정런 내부의
  미판독 프레임을 그 런의 값으로 라벨)를 D 채널에 적용. D 채널엔 별도 method
  태그가 없어 d=None 자체를 미스 신호로 사용. 결과: 771개 확보
  (`{'0':171,'1':42,'2':73,'3':128,'4':14,'5':316,'6':3,'7':19,'9':7}` — D=8은
  여전히 0건, 아래 "D=8 한계" 참고).
- `_digit_cnn_v2_gate.py`에 G4 추가: `hardneg_<digit>/`의 '_d' 파일(held-out)에
  대해 "자신있게 틀린 숫자" 비율만 위험 지표로 측정('junk' 오분류는 값 미주입이라
  기권과 동일 취급, 위험에서 제외). 구 G3는 참고용으로 강등(docstring에 결함 기록).
- 재학습 후 최초 실측(n=118): **자신있게 틀린 숫자 0건(0.00%)**, 발동시 정밀도
  100%, 커버리지 91.5%. G4 PASS.

## D=8 한계 (불변)

D와 K는 같은 템플릿 매처(`matcher.k_templates`)를 공유 — K=8과 완전히 동일하게
템플릿에서 구조적으로 제외됨. 전체 캐시 296,105건의 원시 D판독 전수 조사 결과
**D=8은 0건**(D=7 22,654건 → D=9 485건으로 직접 점프). 완화 하베스트로도 0건
확보 — 데이터로 해결 불가능한 구조적 한계, 향후 세션에서도 재시도 불필요.

## 조치 2 — 파일럿(end-to-end 캐시 비교)

CNN-8의 `fast_restore`(라벨만 'C'→'M'으로 바꾸는 트릭)는 D에 적용 불가 —
d=None 프레임은 캐시에 원본 픽셀이 없어 실제 영상 재방문이 필요하다. 범위를
좁힘: D가드는 킬을 지우는 방향으로만 작동하므로 영향받을 수 있는 건 현재
ace=True인 라운드뿐이고, 그 중에서도 [start_sec, ace_sec](3번째 킬 이전) 구간만
결과에 영향을 준다. 이 범위의 D-미스는 전체 corpus D-미스 503,761건 중 7,960건
(1.6%)뿐 — 여기만 CNN으로 채워 재계산.

**결과 (`scratchpad/_cnn_d_pilot.py`, 51개 ace-보유 영상):**
```
D-미스 patched(p>=0.95 통과) = 1,350
ace 구성 변경된 영상 = 0/51
```
200건 넘게 패치된 영상(예: 2026-04-23 23-41-07, patched=212)도 ace 목록이
완전히 그대로. **TP 손실 0, FP 제거도 0** — 안전하지만 현재 GT/코퍼스에서는
측정 가능한 이득이 없음. D가드가 원래 겨냥한 kill_shortfall류 FP(#120/#122
유형)는 이미 R14/R19 등 다른 게이트가 먼저 잡았거나, CNN이 채운 D값이 그
라운드 기존값과 일치해 애초에 이상치가 아니었던 것으로 추정(추가 조사 안 함).

## 결론 / 보류 사유

- 안전성은 G4 + 파일럿 이중으로 확인됨(위험 0, TP 손실 0).
- 그러나 현재 코퍼스에서 입증된 recall/precision 이득이 0이라 "고쳤다"고 보고할
  구체적 성과가 없음.
- **사용자 결정: 지금은 보류.** `_CNN_V2_D` 플래그는 `hud_kda.py`에서 계속
  `False` 유지. 재검토 시 이 문서 + `scratchpad/cnn_d_pilot_result.json`
  (영상별 patched count·old/new ace 목록 원본) 참고.
