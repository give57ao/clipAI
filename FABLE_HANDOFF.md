# Fable 인계 문서 (2026-07-23, Sonnet 작성)

> **✅ 처리 완료 (2026-07-23 Fable, R16)** — 두 요청 모두 핵심 로직 구현·selftest 통과.
> 남은 실행(재수확·재학습·파일럿 검증)은 문서 하단 **"Sonnet 실행 지시 (R16 후속)"** 참고.
>
> **요청 #1 처리**: 후보안 (a)를 채택하되 그대로는 쓰지 않았다 — **시간적 동의만으로는
> FP #1을 못 막는다**(05-26 실측: CNN-8이 450.0/450.5/450.75s 연속 3프레임 — "2프레임
> 동의"를 통과함. CNN이 6을 8로 '일관되게' 오분류하면 시간 축 동의는 무력).
> 대신 2중 조건으로 설계: 시간적 동의 + **체인 인접 불변식**(직전 지지 K값이 7/8일
> 때만 승격 → 승격된 8은 +1 초과 조작이 원리적으로 불가 — 가짜 트리플 원천 차단).
> 구현: `files/hud_cnn8_promote.py`(신규, selftest 8케이스 — FP 실측 지문을 ①번
> 픽스처로 고정), `hud_kda.py`(값 주입 → method='cnn8_cand' 후보 표기로 변경, 단일
> 글리프+IoU None 자리만), `hud_sig_cache.py`(캐시 코드 'C'), `detect_ace_hud.py`
> (정산 직전 승격 호출 + warnings에 cnn8_promoted_N). 플래그는 여전히 OFF —
> 파일럿 게이트 통과 후 Sonnet이 켠다.
> 수용한 대가: 7이 아예 안 읽힌 8-크로싱(6→[7,8 미판독]→9)은 이 경로로 못 구제 —
> 기존 Rule A/B가 계속 담당. (b)·(c)안은 채택 안 함 — (b)는 promote 단계에서 이미
> 같은 효과가 더 정밀하게 걸리고, (c)는 승수 채널 의존이라 이번 FP처럼 승수가
> 없는 구간에서 무력하기 때문.
>
> **요청 #2 처리**: 제안 1안(형태 기준) 채택 — junk를 "안정런 밖 miss"가 아니라
> **"모든 숫자 템플릿과 IoU<0.35"**(어떤 숫자로도 설명 안 되는 형태)로 재정의
> (`_build_digit_dataset_v2.py`의 `_is_true_junk`). 템플릿을 양성 라벨로 쓰면 순환
> (v1 실패)이지만 음성 필터로 쓰는 건 안전하다. 스모크 실측: 희소 노이즈=junk ✓,
> 스쳐간 숫자·흐릿한 숫자=배제 ✓(이번 오염원 차단), 세로 막대=배제(1과 닮음 —
> 오라벨보다 폐기가 옳음). junk 표본 수는 줄지만 오염은 원리적으로 불가.
> 2안(체인 라벨링)은 채택 안 함 — 스쳐간 값의 라벨을 새로 다는 공격적 방식이라
> 얻는 표본 대비 오염 리스크가 비대칭.

핵심 로직 설계·의사결정이 필요한 두 건을 정리했다. 상세 조사 기록은
`SONNET_TASK_DIGIT_CNN.md`에 이미 있음 — 이 문서는 그걸 요청 단위로 압축한 것.

## 0. 현재 파이프라인 상태 (안전, 변경 없음)

- GT 174건 / recall 66.7% / precision 85.9% (원본 보유 서브셋 precision 93.9%)
- `hud_kda._CNN_V2_EIGHT = False`, `_CNN_V2_D = False` — **둘 다 OFF**
- `hud_digit_match._CNN_MODEL_PATH`는 v2(`hud_digit_clf_v2_best.pt`)로 전환된 채 유지 —
  플래그가 꺼져 있어 무해(모델 자체는 T3 게이트 통과, G1 오발률 0%)
- review_ledger.csv 재구축 완료(T5), 무손실 검증됨

---

## 1. 요청 #1 — CNN-8 보정의 정산(settlement) 통합 재설계

### 배경
게임 폰트에서 "0"과 "8"이 시각적으로 거의 구분 안 돼(`hud_round_settle.py`의
가짜0=8 격리 로직이 원래 이 문제의 대응책), 숫자 CNN(TinyDigitCNN)을 학습시켜
K슬롯의 "0 또는 None" 판독 자리에서만 8 여부를 추가 확인하도록 `hud_kda.py`에
`_CNN_V2_EIGHT` 플래그로 통합했다(`_read_digit_group` 내부, 다른 확신 판독은
절대 안 건드리는 원칙 유지).

### 무엇이 실패했나
개별 프레임 정확도는 검증됨(T3: claimed-0→8 오발률 0%, n=372, held-out).
그런데 **실제 파이프라인 파일럿(5영상 재스캔)에서 FP 2건 신규 발생** →
게이트(TP손실0/FP순증0) 미달로 즉시 롤백함.

**원인 (원시 판독 직접 대조로 확인)**: CNN-8이 "0/None 자리만 건드린다"는
원칙은 지켰지만, **"원래 완전히 비어있던(전부 template_miss) 구간에 갑자기
조밀하게 값을 채워넣는" 경우**를 고려 안 함. 두 FP 다 동일 지문:
```
K=5 안정 (수십 프레임, conf 0.65~0.74)
  → 수 초간 전부 template_miss(M)
  → 그 구간 중 4~5개 프레임에서 CNN이 k=8, conf=0.85로 갑자기 등장
  → hud_round_settle이 "5→8(+3)"을 gap-stamp로 같은 시각에 몰아 찍음
  → 폭0(kill_times 3개가 완전 동일 시각) 가짜 트리플킬
```
R10 폭0 게이트(`_SCORE_SPLIT_MIN_EVENTS`)가 원래 이런 패턴을 잡는데, 두 케이스
다 인근 승수(win) 이벤트가 2개 미만이라 커버리지 밖이었음.

**추가 확인**: FP 1건(`2026-05-26 23-45-51` R09, 7:30)은 실제 프레임을 열어
직접 확인한 결과 그 순간 화면 K/D/A는 계속 "6"이었음 — CNN이 6을 8로
**오분류**한 것(진짜 숨은 8이 아니었음). 6클래스 학습 데이터가 51장뿐이라
(다른 숫자는 수천 장) 편중이 원인으로 추정, 현재 6/9 클래스 완화-런
보강 수확 + 재학습 진행 중(별건, 이 요청과는 독립).
나머지 FP 1건(`2026-05-18 01-54-57` R70)은 같은 지문이나 프레임 직접 확인은
안 함 — 같은 원인일 가능성 있음.

### 예시 데이터 (재현용)
- 원시 캐시 백업: `C:\Users\give5\AppData\Local\Temp\claude\C--Users-give5\7713dfb0-cef2-4b15-b4d4-60c807a90980\scratchpad\cnn_pilot_cache_backup\`
  (`2026-05-18 01-54-57.json`, `2026-05-26 23-45-51.json` — CNN-on 상태 원시 판독 그대로 보존.
  `tl_*.json`은 그때의 hud_timeline)
- 재현 라운드: `2026-05-18 01-54-57` R70 (kill_times 2204.5×3), `2026-05-26 23-45-51` R09 (kill_times 450.0×3)
- 프레임 확인용 스크립트 패턴(위 대화에서 사용): `cv2.VideoCapture` + `CAP_PROP_POS_FRAMES` seek → `E:\OBS\<stem>.mp4`

### 후보 수정안 (택1 이상, 순서는 보수적→공격적)

**(a) CNN-8 증거에 시간적 동의(지지) 요건 추가** — 추천 1순위.
`hud_round_settle._supported_obs`(n≥2 또는 conf≥0.88)와 동일 철학을 CNN-8
증거에도 적용: 고립된 단발 CNN-8이 아니라, 인접 구간에서 여러 프레임이
반복 동의할 때만 채택. 문제는 현재 통합 지점(`_read_digit_group`, 프레임
단위 함수)이 시간적 문맥에 접근 불가 — CNN-8 후보를 일단 특수 태그로
원시 캐시에 남기고(예: method='cnn8_cand'), `hud_round_settle.py`(전체
reads 리스트를 보는 지점)에서 사후 확정하는 구조 변경이 필요함.

**(b) R11 암전-gap 가드(`_DARK_GAP_GUARD`) 확장** — 현재 이 가드는 원시
판독 공백(완전 암전 세그먼트)에만 적용됨. CNN이 채워넣은 값도 "원래
암전이었던 구간의 값"이라는 점에서 동일 카테고리로 취급해 gap 귀속을
포기(`gap_unattributed`)하도록 확장.

**(c) R10 폭0 게이트를 CNN-유래 폭0에 한해 완화** — 일반 폭0은 승수
2개 이상 요구하지만, CNN-8이 관여한 폭0은 그 자체가 의심 신호이므로
승수 1개 이상으로 완화(또는 무조건 기각). 코드는 가장 간단하나 "CNN이
관여했는지" 태깅이 선행돼야 함.

### 필수 검증 절차 (이번에 확립·검증된 안전 패턴, 그대로 재사용할 것)
1. selftest(`hud_round_settle.py` 19케이스, `hud_streak_salvage.py` 8케이스) 통과 확인
2. 파일럿 재스캔 5~8영상 (원본 소실 영상은 **재스캔 자체가 안 되니 사전에
   `gt_aces.json`의 `source_available` 확인 후 파일럿 목록에서 제외** — 이번에
   3영상 낭비한 실수 반복 금지)
3. 재스캔 전 **반드시 대상 영상의 기존 sig_cache를 먼저 백업**(이번에 이걸
   빼먹어서 TP손실 원인을 재스캔 비결정성과 CNN 귀책으로 명확히 못 갈랐음)
4. `hud_from_cache.py` + `_compare_hud_gt.py` + 탐지/FP 집합 diff
5. 게이트: 기존 TP 상실 0, FP 순증 0 (GT 외 신규 검출은 FP 아님 — `_검수중` 큐잉)
6. 통과 후에만 `_CNN_V2_EIGHT=True` 커밋 → 전체 재스캔 확대

---

## 2. 요청 #2 — junk 하베스터 재설계

### 배경
`_build_digit_dataset_v2.py`의 6클래스 라벨링 중 "junk"(잡음) 클래스는
"안정런 밖에서 발생한 template_miss 프레임"으로 정의됨.

### 무엇이 잘못됐나
QC 몽타주 육안 검사 결과, 이 폴더가 **실제로는 멀쩡히 읽히는 숫자로 가득함**
(9, 3, 6, 5 등 또렷하게 확인). 원인: 빠른 킬스트릭 중 K값이 순식간에 여러
번 바뀌면 어느 값도 안정런 기준(연속 4프레임)을 못 채우는데, 그 프레임들은
"진짜 노이즈"가 아니라 "그냥 짧게 스쳐간 정상 값"이다 — **정확히 이 프로젝트가
고치려는 그 상황(빠른 킬스트릭 중 과소판독)** 을 하필 학습 단계에서
"답이 없는 잡음"으로 가르치고 있었던 것.

### 현재 임시조치
v2 junk 폴더 전량을 학습에서 제외(v1 junk 135장만 사용) — 근본 해결 아님,
그냥 오염원을 차단만 한 상태.

### 필요한 재설계 방향 (제안, Fable 판단에 맡김)
- "안정런 밖"이 아니라 "글리프 형태 자체가 인식 불가"(예: 모든 숫자
  템플릿과의 유사도가 골고루 낮음, 또는 실제로 블러 판별 가능한 화질
  지표)로 junk 기준을 재정의
- 또는: 짧게 스쳐간 값도 "비-모순 사슬"(직전/직후 값과 단조증가 관계가
  성립하는가) 검증을 통과하면 그 값 그대로 라벨링 — `_best_chain`의
  DP 로직과 유사한 방식을 하베스터 라벨링 단계에 이식하는 방향

---

## 3. 참고 — 별도 진행 중(이 요청과 무관)

6/9 클래스(각 51/4장으로 편중) 완화-런(n≥2) 보강 수확 **완료**
(`files/_harvest_rare_digits.py`). 재학습은 아래 실행 지시에 통합.

---

## 4. Sonnet 실행 지시 (R16 후속 — 순서대로, 2026-07-23 Fable)

### S1. junk 재수확
1. `E:\clipai_result\hud_templates_harvest\cnn_dataset_v2\junk\` 내용물 전부 삭제
   (오염 확정분 — v1 junk는 별도 경로라 무관)
2. `python -u _build_digit_dataset_v2.py` 전체 재실행 (~70분, 파일명이 결정적이라
   기존 클래스 파일은 동일 내용으로 덮어씀 — junk만 새 필터로 다시 채워짐)
3. 새 junk 폴더 몽타주 QC — 이번엔 "숫자로 보이는 게 하나도 없어야" 통과.
   수량이 아주 적어도(수십 장) 정상 — 정밀도 우선 설계

### S2. 재학습 + 오프라인 게이트
1. `train_hud_digit_cnn.py`의 v2 junk 스킵 블록(`if name == "junk": ... continue`)을
   **제거**하고 v2_junk origin으로 복원 (S1 통과가 전제)
2. 재학습 → 6/9 보강분 + 새 junk 포함. per-class/origin 보고 (6클래스 val 정확도가
   26/41(63%)에서 유의미하게 오르는지 확인 — 이번 보강의 목적)
3. `python -u _digit_cnn_v2_gate.py` — G1(0→8 오발률 <0.5%) 필수 재통과.
   **G1 실패 시 이후 단계 진행 금지**, 결과만 기록하고 중단

### S3. R16 파일럿 검증
1. 파일럿 목록(전부 원본 보유 확인됨): `2026-05-26 23-45-51`(FP 픽스처),
   `2026-05-18 01-54-57`(FP 픽스처), `2026-04-27 22-50-54`(7→8 recall 목표),
   `2026-03-26 01-26-52`, `2026-04-10 00-08-17`
2. **재스캔 전 5영상 sig_cache 백업** (§1 검증 절차 3항 — 이번엔 절대 생략 금지)
3. `hud_kda._CNN_V2_EIGHT = True` (커밋은 아직 금지) → `hud_sig_cache.py --force`
   5영상 재스캔
4. `hud_from_cache.py` + `_compare_hud_gt.py` + 탐지/FP 집합 diff (베이스라인:
   recall 66.7%/precision 85.9%, `cmp_after_restore.txt` 시점)
5. 게이트: **기존 TP 상실 0 + FP 순증 0** + 두 FP 픽스처 라운드(05-26 R09 인근,
   05-18 R70 인근)에 가짜 올킬 미재발. 각 영상 timeline `warnings`의
   `cnn8_promoted_N`으로 승격 발동 수도 기록할 것 (0이어도 실패 아님 — 불변식이
   보수적이라 파일럿에 7→8 상황이 없으면 0이 정상)
6. 통과 시에만 플래그 ON 커밋 → 전체 재스캔 확대(§1 절차 6항). 실패 시 플래그
   OFF 롤백 + 원시 캐시 dump로 어느 조건이 뚫렸는지 기록 후 Fable 재인계
