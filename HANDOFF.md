# clipAI 세션 핸드오프

> 마지막 업데이트: 2026-07-20 (KST)  
> 다음 채팅에서 이 파일을 먼저 읽고 이어서 작업하세요.

## ✅ 2026-07-20 미스캔 15개 스캔 완료 + kill_shortfall 6건 원인 조사 완료(코드 미수정, Fable 이관 대기)

**미스캔 15개 스캔 완료**: `python -u batch_hud_ace_pipeline.py --stems-file
files/_scan_batch_20260701.txt` 실행 완료 (로그 `E:\clipai_result\_scan_20260701_15.log`).
15개 중 3개 너무 짧아 스킵(`2026-07-01 01-28-52` 2s, `2026-07-05 03-47-44` 94s,
`2026-07-14 22-34-15` 9s), 12개 스캔 성공, **신규 올킬 14건** 발견·클립 14개 전부 추출 완료
(`2026-07-11 01-04-44` R0,R2 / `2026-07-11 02-55-26` R5,R29 / `2026-07-12 00-11-57` R18,R24 /
`2026-07-14 22-36-39` R0 / `2026-07-15 00-08-59` R7,R9,R51 / `2026-07-15 00-47-57` R45,R61,R95 /
`2026-07-15 02-04-54` R13). **사용자 검수 대기 — 아직 review_ledger.csv 미반영.**

**kill_shortfall FP 6건 전수 프로브 조사 완료** (`files/_probe_window_reads.py`로 표적 구간
디코딩, 재스캔 없음). 코드 수정은 하지 않음 — **아래 1번 패턴(3건)과 2번 패턴(2건)은 모두
"하드" 케이스로 Fable에게 넘길 것**(기존 캘리브된 규칙을 건드려야 해서 회귀 위험 있음):

**패턴 1 — 그레이스 규칙 역방향 오귀속 (4건) → ❌ 수정 불가 판명·revert 완료(2026-07-20 Sonnet)**:
`hud_round_settle.py` L385-395 "경계 그레이스"(`first_t - s <= _GRACE_SEC(10.0)` 이면 다음
라운드 첫 관측 상승분을 **직전 라운드**로 귀속)가 FP 4건(`04-23 R03`, `06-25 R29`, `07-03 R09`,
`06-27 R45`)에서 다음 라운드 자기 킬을 훔쳐 가짜 올킬을 만든 게 맞음(프로브로 확인). **그러나
GT108 재평가로 이 그레이스가 실제 GT 올킬 최소 4개를 정당하게 완성하고 있음이 드러났고, 그
TP들과 FP가 K-판독 신호만으로는 분리 불가능함이 하드 데이터로 증명됨** — 아래 표(캐시 트레이스
실측, `hud_round_settle.py` 주석에도 동일 기록):

| 케이스 | 유형 | gap | pre(s−carry_t) | post(first_t−s) | 경계前 상승값 관측 |
|---|---|---|---|---|---|
| 01-08 R07 | TP(GT) | 1 | 6.25 | 9.75 | O |
| 04-26 R45 | TP(GT) | 1 | 5.50 | 10.0 | O |
| 04-24 R07 | TP(GT) | 2 | 15.0 | 10.0 | X |
| **03-30 R38** | **TP(GT)** | 1 | 7.00 | 8.50 | **X** |
| 06-25 R29 | FP | 1 | 3.9 | 9.4 | X |
| 07-03 R09 | FP | 1 | 4.1 | 9.3 | X |
| 04-23 R03 | FP | 2 | 7.5 | 9.0 | X |

시도한 게이트: "pre ≥ post(경계 前 암전이 더 김)" OR "경계 前 상승값 관측"일 때만 그레이스 발동.
→ 01-08·04-26(관측O)·04-24(pre≥post)는 살지만 **03-30 R38(pre 7.0/post 8.5/미관측)이 FP R03
(pre 7.5/post 9.0/미관측)과 사실상 동일**이라 어떤 조합으로도 R38을 살리면서 R03을 죽일 수
없음. 실제 게이트 적용 시 GT TP 79→76(−3) 회귀 확인 → **전면 revert**(TP 79 복구, selftest 19/19,
pytest 27/27 재확인). 근본 원인: "직전 라운드 막판 킬이 경계 넘어 늦게 읽힘"(TP)과 "다음 라운드
자기 킬"(FP)이 **판독 지연 때문에 관측상 완전히 동일** — K-판독만으로 원리적 분리 불가.
**결론: 이 FP 4건은 현재 신호 한계상 '수정 불가'로 분류.** 진짜로 고치려면 K-판독 외 신호 필요
(킬피드 OCR, 또는 전광판의 라운드별 킬 수 교차검증) — 별도 대형 과제. 재시도 시 위 표 먼저 재현.

**패턴 2 — "9→8 오분류" 가설 → ❌ 반증됨, 실제로는 패턴 1과 동종(2026-07-20 Sonnet)**:
가설은 R8 8-위상 프로브가 9를 8로 오분류(conf=0.80 고정 시그니처)해 carry가 1 낮아졌다는 것.
**검증: 문제 글리프의 원본 BGR 크롭을 직접 추출·육안 확인 → 두 케이스 다 명백한 "8"**
(고리 2개, 위상 구멍 2개가 7개 임계값 중 4~5개에서 견고하게 검출 = genuine 8 지문; 진짜 9라면
구멍 1개). 크롭은 scratchpad에 저장 후 삭제.
  - `2026-04-28 23-17-41 R53`: 문제의 "8"들(38:00-38:28, conf 0.80)은 R53이 아니라 **직전 라운드
    R52의 carry 값**. R52 K=8 관측(38:28) → **암전 38:36-39:28(R52/R53 경계 38:59.875를 가로지름)**
    → R53 첫 판독 K=10(39:28) → K=11(39:54). 즉 8→11 세 번의 킬 전환이 경계-가로지르는 암전에
    숨음. carry(8)→10 gap=2가 R53 시작에 스탬프 + 10→11 = 3킬로 집계. 사용자 "2킬"이 맞다면
    8→9 킬 1개가 R52 몫인데 암전 위치를 알 수 없어 **패턴 1과 정확히 동일한 경계-암전 귀속
    모호성**(단 grace가 아니라 forward-gap `else` 경로 — first_t-s=28s>10s라 grace 미발동).
    ※사용자 readout "9/9"는 글리프가 8이므로 육안 오독으로 보임.
  - `2026-06-22 02-08-51 R77`: 동일 — 글리프 "8" 확인(58:22, 구멍 2개), 경계-가로지르는 암전.
  - **결론: 8-프로브는 정상. 패턴 2는 별개 버그가 아니라 패턴 1과 같은 "경계 가로지르는 암전
    속 킬 귀속 불가" 클래스.** 따라서 **동일하게 현재 신호론 수정 불가**로 종결. (게다가 두 영상
    다 캐시 없어 재스캔 없이는 어떤 gap-로직 변경도 검증 불가 + gap/grace 경로는 GT TP와 얽혀
    있음이 패턴 1에서 증명됨.)

**패턴 3 — 원인 불명, 재검수 필요 (1건)**: `2026-07-03 21-42-21 R56` — 프로브 관측 K
4→5(35:54.25, 정상)→7(36:10.25, 정상, 사이 6 관측 없이 스킵)은 전부 conf 정상 변동폭(위상
프로브 시그니처 없음). 그런데 사용자 육안 노트("3/6->4/7, 1킬+1데스")의 절대값이 프로브
실측(K 4/5/7대)과 **1 단위 어긋남** — 라운드/구간을 잘못 짚었을 가능성 있음. 다음 세션에서
클립 직접 재생해서 정확한 시각-값 대조 필요(추정만으론 결론 내리지 않음).

**kill_shortfall 6건 최종 결론(2026-07-20 Sonnet)**: 6건 중 **5건이 "경계 가로지르는 암전 속 킬
귀속 불가"라는 단일 근본 원인으로 수렴** — 패턴 1(grace 경로: R29/R09/R03/R45)과 패턴 2(forward-gap
경로: R53/R77). K-판독 신호만으로는 TP 올킬과 원리적으로 분리 불가(GT 데이터로 증명). **현재 신호
한계상 전부 '수정 불가'로 종결.** 근본 해결책은 K-판독 외 신호 도입(킬피드 OCR / 전광판 라운드별
킬수 교차) — 별도 대형 과제로만 가능. **나머지 1건 R56(`07-03 21-42-21`)은 재검수 결과 실제 TP
(K 4→7 3킬 올킬)로 판명 — 애초에 FP가 아니라 사용자 육안 오독("3/6->4/7")이었음. 즉 kill_shortfall은
실질 5 FP(수정불가) + 1 오분류(정정).** ledger에서 R56 fp→tp 정정 완료.

**2026-07-20 신규 스캔 14 올킬 사용자 검수 완료 → 12 TP / 2 FP**: FP 2건 —
`2026-07-15 00-08-59 R09`("1킬 후 9초 라운드변경 후 2킬" = 실제 1킬, boundary_merge류),
`2026-07-15 00-47-57 R61`("4/5->6/5" = 2킬뿐, kill_shortfall류). **둘 다 위 "경계-암전 귀속" 클래스가
신규 영상에서 재현된 것** — 예상된 잔존 FP. 나머지 12건 전부 TP 확정(ledger 반영). TP 12건은
GT 후보로 승격 검토 가능(`gt_aces.json` 추가 시 recall 기준 확대).

**다음 세션 진행 순서**: 1) 신규 올킬 14건 사용자 검수(위 목록), 2) kill_shortfall 6건 → **종결**
(위 결론), 3) R56 재검수, 4) (선택) 킬피드/전광판 교차검증 대형 과제 검토 — 위 5건을 한 번에
풀 유일한 경로. 5) 모든 수정은 pytest+selftest+캐시 재평가(`_tp_diff --compare-to r10_gt107`)로
TP 손실 0 검증.

## ✅ 2026-07-19 사용자 육안 검수 완료 — 120개 중 15개 FP 확정, 패턴 3종 확인 (`review_ledger.csv`에 기록)

`E:\clipai_result\ace_clips_flat`(현재 hud_timeline과 정확히 일치하는 120개, 중복/레거시 제거 후)을
사용자가 전수 육안 검수. 105개는 문제 언급 없음(정상 확인으로 간주), **15개 FP 확정** — 유형 3종으로
분류됨(상세 근거·round/t는 `E:\clipai_result\review_ledger.csv` 신규 15행, verdict=fp 참고):

1. **kill_shortfall(7건)**: 실제 킬이 3 미만(대부분 2킬+데스1, 또는 1킬뿐)인데 ace 처리됨 —
   `2026-04-23 23-41-07 R03`, `2026-04-27 22-50-54 R69`, `2026-04-28 23-17-41 R53`,
   `2026-06-22 02-08-51 R77`, `2026-06-25 03-10-00 R29`, `2026-07-03 20-07-56 R09`,
   `2026-07-03 21-42-21 R56`.
2. **boundary_merge(4건)**: 라운드 경계(라운드윈 배너)를 못 잡아서 서로 다른 라운드 킬을 합쳐
   3킬로 오판 — `2026-05-19 01-36-22 R03`(9초 지점 라운드윈 미검출 명시), `2026-06-14 00-55-19 R06`,
   `2026-06-27 01-31-51 R45`, `2026-06-28 01-42-47 R22`. **07-19 앞부분 R10 FP 조사(R26/R48/R16)와
   동일 버그 클래스** — 이번에 4건 더 확인돼 표본이 늘었음. R10 게이트뿐 아니라 라운드 경계 검출
   자체(row_miss 기반)가 이 실패 모드의 근본 원인일 가능성.
3. **death_misread(2건, 신규 확인·우선순위 상향)**: 킬이 아니라 **데스**인데 K값이 튀어서(또는
   K 완전 불변인데) ace로 잡힘 — `2026-03-29 03-12-36 R48`(앞부분 07-19 조사에서 이미 FP 확정했던
   건, 재확인), **`2026-05-26 23-45-51 R12`(신규, K값 11→11 완전 불변인데 ace 처리 — 가장 심각)**.
   2건째 확보로 "선택사항, 필요성 낮음"에서 **다음 세션 조사 우선순위로 상향**. 원인 미상 —
   `hud_kda.py`/`read_kda_triple_from_game`(K/D/A 슬롯 판독) 쪽 의심되나 코드 미확인. 조사 시작점:
   `python -u hud_from_cache.py --dump "2026-05-26 23-45-51" 9:00 10:00`로 원시 K/D 판독값 먼저 볼 것
   (캐시 있음 — 영상 재생 불필요).
4. **inconclusive(2건)**: 클립이 결과 확정 전에 끝남 — `2026-06-26 23-43-25 R54`(11킬/5데스 불명),
   `2026-06-27 01-31-51 R16`(과정 중 종료). **꼬리 버퍼 부족 의심** — 이번 세션 초반에 앞부분
   버퍼(`_CLIP_PRE_KILL_SEC`) 부족 문제(R16 건)는 고쳤는데(`_CLIP_MAX_SEC` 55→120), 반대로 뒷부분
   (`ace_sec+12.0` 고정값)도 부족할 수 있음 — 다음 세션에서 `ace_clip_window()`의 tail 버퍼 상향
   검토.

**⚠ 부작용 발견**: 이번 정리(플랫 폴더 작업) 중 `ace_clips_hud/_폭0오탐_검수용`(width0 FP 레거시
검수 대기 폴더, `review_ledger.csv`엔 `unreviewed`/`unparsed_filename`으로만 남아있던 미처리
항목들)이 실수로 플랫 폴더에 섞였다가 "다른 명명규칙"으로 분류돼 삭제됨. 원본 영상은 안 건드림
(재추출 가능), ledger 상 어차피 미검토 상태였고 현재 R6 width0 게이트가 이 조사를 사실상
대체했으므로 손실 영향은 낮다고 판단 — 다만 완전한 재현은 불가하니 참고.

**남은 다음 TODO(갱신)**:
1. ~~`ace_clip_window()` 꼬리 버퍼 상향~~ → **✅ 완료(2026-07-19, "쉬운 것부터")**:
   `_CLIP_POST_END_SEC` 1.5→6.0, 내부 floor(`r.end_sec+0.5`)→`+5.0`으로 상향. 검증: 두 inconclusive
   건(`2026-06-26 23-43-25 R54` 39:38→39:42, `2026-06-27 01-31-51 R16` 16:06→16:11) 재추출 완료,
   `ace_clips_flat`에 갱신됨 — **재검수 필요**. 단, 두 건 다 킬타임 3개가 전부 동일 시각(width0)이라
   단순 버퍼 문제가 아니라 death_misread/width0류 오탐일 가능성도 있음 — 재검수 시 참고.
2. ~~`2026-05-26 23-45-51 R12` death_misread 원인 조사~~ → **✅ 완료(2026-07-19 Fable, 아래
   "R11 암전-gap 가드" 절)** — "데스 오독"이 아니라 **암전 라운드 킬 델타 오귀속**으로 규명·수정.
3. ~~boundary_merge 4건 조사~~ → **✅ 조사 완료(2026-07-19 Fable, 아래 "boundary_merge 4건 조사"
   절)** — 1건 로직 수정(R12 게이트), 2건 구버전 스캔 잔재(재스캔 시 자가치유 확인), 1건 재스캔 후
   재진단 필요.
4. (기존 유지, 무거운 작업) 미스캔 영상 15개(7/1~7/15) 스캔 — 타이밍 확인 후. **+ 아래 3건도 같은
   배치에 포함할 것**: `2026-06-14 00-55-19`, `2026-06-28 01-42-47`(구버전 스캔 FP 자가치유),
   `2026-06-27 01-31-51`(재스캔 후 R45 재진단 — 그레이스 역귀속 의심). 재스캔 시 `--redo`
   + `cache_dir` 지정해 sig_cache_v2도 같이 확보 권장.

## ✅ 2026-07-19 (Fable) boundary_merge 4건 조사 — R12 경계-이전 완료 게이트 추가, FP 1건 제거·2건 재스캔 처방

사용자 검수 boundary_merge 4건을 캐시 덤프(GT 1건) + 표적 구간 프로브(비GT 3건,
`files/_probe_window_reads.py` 신규 — 전체 스캔 없이 해당 1분 구간만 seek 디코딩, 수 초 소요)로
전수 분석. 결과는 4건이 **서로 다른 3가지 원인**으로 갈림:

1. **`2026-05-19 01-36-22 R03`(#49) — 로직 결함, 수정 완료**: K 4→6 킬 2개가 라운드윈 배너
   이전(경계 2:08.9 전 2:06.0)에 완료됐고 그 K=6이 단발 conf 0.79로 관측까지 됐는데, 지지 필터
   (n≥2 or conf≥0.88)에 걸러져 직전 라운드 체인 진입 실패 → carry(4)→k_start(6) gap=2가 R03
   시작에 폭0 스탬프, 6→7 킬 1개와 합쳐져 가짜 올킬. **수정**: `_GAP_PREBOUNDARY_CAP`
   (`hud_round_settle.py`) — blind gap 중 **경계 이전 구간**(t ≤ round_start)에서 k_start와 같은
   값이 관측되면 gap 킬 전부 경계-전 완료로 보고 소거. 경계 *후* 관측은 증거로 안 씀(이 라운드
   킬일 수 있음 — TP 보호 핵심). selftest ⑮/⑮' 추가(19/19), pytest 27/27, 캐시 재평가
   `_tp_diff` **유지79/획득0/상실0**, 정확히 R03 1건만 제거. **precision 81.4%→82.3%**(검출 96,
   GT108). 프로덕션 반영·FP 클립 제거 완료(flat 116개).
2. **`2026-06-14 00-55-19 R06`(#70)·`2026-06-28 01-42-47 R22`(#104) — 구버전 스캔 잔재, 재스캔
   처방**: 두 영상 다 프로덕션 타임라인이 **7/13 스캔본**(R8 8-위상프로브 7/14, R10 게이트 7/17
   이전) — 당시 K=8 판독 불가라 7→10 gap=3 폭0 오판. 현재 코드 프로브로 **K=8 conf 0.80 정상
   판독 확인**(00-55-19: 4:47부터 8 안정, 경계 4:53 전. 01-42-47: 17:00 K=8 D=4 안정) — 재스캔하면
   R05가 7→8 킬을 제 라운드에 갖고 gap이 2로 줄어 ace 소멸(자가치유). 비GT라 sig_cache_v2 없음 —
   로직만으론 못 고치고 재스캔 필요(위 TODO 4에 편입).
3. **`2026-06-27 01-31-51 R45`(#100) — 미해결, 재스캔 후 재진단**: 이것도 7/13 스캔본. 경계 자체는
   잡혀 있었음(30:44.4, 프로브로 row_miss run 30:42-30:47 실재 확인) — 문제는 3번째 킬이 **그레이스
   규칙**(경계+10s 내 첫 관측 상승분을 직전 라운드로 귀속)으로 다음 라운드(R46, K=3 첫 관측
   30:50.75 = 경계+6.3s)에서 역귀속된 것. 사용자 육안(1킬 후 라운드 변경, 나머지 2킬은 다음 라운드)
   과 배치: 그레이스가 이 케이스에선 틀린 방향. 단 그레이스는 실측 TP(23-51-52 16:23)로 캘리브된
   규칙이라 함부로 못 건드림 — 재스캔(새 판독 품질) 후 여전히 재현되면 그때 그레이스 조건
   정밀화(예: D-채널 사망/승수 이벤트와 교차) 검토.

**이번 세션 누적**: precision 79.0% → 81.4%(R11 암전-gap) → **82.3%**(R12 경계-이전), TP 79 불변.
사용자 확정 FP 15건 중 **4건 로직으로 제거**(R11: 3건, R12: 1건), 2건 재스캔 처방, 나머지는
kill_shortfall 6건(개별 원인 미조사)·inconclusive 2건(재검수 대기)·boundary_merge 1건(위 3번).

## ✅ 2026-07-19 (Fable) R11 암전-gap 가드 — "death_misread" 2건 진범 규명·수정, FP 3건 제거·TP 손실 0

**조사**: `hud_from_cache.py --dump "2026-05-26 23-45-51" 9:00 10:00`로 원시 판독 확인 —
R12(9:27-9:57) 구간의 성공 판독은 **전부 K=11로 정확**(오독 없음). 진범은 판독이 아니라 귀속:
R10이 K=8로 종료(마지막 판독 8:40) → R11(8:48-9:27)은 판독 성공 **0회**(완전 암전, n=0) →
R12 첫 판독 K=11(9:40) → `hud_round_settle.settle_rounds`의 carry-gap 경로가 `11-8=3킬`을
R12 시작 시각에 폭0 스탬프. 실제 3킬은 R11 몫(사용자 육안: 클립 시작 9:13에 이미 11/8)이므로
"킬 0에 데스 2인 라운드"가 올킬로 승격된 것. **"데스 오독" 가설은 기각** — D 채널은 무관했다.

**수정**(`hud_round_settle.py`, `_DARK_GAP_GUARD`): gap 킬 귀속은 carry가 **직전 세그먼트**에서
온 경우(경계 부근 미관측 킬 — 04-24 00-43-29 5:50 실측 TP 패턴)만 허용. 사이에 체인 없는
세그먼트가 통째로 끼면(`carry_seg < idx-1`) 발생 라운드 특정 불가로 귀속 포기 —
`gap_unattributed` 진단 필드에 기록, k_base/k_end 이월은 유지(이후 라운드 정산 무영향).
selftest ⑭/⑭'(암전 차단 + 직전이월 보존 대조) 추가, 17/17 통과 + pytest 27/27.

**검증(캐시 재평가, 재스캔 0회)**: 프로덕션 복사본에 재계산 → `_tp_diff --compare-to r10_gt107`
**유지79/획득0/상실0(TP Δ0)**, 검출 100→97. 제거된 3건 전부 사용자 검수 FP와 일치:
`2026-05-26 23-45-51 R12`(주 타깃), `2026-03-29 03-12-36 R48`(아침 조사 건 — 같은 지문이었음),
`2026-04-27 22-50-54 R69`(kill_shortfall로 분류했던 것도 실은 같은 원인). **precision
79.0%→81.4%**(GT108 기준). 프로덕션 `hud_timeline` 재생성 반영 완료, 해당 FP 클립 3개
`ace_clips_flat`에서 제거(117개 남음, 이 중 2개는 재검수 대기 재추출본).

**남은 사용자 확정 FP**: 15 - 3 = 12건 (kill_shortfall 6, boundary_merge 4, inconclusive 2 —
inconclusive 2건은 꼬리버퍼 늘린 재추출본 재검수 대기).

## ⏳ 다음 세션 TODO — 미스캔 영상 15개 (2026-07-19 보류, 사용자 게임 중이라 무거운 작업 연기)

`E:\OBS`에 최근 영상 15개(2026-07-01~07-15, 합계 49.5GB)가 **한 번도 스캔된 적 없음** —
`batch_hud_ace_pipeline.py`로 스캔해야 이 안의 트리플킬/올킬 클립이 생성됨(영상 디코딩+OCR/CNN
필요, 무거운 작업 — 07-18 GT51 캐시 구축 51개가 17~25h 걸렸던 걸 참고하면 15개는 그보단 짧겠지만
가볍지 않음, 실행 전 유저에게 타이밍 확인할 것). 목록:
`2026-07-01 01-28-52`, `2026-07-05 03-47-44`, `2026-07-11 00-14-53/00-36-11/01-04-44/02-46-11/
02-55-26`, `2026-07-12 00-11-57`, `2026-07-14 22-01-30/22-34-15/22-36-39`,
`2026-07-15 00-08-59/00-47-57/02-04-54/02-27-05`.

기존 스캔된 영상들의 하이라이트 클립(275개, `E:\clipai_result\ace_clips_hud`)은 생성 완료 —
사용자 검수만 남음.

## ✅ 2026-07-19 클립 최대길이 55s→120s 확대 + 프로덕션 재추출 + E: 드라이브 풀 사고 수습

R16 육안검수(아래 절)에서 `_CLIP_MAX_SEC=55`가 넓은 스팬 라운드의 첫 킬 직전 버퍼를 밀어내
킬 장면 자체가 잘리는 걸 발견 → **`detect_ace_hud.py`에서 120초로 상향**(사용자 확정: 대신
라운드 경계 오검출 시 클립이 최대 2분까지 길어질 수 있음, 인지 후 승인). 검증: 현재 프로덕션
156개 ace 라운드 중 69개(44%)가 구 55s 캡에 잘려있었음, 신규 120s 캡에서는 1건만 캡 도달(정상
범주의 진짜 긴 라운드).

**프로덕션 재추출 중 E: 드라이브 완전 풀 사고**: `files/_reextract_ace_clips.py`(신규, 재스캔
없이 기존 hud_timeline JSON + 원본 영상으로 `extract_ace_clips` 재호출)로 156개 재추출 실행 →
88개 성공 후 여유공간 4KB까지 소진, 32개 `ffmpeg -c copy`가 "No space left on device"로 실패.
**`-y` 플래그가 출력 파일을 먼저 truncate하고 쓰기 때문에, 실패한 32건은 기존(55s 캡) 정상
클립까지 0바이트 근처로 같이 날아감**(원본 영상은 안 건드림 — 재추출로 복구 가능, 데이터
영구손실 아님). OBS 프로세스는 당시 미실행 확인(실시간 녹화 파손 아님).

**공간 확보**: (1) 조사용 임시폴더 `_precision_investigate`(596MB) 삭제. (2) 그래도 부족해서
`E:\OBS`에서 **GT 아니고 스캔완료·ace 0건(클립 없음) 확인된 가장 오래된 영상 5개** 삭제(사용자
승인, "하이라이트 폴더는 보존" 조건):
`2026-03-28 18-07-58`(3.04GB), `2026-03-28 23-55-49`(1.77GB), `2026-04-08 23-48-49`(3.40GB),
`2026-04-09 00-20-26`(6.84GB), `2026-04-10 02-39-01`(1.85GB) — 합계 16.9GB, GT 영상은 전부 보존.
여유공간 4KB → 17.5GB 확보 후 `files/_retry_failed_clips.py`(신규)로 32건 전부 재시도 → **32/32
성공**. 최종 `ace_clips_hud` 총 275개 클립, 여유공간 15GB 유지.

**⚠ 후속 주의**: `_reextract_ace_clips.py`/`_retry_failed_clips.py`류 배치 클립 작업 실행 전엔
**항상 여유공간부터 확인**(`Get-PSDrive E`) — 이번처럼 대량 ffmpeg 배치 도중 풀 나면 기존 정상
파일까지 truncate되는 사고 재발 가능. 프로덕션 클립 246→275개 갱신됐으니 최종 산출물은
`E:\clipai_result\ace_clips_hud`(276→275, `_miss_review` 서브폴더 제외 기준) 참고.

## ✅ 2026-07-19 precision 하락 원인 규명 — R10 게이트가 새 FP 3건 생성 (재스캔 없이 캐시로 확인)

07-18 절에서 미조사로 남았던 "precision 78.0%(80.2%에서 하락)" 원인 조사 완료. 영상 재스캔 없이
**캐시(sig_cache_v2)만으로 gate-off 상태를 재구성**해 비교(`hud_from_cache.py`에 `--no-boundary-score-gate`
플래그 추가 — 기존 기본값 True는 안 건드림, opt-in 실험용). CPU 부하 거의 없음(초 단위, 영상 디코딩 0회).

- **방법**: `hud_from_cache.py --out-dir .../gate_on`(현재=r10_gt107 재현, 검증: recall/precision
  수치 78.0%/72.9% 완전 일치 확인) vs `--no-boundary-score-gate --out-dir .../gate_off`(R10 이전
  상태 재현, 77/96=80.2% 일치 확인) → `files/_r10_fp_diff.py`(신규)로 gate-on의 FP 22건 각각이
  gate-off 쪽에 시간대 겹치는 검출이 있는지 대조.
- **결과**: FP 22건 중 **19건은 R10과 무관한 기존 FP**, **3건이 R10 게이트가 새로 만든 FP**:
  - `2026-03-29 01-01-04` R26(17:19-18:28) — OFF에서는 1킬 라운드 + 2킬 라운드(width0, n=4)로
    분리돼 있었는데, ON이 경계를 병합해 kills=3 + n=33(G5 문턱 10 통과)으로 승격.
  - `2026-03-29 03-12-36` R48(28:33) — OFF에 이미 width0 3킬(n=7, **G5 문턱 미달로 원래 기각됨**)이
    있었는데, ON의 병합이 n을 11로 올려 G5를 통과시켜버림.
  - `2026-05-17 21-47-39` R16(10:20-11:04) — OFF의 1킬+2킬 별개 라운드를 ON이 병합해 kills=3.
- **공통 원인(가설)**: 세 건 모두 R10이 "경계 근방에 승수(win) 이벤트 없음"으로 판단해 병합한
  경우 — 그런데 R6 폭0 게이트(`_SCORE_SPLIT_MIN_EVENTS`)도 같은 win 이벤트 신호를 보는데 걸러내지
  못한 걸 보면, 이 구간들은 **승수 OCR 자체가 미탐(scoreboard 판독 실패)** 이었을 가능성이 높음 —
  탭-메뉴 오탐(R10이 원래 겨냥한 사례)이 아니라 다른 실패 모드. 미확정 — 검증하려면 해당 3영상
  구간을 육안 확인하거나 score_cache 원시 로그를 봐야 함(다음 세션 과제, 영상 재생 필요 없이
  `score_cache` JSON만 봐도 될 수 있음 — 캐시 존재 여부 다음 세션에서 확인).
- **산출물(임시, git 미추적)**: `E:\clipai_result\_precision_investigate\{gate_on,gate_off}\*.json`,
  `files/_r10_fp_diff.py`. `files/hud_from_cache.py`에 `--no-boundary-score-gate` 플래그 추가(기본
  동작 불변).

**후속: 사용자 육안 검수(같은 날) — 클립 3개 추출(`files/_extract_r10_fp_clips.py`, ffmpeg
`-c copy` 재인코딩 없음) 후 K/D HUD 직접 확인. 결과로 가설 수정됨:**

- `2026-03-29 01-01-04` R26 (17:19-18:28): **FP 확정**. 실제 K/D 6/6→7/6, 킬 1회뿐. OFF 상태의
  "1킬+2킬(width0,n=4)" 중 2킬 쪽부터가 이미 과다판독이었고, R10 병합이 그 위에 얹혀 3킬로 승격.
- `2026-03-29 03-12-36` R48 (28:33): **FP 확정, 더 심각**. 실제 K/D 17/9→17/10 — **킬이 아니라
  데스**였음. width0 "3킬"(K 불변인데 킬 이벤트 3개 기록) 신호 자체가 완전 오독 — R10 병합은
  이미 틀린 신호를 G5 문턱만 넘겨준 것뿐, 근본 버그는 다른 곳(데스 이벤트가 K 채널 점프로
  잘못 잡히는 경로)에 있음. **다음 세션 조사 후보**(원인 미상, `read_kda_triple_from_game`/
  `hud_kda.py` 쪽 의심 — 아직 코드 미확인).
- `2026-05-17 21-47-39` R16 (10:20-11:04): **FP 아니었음 — 실제 트리플킬**. K/D 10/10→13/10,
  데스 없음(단 10→11 전환 순간은 영상에 거의 안 잡히고 이미 잡은 직후부터 노출 — 판독 난이도만
  있었을 뿐 이벤트 자체는 진짜). **GT 누락으로 판명** → `files/gt_aces.json`에 스팬 `[620, 665]`
  추가(사용자 확인 2026-07-19), `_tp_baselines/r10_gt107.json` 재저장.
  **GT 107→108, TP 78→79, precision 78.0%→79.0%**(재계산: `python -u _compare_hud_gt.py`).

**R10 실제 순효과(수기 검증 반영, 3건 중 2건 FP 확정 + 1건 GT 편입)**: TP **+2**
(`02-55-36` 07-17 절 + 이번 `21-47-39` R16), FP **+2**(R26, R48) — 애초 추정(TP+1/FP+3)보다 낫게
정정됨.

**남은 다음 TODO**:
1. `2026-03-29 03-12-36` R48 데스→킬 오독 버그 원인 조사 (코드 레벨, 재스캔 불필요할 수도 있음 —
   해당 구간 원시 K/D read를 `hud_from_cache.py --dump "2026-03-29 03-12-36" 28:00 28:40`로
   먼저 찍어볼 것).
2. R26/R48 두 건에 대한 후속 가드 검토(병합 라운드는 G5 문턱을 원 세그먼트 기준으로 재적용 등)
   — 선택 사항, 필요성은 낮음(FP 2건뿐).

## ✅ 2026-07-18 GT51 sig_cache_v2 일괄 구축 완료 — TP 77→78 순증(회귀 0), 원본 삭제 가능

T4 각주의 후속 대기 작업(원본 삭제 전 마지막 보존 기회) 완료. `_gt_source_audit.py`로
재확인해 원본 있는 51영상(E:\OBS 50 + D:\ 1) 전부 sig_cache_v2 없음을 먼저 확인했음
(기존 sig_cache_v2 9개는 전부 원본 소실된 다른 GT라 무관). E: 드라이브 여유 7.4GB뿐이라
**`--no-extract`로 클립 추출은 생략**, 캐시+JSON만 저장(사용자 승인 완료).

- **실행**: `files/_gt51_cache_stems.txt`(OBS 50개 stem) 대상 단일 프로세스
  `--stems-file ... --redo --no-extract` 백그라운드 실행(17~25h 예상대로 소요) →
  46/50 성공, 4건은 `--min-duration-sec` 기본 120s에 걸려 스킵(전부 GT 0구간짜리
  초단편 영상: 74s/5s/4s/13s) → `--min-duration-sec 0`으로 개별 재실행해 4건 전부
  캐시 확보. 이어서 D:\ 소재 `2026-01-08 02-33-22` 1건도 `--obs-dir D:\` 로 별도
  단일 실행(단일 프로세스 유지 위해 OBS 배치 완료 후 순차 진행) → **51/51 전부
  sig_cache_v2 확보** (`ls sig_cache_v2` distinct stem 60개 = 기존 9 + 신규 51).
- **검증**: `_tp_diff.py --compare-to r10_cleanbase` → 유지77 획득1 상실0,
  **TP 77→78(Δ+1)**. 획득분은 `2026-03-30 02-55-36|17:14-18:00`(R10 탭-스코어보드
  가짜경계 회수, 07-17 절에서 51-subset 평가로 이미 확인했던 바로 그 사례) —
  이번엔 **GT107 전체 hud_timeline 기준으로 R10 효과가 처음 실측 반영**된 것뿐이라
  회귀 없음. E: 여유공간 7.4GB 그대로(클립 미생성 확인).
- **✅ 2026-07-19 사용자 승인**: `files/_tp_baselines/r10_gt107.json`(78/107)을 정식
  baseline으로 확정. 향후 판독 경로 변경 시 TP 무변화 증명은 이 파일 기준
  (`--compare-to r10_gt107`)으로 하고, `r10_cleanbase.json`(77/107, 07-16)은
  낡은 참조로 격하.
- **원본 삭제 보류**: sig_cache_v2로 재검증 가능은 하지만, **사용자가 원본은 일단
  킵하기로 결정(2026-07-19)** — 드라이브 정리 진행하지 말 것.

## 현재 유효한 사실 요약 (T7 §E-1, ≤30줄 — 아래 절 전체를 읽기 전 먼저 볼 것)

- **현재 정식 TP 베이스라인: 78/107**(recall 72.9%, **precision 78.0%(78/100) — `r10_cleanbase` 대비
  80.2%에서 하락**, 검출 총량이 96→100건으로 늘면서 recall 대비 FP 비중이 커짐) —
  `files/_tp_baselines/r10_gt107.json` (2026-07-18 스냅샷, **2026-07-19 사용자 승인으로 정식
  baseline 확정** — 위 "2026-07-18" 절 참고).
  ⚠ precision 하락분은 아직 원인 미조사(R10이 어디선가 FP 4건을 새로 만들었을 가능성) —
  다음 세션에서 `r10_cleanbase` vs `r10_gt107` 검출 목록(ace_rounds 전체, GT 미매칭 포함) diff로
  어떤 FP가 늘었는지 확인할 것 — 이미 저장된 `E:\clipai_result\hud_timeline`(현재=r10_gt107
  상태) JSON을 읽기만 하면 되므로 재스캔 불필요(가볍게 실행 가능).
  판독 경로를 건드릴 때마다 `python -u files/_tp_diff.py --compare-to r10_gt107`로 TP
  무변화 증명할 것(`r10_cleanbase`·`r8_final`은 이제 낡은 참조).
- **원본 삭제는 보류 중**(2026-07-19 사용자 결정) — sig_cache_v2로 재검증 가능한 상태지만
  GT 51영상 원본은 당분간 유지. 드라이브 정리 작업 시작하지 말 것.
- **R10 게이트(`verify_boundary_wins`)는 2026-07-17부터 기본 활성** — GT 51영상 재평가
  (유지54/획득1/상실0, TP 손실 0 원칙 통과) 검증 후 전환. 끄려면 `--no-verify-boundary-wins`.
  상세는 아래 "2026-07-17 R10 재평가 완료" 절.
- **배치 재스캔 2-way 병렬 중 새 I/O 버그 발견·수정**: `collect_reads()`(디코딩 메인 루프)가
  read 실패를 곧장 EOF로 취급해 영상 뒷부분이 경고 없이 통째로 잘리는 사고 실측
  (`2026-05-31 21-57-14`, 163→66라운드 절단) → 재시도 1회 + `early_break_*` 경고로 수정
  (`verify_runs_live`의 07-16 fail-open 계약과 동일 사고). 기존 클린 베이스라인 97건은
  감사 결과 이 버그에 안 걸렸음(오염 없음).
- **배치 재스캔은 원칙적으로 단일 프로세스** — 위 I/O 버그 수정 전 병렬 재스캔이 결과를
  조용히 오염시킨 사고(아래 "2026-07-16 정정" 절) 이후, `batch_hud_ace_pipeline.scan_lock`
  으로 동시 3번째 실행부터 자동 거부(`--force-parallel`로만 우회). 2-way까지는 안전이
  검증된 범위(위 I/O 버그 수정 후).
- **`SONNET_TASKS.md`가 작업 명세·진행상황의 단일 소스** — T1~T7 전부 완료(2026-07-16~17):
  requirements 보수, GT→`files/gt_aces.json` 이관, `files/tests/` pytest 27건, 신호캐시를
  스캔 경로에 배선(`hud_cache_io`), 동시실행 락, `E:\clipai_result\review_ledger.csv`(326행),
  저장소 위생(레거시 태깅·미추적 분류표).
- ~~**후속 대기(사용자 승인 필요, T4 각주)**: 원본 있는 GT 51영상 캐시 일괄 구축~~ →
  **✅ 완료(2026-07-18)**, 위 "2026-07-18" 절 참고. 51/51 sig_cache_v2 확보, 원본 삭제 가능.
- 아래 **"2026-07-15 R10: 미탐 34건 분석" 절은 폐기됨**(`[폐기됨 → 07-16 "정정" 절 참고]` 표시) —
  "R5 CNN 검증기 연쇄 기각 추정"은 틀렸고, 진범은 병렬 재스캔 I/O 타임아웃 × fail-closed 버그.
- 구조적 원인·근거는 `IMPROVEMENT_REPORT.md`, 작업 단위 명세는 `SONNET_TASKS.md` 참고.

## ★★★★★ 2026-07-17 R10 재평가 완료 — 기본 활성 전환 + collect_reads I/O 절단버그 발견·수정

**배경**: 07-16 클린 베이스라인(77/107 TP) 위에서 R10(`verify_boundary_wins=True`) 재평가를
GT 51영상(원본 있는 것 전부, E:\OBS 50 + D:\ 1)으로 실행. 이전 세션이 토큰 한도로 끊기기 전
25/51을 이미 게이트-ON으로 스캔해뒀던 것을 발견 → 알려진 탭-병합 케이스(`02-55-36`,
`22-46-39`)의 라운드 경계가 클린 베이스라인과 실제로 다른 것을 확인해 진짜 게이트-ON
결과임을 검증하고 재사용, 남은 26개만 2-way 병렬로 마저 스캔.

**1차 결과(오염됨)**: `_tp_diff --dir _r10_eval/hud_timeline --compare-to r10_cleanbase` →
유지53 획득1 **상실24** — 그러나 상실 24건 중 23건은 원본 소실 10영상(애초에 51개 스캔
대상 밖이라 `_r10_eval`에 파일 자체가 없음)이 diff 비교 범위에 잘못 끼어든 측정 방식
문제였고, 남은 1건(`2026-05-31 21-57-14`)은 **2-way 병렬 스캔 중 진짜 데이터 손실**로 드러남
— 로그에 `Stream timeout triggered`(ffmpeg, 30초) 발생, 정상 163라운드짜리 결과가
66라운드(4067s 지점)에서 경고 없이 뚝 끊김.

**근본 원인**: `detect_ace_hud.collect_reads()`(영상 1패스 디코딩 메인 루프,
당시 `detect_ace_hud.py:654-656`)가 `cap.read()`/`cap.grab()` 실패를 재시도 없이 곧장
"영상 끝"으로 취급 — I/O 타임아웃이 EOF와 동일하게 처리돼 영상 뒷부분이 통째로 사라짐.
07-16에 고쳤던 `verify_runs_live`의 fail-closed 버그와 **동일한 클래스, 다른 위치**의
미해결 버그였음. 기존 클린 베이스라인(97개 영상, ffprobe 실측 길이 대조)은 감사 결과
이 버그에 안 걸렸음을 확인 — 오늘 이 특정 2-way 병렬 실행에서만 우연히 노출됨.

**수정**(`detect_ace_hud.py`): read/grab 양쪽에 1회 재시도 추가(`verify_runs_live`와 동일
계약). 재시도 후에도 실패하고 실제 프레임카운트 기준 길이보다 5초 넘게 일찍 끝나면
`early_break_Xs_of_Ys` 경고를 반환(진짜 EOF는 조용히 종료, 오차 <5s 허용 — 컨테이너의
프레임카운트 추정 오차 범위). `files/tests/test_collect_reads_failopen.py` 4건(transient
read/grab 복구, persistent 실패 시 early_break 보고, 정상 EOF는 무경고).

**손상된 영상 재스캔**(단일 프로세스): `2026-05-31 21-57-14` 완전 재스캔 → R153(147:48)까지
정상 완주, 클린 베이스라인과 길이 일치.

**최종(정정) 결과**: GT 51영상 범위 내에서만 비교 —

```
유지 54 | 획득 1 | 상실 0     범위내 TP 54 → 55 (Δ+1)
  +획득 2026-03-30 02-55-36|17:14-18:00 (탭-스코어보드 가짜경계 회수 — R10이 겨냥하던 정확한 사례)
```

TP 손실 0 원칙 통과 + 순증 1건. 다만 애초 가설(§ "인접 라운드 킬 합==3" 16건 후보) 대비
실제 회수는 1건뿐 — 나머지는 원본 소실 10영상 쪽에 있거나 가설이 과했던 것으로 추정,
별도 검증 불가.

**조치**: `timeline_from_reads`/`scan_hud_aces`의 `boundary_score_gate`/`verify_boundary_wins`
기본값을 **True로 전환**(사용자 확인 후). `batch_hud_ace_pipeline.py`는
`--verify-boundary-wins`(opt-in) → `--no-verify-boundary-wins`(opt-out)로 CLI 반전.
`hud_from_cache.py`는 이 인자를 안 넘기므로 자동으로 새 기본값(True) 적용.

**남은 일**: GT 61영상 중 10개(원본 소실)는 R10 효과를 영구히 검증 불가 — recall 분모에는
계속 포함됨(§D-1 참고). `_MAX_CONCURRENT_SCANS`를 3 이상으로 올리는 건 이번 I/O 버그
수정과 별개로 아직 근거 없음 — 2-way까지만 검증됨.

## ★★★★★ 2026-07-16 클린 재스캔 완료 — recall 68.2%→72.0%, TP 손실 0, 획득 8건 (fail-open 수정 효과만으로)

fail-open 수정(아래 절) 후 GT 50영상(원본 있는 것 전부)을 **2-way 병렬**로 재스캔,
타임아웃 0건으로 클린 완료. `_tp_diff --compare-to r8_final`:

```
유지 69 | 획득 8 | 상실 0     TP 69 → 77 (Δ+8)
recall 68.2%(73/107) → 72.0%(77/107)     precision 80.2% 유지(77/96)
```

**R10(`verify_boundary_wins`)은 이 측정 내내 기본 비활성 상태였다** — 즉 이 +8건은
전부 fail-open 버그 수정 자체의 효과. CNN 경계검증기가 배치에 연결된 2026-07-10
이후 계속 recall을 깎아먹고 있었을 가능성(병렬 스캔으로 처음 노출됨). 클린
베이스라인 저장 완료: `files/_tp_baselines/r10_cleanbase.json` (77/107 TP).

**다음**: 이 클린 베이스라인 위에서 R10(`verify_boundary_wins=True`) 재평가 — 이번엔
안전하게 측정 가능(I/O 타임아웃 요인 제거됨). 미탐 34건 분석(아래 R10 절)에서 나온
"인접 라운드 킬 합==3" 16건이 회수 후보. `_tp_diff --compare-to r10_cleanbase`로
비교할 것(`r8_final` 아님 — 그건 이제 낡은 참조).

## ★★★★ 2026-07-16 정정: "TP 손실"의 진범 = 병렬 재스캔 I/O 타임아웃 × CNN 검증기 fail-closed 버그 (수정 완료) — 어제 결론 뒤집힘

**어제(07-15) "R5 CNN 경계검증기의 연쇄 기각 버그 추정"은 틀렸다.** 오늘 실측으로 확정:

- **단일 프로세스 감사**(`files/_audit_boundaries.py`, `01-26-52`): row_miss 후보 99개,
  CNN 진짜 82/가짜 17, **연속 기각 최대 2** → CNN 캐스케이드는 존재하지 않음.
  경계 82개는 베이스라인(81+)과 일치. 문제의 R80 올킬 경계(58:33, 21프레임)도
  CNN prob 0.87~0.97로 정상 유지 판정.
- 그런데 어제 **6중 병렬**(`xargs -P 6`) 재스캔 JSON은 같은 영상이 경계 50개 —
  로그에 ffmpeg `Stream timeout` 12+16건. **6개 디코더가 E: 드라이브를 동시에
  때려 read가 실패**했고, `verify_runs_live`가 read 실패를 "스코어보드 없음"과
  동일 취급(best=0.0 유지 → False)해 **진짜 경계를 조용히 폐기**(fail-closed).
  경계 14~39개/영상 증발 → 라운드 수십 분 단위 병합 → K체인 붕괴 → 올킬 소실.
- **따라서 어제 GT 61영상 측정(TP 69→67/68)은 전부 무효** — R10 탓이 아니라 측정
  자체가 오염. R10의 진짜 효과는 아직 미측정.
- **수정 완료**(`files/hud_boundary_verify.py`): ① 기각(False)은 **3장 전부 판독됐을
  때만**(증거 불완전 시 무조건 경계 유지 — 기각은 비가역·유지는 R5 이전의 안전한
  동작이라 비용 비대칭) ② read 실패 시 재시도 1회. 결함주입 테스트로 실증
  (read 전부 실패 → 종전 False, 수정 후 True).
- **교훈: 이 파이프라인의 배치 재스캔은 병렬 금지(단일 프로세스, 최대 -P 2).**
  I/O 경합이 결과를 조용히 오염시킴 — 속도보다 측정 신뢰가 우선.

### 다음 작업
1. ~~오염된 hud_timeline JSON 재생성~~ → ✅ 완료(2026-07-16, 2-way 병렬, 타임아웃 0).
2. ~~클린 측정·베이스라인 저장~~ → ✅ 완료 (`r10_cleanbase.json`, 77/107 TP).
3. **R10(`verify_boundary_wins=True`) 재평가** — 위 "2026-07-16 클린 재스캔 완료" 절
   참고. `_tp_diff --compare-to r10_cleanbase`로 비교.

## [폐기됨 → 위 "2026-07-16 정정" 절 참고] ★★★ 2026-07-15 R10: 미탐 34건 분석 → 라운드 경계 오류 규명, 승수 교차검증 게이트 구현(기본 비활성)

**세이브 감지는 종결**(사용자 결정) — 올킬 아닌 세이브는 실질적으로 없다시피 함(1건 예외).
대신 **올킬 미탐 34건**(recall 68.2%, GT107)을 파고듦.

### 미탐 34건 원인 분류 (`files/_miss_diag.py`)
- **KILLS_LOW 33건**(라운드는 정확히 찾았는데 킬 수 과소판독) vs ACE_FALSE 1건(과대판독).
- **라운드 분할 실패는 0건** — 미탐은 전부 "라운드 안에서 킬 카운팅 과소" 문제.
- KILLS_LOW를 kills 값으로 쪼개면 kills=2(하나만 더 잡으면 올킬)가 12건으로 최다.

### 메커니즘 A 규명·구현: 탭 스코어보드가 가짜 라운드 경계를 만듦
- **실측 확정**(사용자 육안 확인, `02-55-36` 17:11, `22-46-39` 41:06): 탭(Tab)으로
  스코어보드를 잠깐 연 것도 상단 HUD를 가려 `row_miss` run을 만들고, R5 CNN
  경계검증기가 이걸 진짜 스코어보드로 오인 → 진짜 3킬 시퀀스가 두 라운드로 쪼개져
  ΔK==3을 못 만듦. 미탐 33건 중 16건이 "인접 라운드 킬 합==3" 지문과 일치(단,
  이 휴리스틱엔 오탐도 섞여있음 — 예: `02-43-33` 8:21은 실제로는 다른 원인).
- **독립 검증 신호 발견**: 진짜 라운드 종료는 상단 승수(WIN 카운터,
  `hud_score_wins.py`)가 반드시 오르고, 탭 가림은 승수가 안 오름(대조군 59건 실측:
  오프셋 대부분 -5~-6s, 범위 -23.6~+21.2s). 앵커(`879,0`) 자체는 탭에 안 가려짐 —
  승수만 유효한 신호.
- **구현**: `detect_ace_hud.py` `_has_nearby_win_event` + 경계 루프에 게이트 추가
  (margin -28/+24s). 단발 사례(`02-55-36`, `22-46-39`)에서 병합 성공 확인.

### ★ 실측 결과: TP 손실 발견 → 게이트 기본 비활성으로 되돌림
- GT 61영상 전체 재스캔(2026-07-15, ~6시간): **TP 69→67(Δ-2)**, 획득 9건(A 케이스
  포함) vs 손실 11건.
- **원인 분석**: 손실 라운드들이 수십 초~22분짜리로 거대 병합됨. "연속 경계 2개
  이상 연쇄 기각 금지" 안전핀 추가 → 손실 11→10건으로 소폭 개선(획득 9 유지).
- **결정적 증거**: `01-26-52`는 점수 앵커 탐지 자체가 실패(`find_score_anchor`→
  `None`)해서 R10 게이트가 원천적으로 비활성인데도 R80(58:05-58:31 올킬)이 8.5분
  라운드에 먹혀 사라짐. `03-23-57` 등은 앵커·승수 이벤트가 충분한데도(3400~3900s
  구간 11개) 안전핀 적용 후에도 손실 그대로 — **안전핀(R10 전용 보호)이 못 막는
  범위에서 동일하게 발생 = R10이 아니라 선행 원인**.
- **결론(잠정)**: 진범은 **R5 CNN 경계검증기(`hud_boundary_verify.py`)의 무제한
  연쇄 기각**으로 추정. 이 영상들이 R7/R8 이후 **한 번도 전체 재스캔된 적이
  없어서**(r8_final 베이스라인이 부분 rescan 상태였음) 지금까지 안 드러났던
  잠복 버그. 오늘 처음 GT 61영상 전체를 재스캔하면서 우연히 노출됨. **미해결**.
- **조치**: `boundary_score_gate` 파라미터 신설, **기본 `False`**(비활성) —
  `scan_hud_aces(..., verify_boundary_wins=True)`로만 켜짐. `hud_from_cache.py`는
  이 인자를 안 넘기므로 자동으로 비활성. R6 폭0-ace 기각 게이트(이미 검증됨)는
  이 스위치와 무관하게 항상 켜져 있음 — 회귀 없음.
- **⚠ 디스크 상태 주의**: 오늘 재스캔한 ~54개 영상의 `E:\clipai_result\hud_timeline\*.json`은
  R10이 **켜진 상태**로 생성됐음(안전핀 적용 버전). 코드 기본값은 이제 꺼짐이지만
  JSON은 아직 그 결과를 반영 중 — recall/precision 재측정 전엔 오늘 수치를
  신뢰하지 말 것. 다음 세션에서 CNN 캐스케이드 버그를 고치거나, 최소한 영향받은
  영상들을 `verify_boundary_wins=False`(기본값)로 재스캔해서 클린 베이스라인부터
  다시 잡을 것.

### 다음 세션 우선순위
1. **R5 CNN 경계검증기 연쇄 기각 버그 조사·수정** — `hud_boundary_verify.py`,
   `_lookup_boundary_verdict` 호출부. 안전핀과 동일한 패턴(연속 기각 금지)을
   적용할 수 있는지 검토. 이게 잠재적으로 가장 큰 회수(11건 손실의 대부분).
2. 위 버그 수정 후 GT 61영상 재측정 → R10(`verify_boundary_wins=True`) 재평가.
3. kills=2 표본(12건)의 프레임 단위 원인 조사는 미착수 — 세 번째 킬이 라운드
   막판 어디서 왜 빠지는지 아직 안 봄.
4. GT 충돌 6건 중 5건 정탐 확정·1건(01-01-04 17:15) GT 제거 완료(2026-07-15,
   `files/_compare_hud_gt.py`) — GT 108→107건.

### 새 진단 도구 (커밋됨)
- `files/_miss_diag.py` — 미탐 GT 구간 → HUD 타임라인 라운드 매칭 → 원인 분류
- `files/_probe_wins_window.py` — 구간별 승수(R/B wins) 프레임 단위 덤프
- `files/_probe_anchor_window.py` — 구간별 점수 앵커 매치 신뢰도 프레임 단위 덤프

## ★ HUD 올킬 파이프라인 (2026-07-14 R8 완료 — recall 67.6%, precision 80.2% ★목표 80% 돌파)

**닉·스코어보드 없이** HUD K/D/A로 올킬 탐지. R5(66.7%/71.1%)→R6(66.7%/68.8%,
부분 성공)에 이어 **R7(2026-07-14)이 폭0 FP(3킬이 한순간에 몰리는 오탐)의
지배적 원인을 찾아내 큰 폭으로 해결**.

- **현재 성능(GT 107건, 2026-07-15 GT 충돌 정리 후)**: recall 68.2%(73/107),
  **precision 80.2%(73/91) — 원래 목표(80%) 유지**. 베이스라인 `r8_final`
  (69/97, GT 확장 전) 대비 상실 0. R7(8 블라인드 carry 보정) 위에
  **R8(8을 위상topology으로 직접 판독, `hud_digit_match.probe_eight_topology`)**
  + 사용자 육안 4차 GT 확장(+10건, 15영상)까지 반영된 수치.
- **R9 검증기**(`files/_ace_verifier.py`, 보조도구): ace 후보에 P(진짜) 점수 —
  검수 큐 정렬용(자동 기각 금지). LOVO AUC ~0.69(약함), 미검수 56건 큐는
  `E:/clipai_result/ace_verifier_scores.csv`.
- **GT 충돌 6건 해결(2026-07-15 사용자 재확인 완료)**: 01-01-04 6:28,
  02-43-33 8:21, 23-58-38 1:18:37/1:22:00, 02-15-28 25:58 → **전부 정탐(올킬) 확정**,
  GT 유지. 01-01-04 17:15만 **실제로는 4→6 더블킬(2킬)**이고 끝은 플레이 중
  녹화 끊김 → GT에서 제거(GT 108→107건). `files/_compare_hud_gt.py` 반영 완료.
- **R7 핵심 발견** (상세: [`HUD_ACE_HANDOFF.md`](HUD_ACE_HANDOFF.md) §0 "R7" 절):
  1. **사용자 육안 검수로 R6의 "폭0 FP는 여러 원인 혼재" 결론이 틀렸음이 드러남**
     — 폭0 오탐 11건 중 9건이 전부 동일 지문: 실제 킬은 8→10(2킬)·9→10(1킬)·
     10→10(0킬) 등인데 시스템이 전부 "7→10(+3 가짜 올킬)"로 오판.
  2. **근본 원인 = "8 블라인드"** — 숫자 8은 0/6/9와 혼동돼 구조적으로 판독
     제외돼 있어서(`EXCLUDE_DIGITS`), 누적 K가 8을 지나면 시스템이 마지막
     깨끗값 7에 얼어붙었다가 다음 깨끗값 10에서 +3으로 오판.
  3. **수정**: 이미 "가짜 0"으로 격리만 하던 8의 흔적을 **carry 전진 증거로
     재활용**(`hud_round_settle.py` `_EIGHT_EVID`) — 7→10을 8→10으로 정정.
     gap-path 전용이라 킬 수를 줄이는 방향으로만 작동 → 정탐은 원리적으로 안 건드림.
  4. 나머지 2건은 오탐이 아니라 **GT 누락**(진짜 올킬)으로 확인돼 GT에 추가.
  5. **결과**: 원본 있는 12개 영상에서 폭0 FP가 사실상 전멸(17건→6건, 남은
     6건은 전부 원본 삭제된 라벨10 영상이라 검증 불가한 잔재).
- **코드**: `hud_round_settle.py`(`_EIGHT_EVID`, selftest 15/15), `_compare_hud_gt.py`
  (GT 2건 추가), `_r7_rescan_todo.txt`(재스캔 대상 12개 영상, 영구 보존)
- **R6(2026-07-13)도 유지**: 상단 스코어 앵커 판독(`hud_score_wins.py`) —
  자체 효과는 작았지만(FP 1건) 인프라(`_TOP_Y` 재보정 등)는 유효하게 남아있음.
- **하위 AI 작업 명세**: [`SONNET_TASK.md`](SONNET_TASK.md) R6 절 완료됨. R7은
  이번 세션에서 명세 없이 바로 구현·완료(빠른 반복).
- **클립 폴더**: `E:\clipai_result\ace_clips_hud\` 평탄화(`<날짜>_하이라이트(n).mp4`),
  오탐 `_오탐\`, 미탐 검수용 `_miss_review\`(43건), 폭0 검수용 `_폭0오탐_검수용\`(11건)
- **사용자 육안 검수 원본 데이터** — ⚠ 이미 완료·기록됨, 다시 요청하지 말 것:
  - 오탐 16건 K/D 흐름 답변 → `files/_reorg_highlights.py`의 `FP_SET` 주석
  - 미탐 43건 K/D 흐름 답변 + 판정 → [`MISS_FEEDBACK_FORM.md`](MISS_FEEDBACK_FORM.md)
    (12건 복구/1건 GT제거/30건 아직미탐, 클립별로 전부 기록됨)
  - 폭0 오탐 11건 K/D 흐름 답변(9건 단일원인 확정, 2건 GT편입) → 위 R7 절 참고
- **다음 (남은 숙제)**:
  1. **E:\OBS 재처리 진행 중** — 75개 중 2026-07-13 세션에서 계속 처리 중
     (todo 리스트: `files/_obs_reprocess_todo.txt`). R7 로직이 반영됐으니 이어서
     돌리면 폭0 FP도 자동으로 줄어든 채 재스캔됨.
  2. **폭0 FP는 사실상 해결** — 남은 6건은 원본 삭제로 검증 불가한 잔재뿐.
  3. **경계 넘는 미관측 첫 킬** (02-55-36 11→14 유형), 세이브 감지, D드라이브 162개.

## 보고·도메인 규칙 (2026-06-26 확정)

### 시각 표기
- **초(s) 단독 사용 금지** — 보고·로그·문서는 **`M:SS`** (예: `3:37`, `29:01`)
- 코드 내부·CSV는 `start_sec` float 유지, **사람에게 보여줄 때만** 분:초 변환

### ΔK (라운드 간 킬 증가량) — 절대 규칙
- 서든어택 한 라운드에서 본인이 낼 수 있는 킬은 **최대 3** (올킬)
- 따라서 **정상 ΔK ∈ {0, 1, 2, 3}**
- **ΔK ≥ 4 → OCR/K 읽기 오류 또는 팀원 K 오귀속** — 올킬로 취급하면 안 됨
- ~~현재 `compute_delta_kills`는 `delta >= 3`으로 ace 판정~~ → **2026-06-26 수정 완료**: `delta == 3`만 올킬, `delta >= 4`는 `k_read_error` 플래그
- 올킬 판정은 **`ΔK == 3`만** (또는 도메인상 3킬 확정 시)
- **추론(팀폴백 `[T]`) 라운드는 올킬 판정에서 제외** — 서로 다른 팀원 K를 비교해 거짓 ΔK 생성 (R03 오탐 원인)

### ★ K 컬럼 OCR 버그 (2026-06-26 발견·수정) — 가장 중요

- **증상**: 괴명 실제 `K=0 D=3 A=0`인데 K를 **3(데스값)으로 오독** → 거짓 올킬(R52 등)
- **원인**: `_RED_K=(0.400,0.452)` ROI가 **K와 D 두 숫자를 모두 포함**, `_ocr_best`가 신뢰도 높은 D 숫자를 반환
- **수정**:
  - K 단독 ROI로 재보정: `_RED_K=(0.407,0.430)`, `_BLUE_K=(0.724,0.748)` (픽셀 측정 검증)
  - 단일 숫자(특히 0) 인식용 `_ocr_digit()` 추가 — Otsu 이진화 + allowlist (`scoreboard_layout.py`)
  - 검증: 괴명/희령/massacre/길증경/chal 전원 K conf 1.00 정확
- ⚠️ 처음엔 "WIN 화면이 스코어보드로 오분류" 로 오진했으나, 윈도우엔 진짜 스코어보드가 들어있었고 **K컬럼 위치 오류**가 진짜 원인이었음

## 1b 구현 현황 (2026-06-26)

### 완료 모듈

| 모듈 | 역할 | 상태 |
|------|------|------|
| `player_identity.py` | 영상 단위 닉 확정 (스카우터 + 스코어보드 투표) | ✅ v1 |
| `scoreboard_layout.py` | Clan Match 6행 ROI, 닉/K OCR 전처리 | ✅ |
| `nick_fuzzy.py` | 닉 정규화·fuzzy 매칭·클러스터 | ✅ |
| `scoreboard_k_reader.py` | 확정 닉으로 K 읽기, ΔK, 올킬 후보 | ✅ v1 |
| `detect_rounds.py` | 스코어보드 CNN 스캔 → 라운드 분할 | ✅ (순차 디코드) |
| `game_frame.py` | game_roi crop | ✅ |

### 핵심 개선 (이번 세션)

1. **닉 OCR**: `_ocr_nick` — 반전 이미지 우선 + CLAHE 폴백 (`scoreboard_layout.py`)
2. **fuzzy 점수**: `nick_match_score` = `1 - dist/max_len` (정규화 편집거리)
3. **fuzzy 허용**: 6글자 이상 닉 편집거리 3까지 (`nick_fuzzy.py`)
4. **팀 안전장치**: 2-pass pre-pass로 전역 `team_lock` 선수집 → 실패 라운드만 `[T]` 폴백 (`inferred_by_team` 플래그)
5. **K cross-frame**: 닉 매칭 프레임과 K 읽힌 프레임이 달라도 동일 `row_index`에서 K 보정
6. **detect_rounds**: 긴 영상 seek 병목 → **순차 디코드 + N프레임 샘플**로 교체

### 파일럿 결과

#### `2026-03-19 23-00-50` (~12분, 닉 `overclock`)

| 항목 | v2 (이전) | v5 (현재) |
|------|-----------|-----------|
| 직접 매칭 | 2/8 | **6/8** |
| 안전장치 `[T]` | 4~5 | **1** (R01) |
| `ace_rounds` | 잘못된 양성 | **[]** (이 구간 올킬 없음) |

출력: `E:\clipai_result\kill_timeline\2026-03-19 23-00-50_v5.json`

#### `2026-03-21 00-40-56` (~81분, 닉 `괴명`) — 장시간 파일럿

| 항목 | v1 (구) | **v3 (현재, K컬럼 수정)** |
|------|---------|--------------------------|
| `ace_rounds` | 12개 (대부분 오탐) | **2개** (R36, R73) |
| `k_error_rounds` | 0 | **3** (R30 ΔK=10, R38 ΔK=20, R75 ΔK=90) |

확정 올킬 (직접매칭 conf 1.00, 킬피드 육안 검증 완료):

| 라운드 | 시각(M:SS) | ΔK | 검증 |
|--------|-----------|-----|------|
| R36 | 38:02 | 3 (0→3) | 킬피드 `괴명→맘스터처…` ✓ WIN |
| R73 | 72:44 | 3 (2→5) | 킬피드 `괴명→길종경`(권총) ✓ WIN |

**제거된 오탐**: R52(53:08)는 실제 `K=0 D=3 A=0`인데 D를 K로 오독해 거짓 올킬이었음 → 수정 후 K=0, ΔK=0. R03도 팀폴백 오귀속으로 거짓 ΔK=3 → 수정 후 ΔK=1.

출력: `E:\clipai_result\kill_timeline\2026-03-21 00-40-56_v5.json` (v5 = 결합 OCR + 인접성 규칙)
클립: `E:\clipai_result\ace_clips\2026-03-21 00-40-56\` (R36, R73 — 라운드 플레이 35초 + 확인 스코어보드)
ROI 검증 샘플: `E:\clipai_result\roi_check\` (랜덤 R05/R11/R74 — K셀 크롭 + 샘플 클립)

#### `2026-03-19 23-00-50` (~12분, 닉 `overclock`) — v7

`ace_rounds=[]` (이 구간 올킬 없음, 정상)

### 알려진 이슈 (다음 작업)

1. ~~**ΔK 상한 미적용**~~ → ✅ `==3` ace, `>=4` k_read_error
2. ~~**K 컬럼 OCR 오독**~~ → ✅ K 단독 ROI + Otsu 이진화
3. ~~**안전장치 오귀속**~~ → ✅ 추론(`[T]`) 라운드는 ace 판정 제외
4. ~~**1c 미구현**~~ → ✅ `extract_ace_clips.py` (라운드 인지형 클립)
5. ~~**리포트 시각 M:SS**~~ → ✅ `sec_to_mss`
6. ~~**2자리 K 오독**~~ → ✅ 원인=**빨간 킬바**가 하이라이트(1위/본인) 행 숫자에 겹침 + K 누적 2자리. `_ocr_digit`을 **Otsu + 흰글씨분리(HSV) 결합**으로, ROI 2자리 폭(`0.406~0.432`)으로 수정. 괴명 0/3/7/10 전부 conf 1.00
7. ~~**라운드 건너뛴 거짓 ace**~~ → ✅ **인접성 규칙**: ace는 직전 유효 K가 "바로 앞 라운드"일 때만 (중간 읽기실패 시 ΔK가 여러 라운드 합산 → R55 거짓 제거)
8. **연속 라운드 읽기 실패** — 일부 구간 닉 매칭 실패 (인코딩/조명). 상대(길증경 등) 하이라이트 행 K는 간헐 None (본인 K는 견고)
9. **detect_rounds 인코딩** — stdout UTF-8 reconfigure 미적용 (한글 깨짐, 기능엔 무영향)
10. **결합 OCR 속도** — K셀당 OCR 2회로 긴 영상 느림. 델타 로직만 바꿀 땐 기존 JSON 재계산으로 OCR 생략 가능

### 명령어 (1b 파이프라인)

```powershell
cd C:\clipAI\files

# 1a 라운드 분할 (긴 영상 ~30분+)
python -u detect_rounds.py "E:\OBS\<영상>.mp4" --dataset-root "E:\Highlights\ml_dataset"

# 1b 닉 확정 + K 읽기
python -u scoreboard_k_reader.py "E:\OBS\<영상>.mp4" `
  --rounds-dir "E:\Highlights\ml_dataset\rounds\<stem>" `
  --dataset-root "E:\Highlights\ml_dataset" `
  --json-out "E:\clipai_result\kill_timeline\<stem>_v1.json"
```

디버그: `_debug_roi.py`, `_visualize_roi.py`, `_debug_preprocess.py`, `_debug_k_col.py`

---

- **1차**: 전체스코어 기준 라운드에서 **올킬** 잡기 (라운드 분할 = 완료, 킬 집계 = 진행)
- **2차**: 잡은 올킬 내 더블킬/멀티킬/세이브 콤보 분류 (clips types 모델 보유)
- **3차**: 킬 자체 학습도 올려 더블킬 직접 탐지

## B안: 스카우터 패널 닉네임/킬 추적 (메인, 2026-06-25)

> 닉네임은 수시로 바뀌므로 B안(스카우터 패널 본인 행 추적)을 **메인**으로 한다.

### scouter_nick.py — 검증 완료 (영상 4개)

방송 레이아웃 2종 + 스카우터 모드 2종을 처리:

1. **레이아웃 감지** `detect_game_width()`
   - 후원패널형: 우측 ~27% 검은 영역 → 게임 폭 ≈ 73% (예 2026-03-26: 1405px)
   - 풀스크린형: 게임이 전체 (예 뮤크퀵, 05-18, 06-14)
   - **이전 OCR 실패 원인 = 이 레이아웃 무시하고 전체폭 고정좌표로 crop → 후원패널(검은영역) 긁음**
2. **패널 crop** = 게임 폭 기준 우하단 (x 0.78~0.995, y 0.61~0.80)
3. **헤더 판별**
   - `스카우터2 (L)` → **맨 위 행 = 본인**
   - `스카우터 (L)` → **점(●) 행 = 본인** (닉이 `null`인 경우도 있음 — 널값 아님, 진짜 닉이름)
4. 특수문자 닉(◇¤깜띸겅쥬¤◇) 가능 → OCR 약점

### 검증 결과

| 영상 | 정답 | 레이아웃 | 모드판별 | 닉추출 |
|------|------|----------|----------|--------|
| 뮤크퀵 | 스2 / 단호 | 풀 | ✓ | **단호 conf1.00** ✓ |
| 2026-03-26 | 스2 / ◇¤깜띸겅쥬¤◇ | 후원 | ✓ | 특수문자 OCR 실패 |
| 2026-05-18 | 스1 / 닉 `null` | 풀 | ✓ scouter | 점 행 = 본인 |
| 2026-06-14 | 스1 / 닉 `null` | 풀 | ✓ scouter | 점 행 = 본인 |

- **레이아웃·패널·모드 판별 = 4/4 성공**
- **스카우터2 일반 닉 = 성공** (단호)
- 한계: ① 특수문자 닉 OCR ② 영문 짧은 닉(`null`)은 OCR과 코드 null 혼동 주의

### 핵심 인사이트 (킬 집계 관점) — **2026-06-26 확정**

- **스카우터2 패널**: 맨 위 행 = 본인 (플레이 중) → **신원(닉) 확보용**
- **Clan Match 전체스코어**: **킬 순으로 행 재정렬** (사용자 확인) → 맨 위 ≠ 본인
- 따라서 킬 집계 흐름:
  1. 라운드 중 스카우터2 **맨 위** 또는 스카우터 **점(●) 행**에서 본인 닉 확보
  2. 전체스코어 프레임에서 **닉 매칭으로 본인 행 찾기** (퍼지 매칭)
  3. 해당 행 **K 숫자** 읽기 → 라운드 간 **ΔK**
- 닉 OCR은 "보조"가 아니라 **전체스코어에서 행을 찾는 데 필수** (위치 고정 불가)
- 특수문자 닉 / 짧은 영문 닉(`null`) OCR 주의

### 다음 (상세 설계 → `PLAYER_IDENTITY_AND_K_READER.md`)

1. ~~**`player_identity.py`**~~ — ✅ 구현
2. ~~**`scoreboard_k_reader.py`**~~ — ✅ v1 (ΔK 상한·시각 표기 보완 필요)
3. **`1c` 클립 추출** — ace_rounds → ffmpeg
4. `scouter_nick.py` 헤더 fuzzy 보완 (스무무터 등)
5. 발로란트 등 타게임 영상 사전 필터(2026-04-09)

> OCR 단독 한계 확인됨 (20샘플 ~50% 실패). 단발 OCR이 아니라 **영상 단위 닉 확정** 방향으로 전환.

## 도메인 규칙 (서든어택, 사용자 제공 2026-06-25) — 중요

### 탐지 방식 구분 (ML vs 규칙/CV)

| 단계 | 방식 | 설명 |
|------|------|------|
| 풀 스코어보드 | **ML (CNN)** | `train_scoreboard_clf.py`로 학습, 화면 전체 분류 |
| WIN/DEFEAT | **ML (CNN)** 또는 템플릿 | `train_win_clf.py` / OpenCV matchTemplate |
| HUD 인원 아이콘 | **규칙/CV (ML 아님)** | `hud_round_end.py` — HSV 색상 + blob 개수 |
| 킬 수 OCR | **OCR** | `scoreboard_k_reader.py` + `scoreboard_layout.py` |
| 하이라이트 types | **ML (CNN)** | clips/ 4종 분류 (`train_highlight_types.py`) |

- HUD·지속시간(3.5초) 규칙은 **학습 없이** 도메인 지식 + OpenCV
- 스코어보드/WIN/types만 **라벨 데이터로 학습**

### 라운드 정의
- **라운드 = 풀 스코어보드("전체스코어") 사이 구간**
- 상단 중앙 **라이브 HUD** (항상 표시, 라운드 경계 아님)
  - 형식: `레드팀점수 | 라운드번호 | 블루팀점수` (예 `001|005|002`)
  - 각 점수 아래에 **팀 인원 아이콘**(빨강/파랑 실루엣) 표시
  - **한쪽 팀 아이콘이 전부 사라지면 = 해당 라운드 종료**(엘리미네이션)
    - WIN/DEFEAT 문구가 없어도 이 상태면 라운드가 끝난 것
    - 이후 킬로그 → 풀 스코어보드(~4초) 순서로 이어짐
  - **전반전/후반전에 점수·라운드번호 초기화** → 킬 증가량 계산 시 전/후반 경계에서 리셋 고려
- 풀 스코어보드는 라운드 종료 시퀀스에서 노출: **(HUD 한쪽 전멸) → 킬로그 → 풀 스코어보드(약 4초)**
  - WIN/DEFEAT는 자주 보이지만 **타임라인에 WIN 구간을 따로 적을 필요 없음**
- 사용자가 키 눌러 잠깐 보는 스코어 ≠ 진짜 경계 → **지속시간으로 구별**
  - `detect_rounds.py --min-scoreboard-sec 3.5` (실제 ~4초, 안전 마진으로 살짝 아래)

### 킬 집계 / 하이라이트 1차 규칙
- **ΔK 최대 3** — 한 라운드에서 본인 킬 증가는 0~3만 정상. **4 이상 = 읽기 오류**
- **올킬 = ΔK == 3** (3킬 = 상대 전원 격파)
- 라운드 내 **단일킬 3개 → 올킬**로 1차 처리 → 하이라이트 후보 합격 (2차 분류는 나중)
- `전체스코어 → 움직임/사망/팀원 → 전체스코어` (킬 없음) → 자연 폐기
- 더 정확한 방법(사용자 제안): **본인 닉네임 추적 + 라운드 간 킬 증가량**
  - **스카우터** 패널: 점(●) 행 = 본인 (닉 문자열이 `null`일 수 있음)
  - **스카우터2** 패널: 맨 위 행 = 본인
  - **전체스코어(Clan Match)** 는 **킬 순 정렬** → 본인 행은 닉 매칭으로 찾기

### 죽음
1. 팀원 생존 → 팀원 플레이(관전) 노출
2. 본인이 마지막 → 곧바로 전체스코어

### 리조인 (검정화면 오래 지속)
- 리조인/게임 나감 시 **본인 킬만 초기화** (예 2/3/4 → 0/0/0)
- 전체스코어 숫자가 갑자기 낮아지면 리조인/검정화면 여부 검토 후 수계산·킬로그로 보정
- 리조인이어도 멀티킬/올킬/세이브 가능 → 오류로 취급 금지

### 세이브
- "[닉네임]님이 세이브찬스를 획득하였습니다" (흰 글씨) → 성공 시 "세이브에 성공하였습니다"
- 세이브 성공 시 **멀티킬/더블킬 로고 동반** (노이즈 가능)
- 세이브 = 기본 올킬급

### 참고 이미지
- `assets/...image-e22d9108...png`: 풀 스코어보드 + 상단 라이브 HUD(빨간 박스 구간)
- `assets/...image-a44f92ba...png`: 라이브 HUD 클로즈업 — 좌/우 팀 점수 + 하단 인원 아이콘
- `assets/...images_3...png`: WIN + Killed 배너 + 스카우터2(본인=맨 위)
- `assets/...images_2...png`: MULTI KILL 로고 + 세이브 성공 + 킬로그(전원) + K/D/A

## 새 방향: 라운드 기반 파이프라인 (2026-06-25)

기존 sliding-window binary는 precision이 낮음(일반 플레이/단일킬 과검출).
→ **라운드 단위로 자르고, 킬 많은 라운드만 2단계에 넘기는** 구조로 전환.

```
[1a] 라운드 분할 — "전체스코어 화면" 감지 (라운드 경계)
     보조 신호(미구현): 상단 HUD 한쪽 팀 인원 아이콘 전멸 → 라운드 종료 직전
[1b] 라운드별 내 킬 수 — OCR 또는 킬 이벤트 카운트
[1c] KEEP — 킬 >= 2 (더블킬 이상) 라운드만
[2]  KEEP 구간에만 types 4종 분류 적용
```

### 진행 (1a 스코어 화면 감지 데이터)

- 라운드 타임라인 raw: `manifests/round_timelines/2026-03-19 23-00-50.txt`
- 파서: `parse_round_timeline.py` → `manifests/round_segments.csv`
  - 42세그먼트, scoreboard(라운드 경계) **13개**, movement14/death6/teammate5/loading2/lobby2
- 프레임 추출: `extract_scoreboard_frames.py --clean`
  - `scoreboard_frames/scoreboard/` **102장** (positive)
  - `scoreboard_frames/other/` **175장** (negative)
  - 샘플 육안 검증: positive=Clan Match 스코어 오버레이, negative=일반 플레이 → 명확히 구분됨

### 핵심 발견 (2026-06-25)

- 이전 90개 "킬 시각" 중 `23-13-48`의 9개가 **전부 단일킬**로 확인됨
  (0:27, 2:26, 2:33, 3:01, 3:25, 4:24, 7:05, 9:11, 13:14 = 사용자 단일킬 목록과 일치)
- → 1단계 binary가 단일킬도 하이라이트로 학습한 직접 원인. `known_highlights.csv` 재정의 필요.

### 스코어 분류기 (train_scoreboard_clf.py) — 영상 2개 학습 완료

- 라운드 타임라인 2개: `23-00-50.txt`, `23-13-48.txt`
- 파서 키워드 매칭으로 개선 ("대기실 재입장", "게임종료로딩…", "움직" 오타, "단일킬" 처리)
- `round_segments.csv`: 105세그먼트, scoreboard 26(영상당 13), single_kill 9
- 프레임: scoreboard 206 / other 405 (single_kill도 negative)
- **학습 결과: test acc 95.7%, P 91.7%, R 91.7%, F1 91.7%** (1개 영상 P63.6% → 대폭 개선)
- 모델: `models/scoreboard_clf_best.pt`

### 라운드 자동 분할 (detect_rounds.py) — 작동 확인

- scoreboard 분류기로 영상 스캔 → 연속 감지 병합 → **4초 지속 규칙**으로 가짜 스코어 폐기 → 라운드 분할
- `23-00-50` 검증: known 스코어보드 **13/13 전부 검출**, FP 1개(사망 구간), 짧은 가짜 9개 자동 제거 → **15라운드 분할**
- `00-40-56` (~81분): 스코어보드 **81**, 라운드 **82** (~31분, 순차 디코드)
- 출력: `rounds/<stem>/detected_scoreboards.csv`, `rounds.csv`
- 실행: `python -u detect_rounds.py "E:\OBS\<영상>.mp4" --scan-fps 2`
- ~~cv2 seek 병목~~ → **2026-06-26 순차 디코드로 해결** (긴 영상 필수)

### 다음

1. ~~**라운드별 킬 수 집계**~~ → v1 완료, **ΔK==3 엄격화**·오류 플래그 필요
2. KEEP 구간에만 2단계 types 적용
3. `1c` ace_rounds → 클립 ffmpeg 추출
4. ~~detect_rounds seek 병목~~ → 완료
5. `known_highlights.csv`에서 단일킬 제외 → 1단계 재정의(또는 라운드 방식으로 대체)

## 사용자 결정 (중요)

1. **2단계 types**: `clips/` 4종 클립 학습 **진행 중** (저녁 백그라운드)
2. **1단계 binary**: window 모델 완료 — precision 낮음, 라벨링 후 재학습 예정
3. **전체 배치는 피드백 후**: `MAX_VIDEOS=0`으로 84개 실행

## 파이프라인 현황

| 단계 | 스크립트 | 상태 |
|------|----------|------|
| 0 ROI | `train_game_roi.py` | **완료** |
| 1a 라운드 | `detect_rounds.py` | **완료** (순차 스캔) |
| 1b 닉+K | `player_identity.py`, `scoreboard_k_reader.py` | **v1 완료** (ΔK 엄격화·클립 추출 남음) |
| 1c KEEP+클립 | — | **미구현** |
| 1 binary (window) | `train_binary.py --mode window` | **완료** (precision 낮음, 레거시) |
| 2 types | `train_highlight_types.py` | **완료** (clips 4종, val macro_recall 74%) |
| OBS 파일럿 추론 | `batch_infer_obs.py` | **완료** (90/90 recall, FP 많음) |

### 2단계 types 학습 (clips 4종, 완료)

- 데이터: 하이라이트 188개 (doublekill 38, multikill 32, save 72, allkill 46)
- best: epoch 12, val macro_recall **74%**, val_acc 76.2%
- val 타입별: doublekill 100%, multikill **25%**, save 87.5%, allkill 83.3%
- test: acc 56.2% — multikill·save 약함, 데이터 부족 영향
- 로그: `train_types_clips.log`, 모델: `highlight_types_best.pt`
- **multikill 샘플 32개로 부족** — 클립 추가 또는 라벨링 후 재학습 권장


- 데이터: `label_segments.csv` 204개 (highlight 90 + background 114)
  - hard negative 90 (`obs_hard_negative`) + background_videos 24
- 클립 추출: `extract_labeled_clips.py --overwrite` → 204개 12초 mp4
- 학습 로그: `E:\Highlights\ml_dataset\models\train_binary_window_run.log`
- best 모델: epoch 1 (highlight_recall 100%)

### 파일럿 추론 + recall (2026-06-24)

| 영상 | known | predictions(merged) | recall |
|------|-------|---------------------|--------|
| 2026-03-19 23-00-50 | 10 | 14 | 100% |
| 2026-03-19 23-13-48 | 9 | 14 | 100% |
| 2026-03-21 00-40-56 | 71 | 67 | 100% |
| **합계** | **90** | **95** | **100%** |

- 이전(클립 모델): `windows_hit=0` → 현재: 전 킬 검출
- **주의**: recall 100%이지만 **false positive 많음** (짧은 영상도 14구간 검출). threshold 상향 또는 negative 추가 학습 필요.

평가 스크립트: `python eval_pilot_recall.py`

## 경로 요약

| 용도 | 경로 |
|------|------|
| 코드 | `C:\clipAI\files\` |
| 킬 라벨 | `E:\Highlights\ml_dataset\manifests\known_highlights.csv` |
| 세그먼트 | `E:\Highlights\ml_dataset\manifests\label_segments.csv` |
| window 클립 | `E:\Highlights\ml_dataset\clips\{label}\` |
| 모델 | `E:\Highlights\ml_dataset\models\` |
| OBS (84 mp4) | `E:\OBS\` |
| 추론 결과 | `E:\clipai_result\` |
| 킬 타임라인 | `E:\clipai_result\kill_timeline\` |

## 다음 TODO

1. **ΔK 엄격화**: ace = `ΔK==3` only, `ΔK>=4` → `k_read_error` (올킬 아님)
2. **리포트 시각** `M:SS` 형식 (`format_report`)
3. **1c 클립 추출** — 검증된 ace_rounds만 ffmpeg
4. 안전장치 오귀속 줄이기 (직접매칭 OCR 개선)
5. precision 개선 (레거시 binary) 또는 라운드 방식으로 대체

## 명령어

```powershell
cd C:\clipAI\files

# 세그먼트 재생성
python build_label_manifest.py --allow-overwrite --obs-negative-per-video 30

# window 클립 추출 + 학습
python extract_labeled_clips.py --overwrite
python -u train_binary.py --mode window --epochs 10 --batch-size 24 --num-workers 0

# 파일럿 추론 + recall
python -u batch_infer_obs.py
python eval_pilot_recall.py
```

## 워크플로

```
[0] ROI                    → 완료
[1a] detect_rounds        → 완료 (순차 스캔)
[1b] identity + K reader  → v1 완료 (ΔK 엄격화 필요)
[1c] ace → 클립           → 다음
[2] types 분류            → 모델 보유, KEEP 연동 대기
[레거시] window binary    → recall 100% / FP 많음
```
