# SONNET 작업 명세 — HUD 올킬 A안 강화: "증거창 확정 + 오독 FP 가드"

> 작성: 2026-07-06 Fable. 배경·용어는 `HUD_ACE_HANDOFF.md` §0 필독.
> 원 목표: recall ≥ 85% AND precision ≥ 80% — 정답 타임라인 밖 올킬은 전부 오답.
> **▶▶▶ R4 Task A 완료·되돌림 (2026-07-07 Sonnet)**: 8 숫자 CNN 학습·통합했으나
> 실측 순손실(recall 88.5%→80.8%, TP 23→21)로 프로덕션 경로(`hud_kda._read_digit_group`)는
> 되돌림 — 인프라(`train_hud_digit_cnn.py`, `hud_digit_match.match_glyph_cnn`)는 보존.
> **다음 세션 = `HUD_ACE_HANDOFF.md` §0 맨 아래 "R4 Task A 결과" 절부터** (원인 분석 +
> 재시도 조건 정리돼 있음). R3 수치가 현재 유효: recall 88.5%(23/26) / precision
> 65.7%(23/35). 베이스라인 `r3_final` 유효. Task B(타이머)는 ROI만 확정, 미완(아래
> "Task B 진행 메모" 절). 아래 "R2"/원 설계 명세는 과거 기록(참고용).
>
> **★★ R2 완료 (2026-07-07 Sonnet)**: `SONNET_TASK.md` R2 Task 0~5 실행 완료.
> GT **26건** 기준 **recall 88.5% (23/26) ✓**, **precision 74.2% (23/31) ✗**.
> 상세 per-video·미탐 3건·코드 변경은 **`HUD_ACE_HANDOFF.md` §0 "2026-07-07 Sonnet R2"** 절.
> 이전 8 템플릿 제거(74.1%)는 그 아래 절 참고.
> **새 도구**: `files/_tp_diff.py` — 실험 전후 GT별 획득/상실을 diff로 보여줌
> (두더지잡기 방지). 베이스라인 3종은 `files/_tp_baselines/`에 고정 저장됨.
> 모든 실험은 `hud_from_cache.py && _compare_hud_gt.py && _tp_diff.py` 3종 세트로 측정할 것.
>
> (2026-07-06 Sonnet, 참고): Phase 0+1 자체는 recall 그대로/precision 56.0%→66.7%.
> 시도했으나 되돌린 3가지 실험·구조적 시사점(전역 순차 상태 리플렉트 문제)도 위 절에 기록.
> 아래는 원 설계 명세(참고용, 이미 구현된 §3 Phase 1 부분은 완료 표시).

---

## 0. 절대 규칙 (위반 금지)

1. ace = 라운드 내 킬 합 **정확히 3** (`kills == 3`). ≥3 금지.
2. **사용자 정답 27건이 유일한 기준** — 정답 밖 탐지는 "실제 올킬일지도"라고 재해석하지 말 것. 전부 FP다.
3. 시각 보고는 M:SS.
4. 라운드 경계는 row_miss 방식 유지 (detect_rounds CNN으로 전면 교체 금지 — 단 §4의 "스팟 검증 게이트"는 허용: HUD 파이프라인의 단일패스 메리트를 유지하는 보조 장치임).
5. `hud_kda.py`의 행 탐지/IoU 매칭 자체는 건드리지 말 것 (검증 완료 영역).

## 1. 문제의 뿌리 — "판독 지연 점프" (17건 중 7건)

라운드 종료 무렵 HUD가 깜빡여 중간 K값(7·8)이 "연속 2회" 확정을 못 받으면,
확정값이 뒤처지다가 **6→9(+3)로 뭉쳐** 가짜 올킬(+진짜 올킬 왜곡)이 됨.

**중요한 사전 발견**: 기존 측정(77.8%)의 캐시는 **숫자 8 템플릿이 없던 시점**에 생성됨.
현재는 `k_8/_b/_c` 설치 완료 → 42:00 사례에서 8이 conf 0.96~0.98로 잘 읽힘을 확인.
**따라서 재캐시만으로도 6→9 계열 상당수가 해소될 가능성이 높다. 반드시 Phase 0부터.**

## 2. Phase 0 — 재캐시 + 기준 측정 (필수 선행, 다른 작업 전에)

인프라는 준비 완료 (Fable이 리팩토링함):

- `detect_ace_hud.py`: `collect_reads()`(영상→원시판독) + `timeline_from_reads()`(판정) 분리.
  **판정 로직의 단일 진입점** — 실스캔과 캐시 재계산이 같은 함수를 씀.
- `hud_sig_cache.py`: 원시 판독(t, K, conf, method) 캐시 빌더. 영상당 1패스.
- `hud_from_cache.py`: 캐시→판정 재계산(3초). `--dump`로 구간 원시판독 열람.

```powershell
cd C:\clipAI\files
# 1) 재캐시 (~70분, 백그라운드 권장. 8 템플릿 포함 판독으로 새로 생성됨)
python -u hud_sig_cache.py
# 2) 기준 측정
python -u hud_from_cache.py
python -u _compare_hud_gt.py
```

결과(재캐시 후 recall/precision)를 기록하고 §5 대조표를 갱신할 것.
이 수치가 이미 목표를 넘으면 Phase 1은 마진만 소폭 실험하고 종료해도 됨.

## 3. Phase 1 — `_KTracker` v2: 증거창(evidence window) 확정

`detect_ace_hud.py`의 `_KTracker`를 아래로 교체. **"연속 2회"의 취약점(깜빡임에 끊김)을
"시간창 내 증거 2회"로 바꾸되, 오독 FP를 막는 가드 4개를 함께 구현.**

### 3-1. 자료구조

```python
_EV_WINDOW = 4.0        # 증거 수집 창(초)
_EV_CONFIRM_HI = 2      # conf>=0.75 판독 포함 시 확정 증거 수
_EV_CONFIRM_LO = 3      # 전부 저신뢰(conf<0.75)면 3회 요구  ← 가드 G2
_EV_REBASE = 5          # 하향/+4↑ 리베이스 증거 수 (기존 유지)
_ROLLBACK_WINDOW = 4.0  # 확정 직후 되돌림 감시 창          ← 가드 G4
_EV_ROLLBACK = 3

class _KTracker:
    confirmed: int | None
    ev: dict[int, list[tuple[float, float]]]   # k값 → [(t, conf), ...] (창 내)
    kills / resets: 기존과 동일
    last_kill: (t_confirmed, from_k, to_k) | None   # 되돌림 감시용
```

### 3-2. update(t, k, conf) 의사코드

```
k None → return
ev 전체에서 t - _EV_WINDOW 보다 오래된 항목 제거
ev[k].append((t, conf))

confirmed None:
    strong(ev[k]) 이면 confirmed=k, ev에서 k 이하 제거. return

k == confirmed:
    가드 G4(되돌림): last_kill 있고, t - last_kill.t <= _ROLLBACK_WINDOW 이고,
      k == last_kill.from_k 이고 len(ev[k]) >= _EV_ROLLBACK 이고
      last_kill 확정 후 to_k 증거가 더 안 쌓였으면:
        kills에서 last_kill 제거(pop), confirmed = from_k, last_kill = None
        # 순간 오독 2프레임이 킬로 확정된 경우 원복하는 안전핀
    return

# 상향 체인: confirmed+1 부터 오름차순으로 확정 시도 (핵심!)
loop:
    nxt = min { v in ev : confirmed < v <= confirmed+3 and strong(ev[v]) }
    없으면 break
    kills.append(KillEvent(first_t(ev[nxt]), confirmed, nxt))
    last_kill = (t, confirmed, nxt); confirmed = nxt
    ev에서 confirmed 이하 항목 제거

# 리베이스 (하향 or confirmed+3 초과) — 가드 G1
v_reb = value with len(ev[v]) >= _EV_REBASE and (v < confirmed or v > confirmed + 3)
있으면 resets.append(...), confirmed = v_reb, ev.clear(), last_kill=None

def strong(entries):  # 가드 G2: 신뢰도 연동 증거 기준
    return (len(entries) >= _EV_CONFIRM_HI and max(conf) >= 0.75) \
        or len(entries) >= _EV_CONFIRM_LO
```

### 3-3. 왜 이 설계가 지연점프를 없애는가 (실사례: 02-03-10 41:52~42:02)

```
판독:  8(0.98) · · · 8(0.96) · · 9(0.93) 9(0.94)     (· = 판독실패)
현행:  8이 "연속 2회"를 못 채움(사이가 끊겨도 pend는 유지되지만 이 케이스에선
       중간 confirmed값 재등장·다른 후보가 pend를 리셋) → 6→9 +3 뭉침 = 가짜 올킬
v2:    ev[8] = 2회(고신뢰) → 창 내 증거로 8 확정 → 6→7? (없음, 7은 이전에 확정됐거나
       증거 없으면 8 확정 시 6→8 +2로 이전 시점 first_t(8)에 기록)
       이후 ev[9] 2회 → 8→9 +1.  라운드 경계가 8확정과 9확정 사이면
       킬이 올바른 라운드에 각각 귀속 → 가짜 +3 소멸
```

핵심 차이: **오름차순 체인**이라 9에 증거가 먼저 쌓여도 7·8 증거가 있으면
7→8→9 순서로 확정되고, 각 킬은 **그 값이 처음 관측된 시각**에 기록됨.

### 3-4. 가드 요약 (A안의 오독 FP 방어)

| 가드 | 내용 | 막는 것 |
|---|---|---|
| G1 | confirmed+3 초과·하향은 _EV_REBASE(5) 증거 필요 | 큰 점프 오독(3→9 등) |
| G2 | 저신뢰(conf<0.75)뿐이면 증거 3회 요구 | 흐릿한 프레임 연쇄 오독 |
| G3 | (기존 유지) 트리플 가드 — K/D/A 셋 다 파싱된 프레임만 | 배너/페이드 행 오염 |
| G4 | 확정 직후 이전 값이 되쌓이면 킬 롤백 | 순간 오독 2프레임 확정 |

## 4. Phase 2 — 가짜 경계 스팟 검증 (Phase 1 후에도 recall < 85%면)

> **2026-07-06 Sonnet 판단: 착수 보류**. Phase 1 완료 후 남은 미탐 13건을 개별 진단한
> 결과, 명확히 "가짜 경계가 쪼갬"으로 특정된 건 51:40-52:10(02-34-09) 1건뿐이고, 그마저
> **경계는 진짜**(5.25초짜리 실제 스코어보드)였다 — 문제는 경계가 아니라 그 경계를
> 넘나드는 킬체인 중간값이 `_EV_WINDOW`(4초)보다 드물게 관측되는 것. 즉 남은 recall
> 손실의 지배적 원인은 **경계 오검출이 아니라 트래커의 증거창 크기**로 확인됐다.
> 이 CNN 경계검증은 "가짜 경계 자체를 없애는" 접근이라 이번에 발견된 지배적 원인에는
> 안 맞음 — `HUD_ACE_HANDOFF.md` "다음 세션 우선순위" 절(라운드별 독립 트래커 재설계
> 또는 confirmed+1 한정 무제한 누적)이 더 높은 우선순위. 착수 전 그 절을 먼저 읽을 것.

미탐 2건(03-02-03 14:15, 02-34-09 2:49)은 킬 합이 정확히 3인데 **가짜 row_miss 경계**가
중간을 쪼갬. row_miss 경계 후보(런 ≥ `_BOUNDARY_ROWMISS`)마다 **런 중앙 프레임 1~3장만**
학습된 스코어보드 CNN(`E:\Highlights\ml_dataset\models\scoreboard_clf_best.pt`,
`detect_rounds.py` 참고 — test acc 95.7%, 13/13 검출 검증)으로 분류:

- 스코어보드 확률 ≥ 0.6 프레임이 있으면 → 진짜 경계 유지
- 아니면 → 가짜 경계로 폐기 (단, 런이 40프레임(10s)↑면 무조건 유지 — 확실한 비플레이 구간)

구현: 별도 스크립트 `hud_boundary_verify.py` — 캐시에서 경계 후보 시각을 뽑아
영상을 **seek로 그 프레임만** 읽어 분류 → `sig_cache_v2/{stem}.boundary.json`에 verdict 저장
→ `timeline_from_reads`가 있으면 반영. 영상당 후보 수십 개 × 3프레임 = 수초.
(전면 CNN 스캔이 아니므로 HUD 파이프라인의 단일패스 메리트 유지)

## 5. 측정·수용 기준

```powershell
python -u hud_from_cache.py && python -u _compare_hud_gt.py   # 3초 루프
```

- 수용: **recall ≥ 85% AND precision ≥ 80%** (정답 27건 기준)
- 각 Phase 후 per-video 표를 `HUD_ACE_HANDOFF.md` §0에 갱신
- 완료 시: 실스캔 일치 확인 1회(`detect_ace_hud.py`를 짧은 영상 1개에 실행, 캐시 결과와 ace 동일한지)
  → 커밋 → 배치 37~113 재개는 사용자 승인 후

### 진단 팁

- 특정 구간 원시 판독: `python -u hud_from_cache.py --dump "2026-03-22 02-03-10" 41:50 42:10`
- 미탐/오탐별 킬 이벤트는 hud_timeline JSON의 `kill_events`/`reset_events`에 M:SS로 다 있음
- 판독 자체가 이상하면(캐시에 없는 값) → 템플릿 문제 → `harvest_hud_digits.py` (단, 재캐시 필요)

## 6. 금지·주의

- `_REQ_CONFIRM` 을 3으로 올리는 방식 금지 (막판 킬 유실 실측됨)
- 경계를 detect_rounds 전면 스캔으로 교체 금지 (§0-4)
- `_EV_WINDOW` 를 6초 이상으로 넓히지 말 것 — 이전 라운드 잔존 증거가 다음 라운드로 새는 창이 됨
- 캐시(sig_cache_v2)는 **판독 로직/템플릿 변경 시에만** `--force` 재생성. 판정 로직 변경은 재생성 불필요
- 구 `sig_cache`(v1, 킬 이벤트 저장 방식)는 폐기 대상 — 쓰지 말 것

---

# R2 (2026-07-07): 미탐 7건 소탕 명세 — 사용자 육안 피드백 반영 (Fable 설계)

> 배경: 현재 recall 74.1%(20/27), precision 76.9%(20/26). 사용자가 미탐 7건을
> 스크린샷으로 직접 확인해 준 결과를 반영한 정밀 타격 명세.
> **순서대로 진행, 태스크마다 3종 세트(`hud_from_cache` → `_compare_hud_gt` → `_tp_diff`) 측정.**

## 사용자 확인 도메인 사실 (2026-07-07, 절대 신뢰)

- **마지막 킬 후 최소 2초간 K/D/A가 그대로 노출**된 뒤에야 라운드 전광판이 뜬다.
- **전광판이 떠 있는 동안 K/D/A는 미노출** (= row_miss 경계 논리의 도메인 근거 확정).
- WIN/DEFEAT 배너 → **전광판 약 3초 유지** → 다음 라운드 이동.
- 상단 점수(000|005|000→001|005|000)는 라운드 승리 시 즉시 갱신되지만 이걸로
  감지하지 말 것(상단 스코어 OCR은 예전에 실패 판정, 사용자도 "이걸로 감지는 안될 것").
- 02-21-23 79:51 케이스: **3번째 킬(7→8)은 실제 있었고 K/D/A=8이 ~2초 표시됨**
  (스크린샷 확인). 우리가 8을 EXCLUDE_DIGITS로 뺐기 때문에 못 읽은 것 → Task 3.

## Task 0 — GT 수정 (오답 1건 제거) + 베이스라인 재저장

사용자 확인: **02-34-09 38:14–38:35는 3번째 킬이 실제로 없었음(오답 제공, 제거 지시)**.
- `_compare_hud_gt.py`의 GT dict에서 `"2026-03-24 02-34-09"` 항목 중
  `(_s("38:14"), _s("38:35"))` 튜플 삭제 → **GT 총 26건**.
- 즉시 재측정 후 `python -u _tp_diff.py --save-baseline c2_gt26` 저장.
  이후 모든 diff는 `--compare-to c2_gt26`.
- 예상: recall 20/26 = 76.9% (분모만 감소), precision 변화 없음.
- 목표 재정의: **recall ≥ 85% = 23/26 이상**, precision ≥ 80%.

## Task 1 — 경계 검증 게이트 (`hud_boundary_verify.py` 신규) ★사용자 요청 확정

사용자 질문 "win/전광판 함수 반영돼 있나?" → **현재 미반영** (grep 확인: HUD 파이프라인
4개 파일 어디에도 scoreboard_clf/win_clf 참조 없음. 경계는 오직 row_miss run 길이).
모델은 둘 다 존재: `E:\Highlights\ml_dataset\models\scoreboard_clf_best.pt`(test acc
95.7%, 13/13 검증)와 `win_clf_best.pt`. 이번에 연결한다.

구현 (§4 원안 + 사용자 확인 도메인 시퀀스 반영):
1. 각 캐시 영상의 row_miss run(≥`_BOUNDARY_ROWMISS`) 후보마다, 영상 seek로
   run의 25%/50%/75% 지점 프레임 3장 추출.
2. `scoreboard_clf_best.pt`로 분류 (로드·전처리는 `detect_rounds.py`의
   `build_model`/transforms 코드 재사용 — 새로 만들지 말 것).
3. 3장 중 하나라도 scoreboard prob ≥ 0.6 → **진짜 경계 유지**. 아니면 **폐기**.
   (도메인상 라운드 경계 = 전광판. 전광판 없는 row_miss run은 배너 가림/사망 연출 등
   가짜 — 실측: 03-02-03 14:17·14:37 경계가 GT 올킬 한가운데를 쪼개고 있음.)
4. verdict를 `E:\clipai_result\sig_cache_v2\{stem}.boundary.json`에 저장
   (`{"run_center_sec": true/false}` 맵) — 이후 재평가는 다시 3초 루프 유지.
5. `hud_from_cache.py`/`timeline_from_reads`에 연결: boundary.json 있으면
   폐기 판정 run은 경계로 쓰지 않음. (실스캔 경로는 나중에 — 우선 캐시 경로만.)

기대 효과: 03-02-03 14:15–14:57 (+1), 02-34-09 2:49–3:10 (+1).
⚠ 가짜 경계 제거로 라운드가 길어지며 이웃 킬이 합산돼 기존 TP가 kills>3으로 깨질 수
있음 — `_tp_diff`에서 "상실" 발생 시 해당 케이스 개별 덤프 후 보고.

## Task 2 — 킬 귀속 그레이스: "경계 직후 첫 판독이 곧 증가값이면 이전 라운드 킬"

도메인 근거(사용자 확인): 마지막 킬 후 ≥2초 K/D/A 유지 → 전광판(미노출) → 다음
라운드는 스폰이라 시작 직후 킬 불가. 따라서 **경계 직후 K가 이전 확정값의 재관측 없이
곧바로 +1/+2 올라가 있으면, 그 킬은 전광판 직전(이전 라운드 막판)에 일어난 것**.

구현 (`_assign_events`):
- kill event `e`에 대해 직전 경계 `B`를 찾고, `(B, e.t)` 구간에 `from_k`의 성공 판독이
  **하나도 없으며** `e.t - B ≤ 10s`이면 → `e`를 B 이전 라운드로 귀속.
- 캐시의 원시 판독 접근이 필요하므로 `timeline_from_reads`에서 k_read 시각·값 리스트를
  `_assign_events`에 넘길 것 (이미 `k_read_times`를 넘기는 구조 있음 — 값 포함으로 확장).

기대 효과: 23-51-52 16:23–16:43 (+1) — 9→10이 경계(16:46) 직후 16:49에 첫 관측,
사이에 9 판독 없음 → 이전 라운드 귀속 → kills 3.
⚠ 부작용 감시: 다음 라운드 초반 진짜 킬을 뺏어올 위험은 "(B, e.t)에 from_k 판독 없음"
조건이 방어하지만, `_tp_diff` 상실 0 확인 필수.

## Task 3 — 팬텀-8 보정: 7→9(+2) 이벤트 분리 배치

8은 EXCLUDE_DIGITS(판독 제외)라서 화면에 "8"이 떠도 template_miss(행 발견, 숫자
미매칭)로 기록됨. 02-21-23 79:51의 3번째 킬(7→8)이 정확히 이 케이스(스크린샷 확인).

규칙 (from_k==7, to_k==9 인 kill event 한정 — 8만 판독 불가 숫자이므로 남용 금지):
- 마지막 "7" 성공 판독 시각 t7 ~ "9" 첫 관측 시각 t9 사이에 (a) K슬롯 template_miss
  프레임이 존재하고 (b) 경계 B가 끼어 있으면 →
  `7→8` 킬을 **B 이전 마지막 template_miss 구간 시작 시각**에, `8→9` 킬을 t9에 분리 배치.
- 경계가 안 끼어 있으면(같은 라운드 내 +2) 현행 유지 — 어차피 합산 결과 동일.

기대 효과: 02-21-23 79:51–80:10 (+1).

## Task 4 — G1 오염 (02-21-23 54:20 완전 미검출) — 실험 2건, 각각 3초 측정

이미 진단 완료된 케이스: 52:26 `7→6`, 52:32 `6→0` **오독 리베이스 연쇄**가 confirmed를
0으로 오염 → 이후 진짜 킬 체인(→9→10→11)이 증발. 과거 "G1 v==0 제한" 실험은 전역
리플렉트로 순손실이었으므로(핸드오프 참고) **그대로 재시도 금지**. 대신:
- 실험 4a: **하향(v≠0) 리베이스만** 증거 요건 `_EV_REBASE` 5→8 상향 (0-리셋은 5 유지).
- 실험 4b: **리베이스 롤백**(G4의 리셋판) — 하향 리베이스 직후 `_ROLLBACK_WINDOW` 내에
  구 confirmed 값 증거가 다시 강해지면(strong) 리셋 취소·원복.
- 각각 독립 실험 → `_tp_diff`로 상실 0 확인. 둘 다 순손실이면 포기하고 결과만 기록
  (이 케이스는 HMM 전역 디코딩 카드로 이월).

## Task 5 — 00-40-56 41:54–42:39 완전 미검출 — 진단부터

`python -u hud_from_cache.py --dump "2026-03-21 00-40-56" 41:40 42:50` 으로 원시 판독
확인 → 원인 분류(행 미발견? 판독 실패? 리셋 오염?) 후 위 태스크들의 규칙으로 커버되는지
판단. 커버 안 되면 원인만 핸드오프에 기록하고 멈출 것(새 메커니즘 발명 금지).

## 측정 규율·기대치

- 태스크마다: `python -u hud_from_cache.py && python -u _compare_hud_gt.py &&
  python -u _tp_diff.py --compare-to c2_gt26`
- 기대 누적(이론치): 20/26 → T1 +2 → T2 +1 → T3 +1 → T4 +1 = **25/26 (96%)**.
  절반만 먹혀도 23/26 = 88.5% ≥ 목표 85%. precision은 T1이 가짜 경계발 FP도 일부
  제거할 것으로 기대(현 FP 6건 중 단발성 +3 점프형 확인 필요).
- 완료 시: 실스캔 1회 일치 확인(짧은 영상) → 핸드오프 갱신 → 커밋. 배치 37~113
  재개는 사용자 승인 후.

---

# R3 (2026-07-07): 라운드별 독립 정산 디코더로 판정 전면 교체 (Fable 설계·핵심 구현 완료)

> 배경: R2 완료 시점 recall 88.5% (23/26), precision 74.2% (23/31).
> 사용자가 미탐 3건을 영상으로 재확인해 준 결과 + 캐시 덤프 대조로 **세 건 모두
> "판독(캐시)에는 정답이 이미 있는데 전역 상태머신이 버리는" 사례**로 확정.
> 핵심 디코더는 Fable이 구현·자가검증 완료(`files/hud_round_settle.py`, selftest 6/6).
> **이 R3의 작업 = 그 모듈을 `detect_ace_hud.py`에 통합하고 3종 세트로 측정·튜닝.**

## 사용자 육안 확인 (2026-07-07, 절대 신뢰) + 캐시 대조 결과

| GT | 사용자 확인 | 캐시 원시 판독 (덤프 확인 완료) | 미탐 진짜 원인 |
|---|---|---|---|
| 00-40-56 41:54–42:39 | 42:35–42:37에 K/D/A **9/4/0** 노출 | `9` 6회 판독(conf ≤0.97) 멀쩡히 존재. 그런데 42:24.75·42:32.75~33.75에 **스퓨리어스 `0` 5회(conf 0.91~0.95!)** | v==0 무기한 누적 예외가 산발 0 오독 5회를 모아 `_EV_REBASE=5` 충족 → **가짜 리셋 6→0, 0→9 연쇄** → 킬 전멸 |
| 02-21-23 54:20–54:41 | 54:37–54:40에 K/D/A **11/8/0** 노출 | `9`×3, `10`×3, `11`×1(conf 0.90) 존재. 54:28.75에 또 스퓨리어스 `0`(0.92) | 52:32 `7→0` 오독 리베이스로 confirmed 오염(전역 이월) → 9/10/11이 rebase 문턱(5회)을 못 넘어 증발 |
| 03-02-03 14:15–14:57 | 14:53–14:56에 K/D/A **3/0/0** 노출 | 직전 세그먼트 `0` 안정 판독 → 긴 row_miss(14:12.5~14:41) → `1`×다수, `2`×3, `3`×6 | 0→1 킬이 경계에 걸쳐 라운드 귀속 실패(킬 1+2 분산). **k_base=0 이월 + 라운드 내 1→2→3이면 ΔK=3 정확** |

공통 패턴 2가지 (이번에 처음 정량 확인):
1. **스퓨리어스 고신뢰 `0` 오독**이 여러 영상에서 반복 실측(conf 0.9+ — 킬 연출 중 슬래시
   분리가 밀려 A슬롯 0을 K로 읽는 것으로 추정). v==0 무기한 누적 예외와 결합하면 치명적.
2. 라운드 끝값은 사용자 도메인 확정("마지막 킬 후 ≥2초 노출")대로 **거의 항상 안정 판독됨**
   → 라운드 단위 정산(끝값-시작값)이 킬 체인 추적보다 근본적으로 강건.

## 설계 요지 (`files/hud_round_settle.py` — 구현·selftest 완료, 수정 금지 영역 아님이지만 로직 변경 전 Fable 근거 주석 필독)

- `settle_rounds(reads, boundaries, duration) -> list[SettledRound]` — 순수 함수.
  세그먼트(경계 사이)마다 독립으로:
  1. **지지 필터**: 세그먼트 내 관측 <2회이고 conf<0.88인 값 제거 (42:00 `7` 0.73 단발 등)
  2. **DP 최적 체인**: 비감소(상승폭≤3) 경로 중 conf 합 최대. **0-리셋 간선은 페널티(2.0)**
     → 산발 0 오독은 뒤따르는 정상 체인에 점수로 지고, 진짜 하프타임(이후 저값 지속)은 이김
  3. **k_base 이월**: 직전 세그먼트 k_end를 기준으로 `kills = k_end - k_base`.
     경계 직후(≤10s) 첫 관측이 곧 +1/+2면 직전 라운드 귀속(그레이스, R2 Task 2 의미 동일)
- 전역 순차 상태가 없으므로 "국소 수정 → 원거리 회귀"(핸드오프 '구조적 시사점') 구조적 소멸.
- ace 판정(kills==3·reset 제외·span·G5)은 기존대로 `detect_ace_hud.py` 소관.

## Task 1 — `timeline_from_reads` 통합 (판정 경로 교체)

`detect_ace_hud.py`에서:
1. `_KTracker` 경로(`tracker.update` 루프, `_postprocess_kills`, `_kill_round_index`,
   `_assign_events`의 킬 귀속부)를 `hud_round_settle.settle_rounds` 호출로 교체.
   - reads → `[(r.t, r.k, r.conf)]` 매핑, boundaries는 기존 CNN 검증(boundary_verdicts)
     반영 후의 것을 그대로 전달. **경계 계산·CNN 검증 로직은 변경 금지.**
   - `SettledRound` → `RoundTrack` 매핑: kills/kill_times/k_samples 그대로,
     `reset=True`→`resets=1`, first_kill_sec=min(kill_times), ace_sec=3번째 kill_time.
   - ace 판정식(G5 `_MIN_ROUND_K_SAMPLES`·`_MAX_ACE_SPAN_SEC`·`kills==3`·resets==0)은
     기존 `_assign_events` 말미 형태 유지.
   - JSON `kill_events`: 라운드별 k_base에서 누적 재구성
     (`KillEvent(t=kill_times[i], from_k=k_base+i, to_k=k_base+i+1)`) — 스키마 유지.
2. 구 코드는 삭제하지 말고 일단 남겨둠 (`_KTracker` 등) — diff 검증 끝나고 Task 4에서 정리.
3. 합성 스모크: `python -u hud_round_settle.py` (6/6) + `hud_from_cache.py` 1영상 실행 확인.

## Task 2 — 3종 세트 측정 + TP diff 정밀 대조

```powershell
python -u hud_from_cache.py && python -u _compare_hud_gt.py && python -u _tp_diff.py --compare-to r2_final
```

- **기대: +2 획득** (00-40-56 41:54, 03-02-03 14:15), 02-21-23 54:20은 **미탐 유지가 정상**
  (base 8이 EXCLUDE_DIGITS로 미판독 → ΔK 확정 불가. selftest ④에 명시. HMM 카드로 이월).
  이론치 recall 25/26 = 96.2%.
- **상실 0 필수.** 특히 확인할 기존 TP: 02-21-23 79:51(팬텀-8 케이스 — 정산 방식에선
  그레이스가 7→9 gap 2킬을 직전 라운드로 귀속해 커버할 것으로 설계됨. `_tp_diff`에서
  상실로 나오면 해당 구간 덤프 후 보고), 23-51-52 16:23(그레이스), 02-34-09 2:49(경계 검증).
- 상실 발생 시: 해당 GT 구간 `--dump` → 원인 분류 → **`hud_round_settle.py`의 상수 스윕**
  (`_SUPPORT_SINGLE_CONF` 0.84~0.95, `_RESET_PENALTY` 1.0~4.0, `_GRACE_SEC` 6~12)으로
  해결 시도. 로직 자체 변경은 selftest 6종 + 상실 0을 모두 유지할 때만.

## Task 3 — precision 정리 (목표 ≥80%)

R2 시점 FP 8건(00-40-56 3, 02-34-09 2, 02-03-10 1, 23-51-52 1, 02-21-23 1).
정산 방식은 (a) 스퓨리어스-0 리셋 오탐 (b) 오염 이월발 가짜 +3을 구조적으로 제거하므로
FP 감소 기대. 남는 FP는 각각 덤프로 분류하고, 위 상수 + `_MIN_ROUND_K_SAMPLES` 스윕.
**recall을 깎는 트레이드는 diff 표로 명시하고 중단** — 총점 개선이라도 TP 상실이 있으면 보고.

## Task 4 — 마무리

1. 실스캔 일치 확인 1회 (짧은 영상 1개 `detect_ace_hud.py` 직접 실행 vs 캐시 재계산).
2. 구 `_KTracker`·`_postprocess_kills`(팬텀-8)·`_kill_round_index` 등 대체된 코드 정리
   (완전 삭제 대신 파일 하단 주석 블록 or git 히스토리 의존 — 판단 맡김, 단 import 오류 없게).
3. `python -u _tp_diff.py --save-baseline r3_final` 저장.
4. `HUD_ACE_HANDOFF.md` §0에 R3 절 추가(수치 표·per-video·남은 미탐), 이 파일 헤더 갱신, 커밋.
5. 배치 37~113 재개는 **사용자 승인 후** (변경 금지).

## 금지·주의 (R3 추가분)

- `hud_round_settle.py`의 DP·이월 로직 수정 시 반드시 selftest 6종 유지 + 수정 근거를
  모듈 docstring에 추가. **실측 픽스처(①~⑥)는 삭제 금지.**
- 8 템플릿 재설치 금지 (EXCLUDE_DIGITS 유지 — 근거는 핸드오프 ★★★★ 절).
- 캐시(sig_cache_v2·boundary.json)는 그대로 재사용 — 판독 로직을 안 건드리므로 재캐시 불필요.
- 절대 규칙(§0) 전부 유효: ace=정확히 3, GT 26건이 유일 기준, M:SS 보고.

---

# R4 (2026-07-07): 숫자 CNN + 라운드 타이머 경계 — 근본 처방 (Fable 설계)

> 사용자 피드백(스크린샷 2장, 절대 신뢰):
> 1. **숫자를 못 읽는 건 치명적** — 8 제외(EXCLUDE_DIGITS)는 임시방편이었고, 남은
>    미탐 3건 중 2건(54:20 base=8, 79:51 3번째킬=7→8)의 뿌리가 그것. 전광판/WIN엔
>    학습 CNN을 쓰면서 숫자만 비학습(템플릿 IoU)으로 남긴 게 근본 문제.
> 2. **라운드 타이머(미니맵 아래, 좌상단)를 쓰라**: 라운드 진행 중 카운트다운 →
>    종료 시 `00:00` → WIN/DEFEAT 배너 → 킬 전광판 → 다음 라운드 시작 시 **02:20으로
>    리셋**. "10초 그레이스" 같은 보조장치는 절대 중요하지 않음 — 타이머 리셋이 경계다.
> 3. **클립 범위 완화**: 사용자는 킬 순간이 잘리는 걸 극도로 싫어함. 퍼스트킬 **4초 전**
>    부터 시작, 멀티킬 뒤가 잘리지 않도록 끝 +n초는 감당 가능.
>
> R4 완료 시 기대: 미탐 3건 전부 해소 (26/26). 폭0 FP(R3 정밀 퇴보의 주범 —
> "6에서 곧장 9로 점프")도 상당수가 8 미판독 때문일 가능성이 높아 precision 동반 개선 기대.

## 절대 규칙 (기존 + R4)

- ace = 킬 합 정확히 3. GT 26건이 유일 기준. M:SS 보고.
- **EXCLUDE_DIGITS(8 제외)는 R4에서 폐지가 목표** — 단 반드시 CNN 판독기로 교체 후.
  IoU 매칭에 8 템플릿을 다시 넣는 방식은 여전히 금지(★★★★ 절에서 순손실 실측됨).
- 측정은 3종 세트 + `--compare-to r3_final`. 태스크마다 상실 0 확인.

## Task A — 숫자 판독기를 CNN으로 교체 (8 부활)

**데이터 (전부 이미 존재)**:
- `E:\clipai_result\hud_templates_harvest\raw\` — 수확 글리프 3789장
- `E:\clipai_result\hud_templates_harvest\cluster_labels.json` — 클러스터→숫자 라벨
  (0~7·9 각 3클러스터). 라벨은 클러스터 단위이므로 raw 글리프를 클러스터에 재배정
  (harvest_hud_digits.py의 IoU 클러스터링 재사용)해서 글리프 단위 라벨 생성.
- `E:\clipai_result\hud_templates_harvest\eight\` — 8 전용 재수확분 (라벨=8).
- **junk 클래스**: 수확 중 클러스터 라벨이 "noise"/"mixed"였던 것 + template_miss
  프레임에서 잘린 글리프 일부 → "숫자 아님" 클래스로 포함 (오탐 방어).

**모델·학습**:
- 입력 = `hud_digit_match.normalize_glyph` letterbox 출력(기존 전처리 재사용 — 판독
  파이프와 완전 동일해야 함). 클래스 11개(0~9 + junk).
- 아키텍처는 `detect_rounds.py`의 scoreboard_clf 패턴(작은 conv 2~3층)을 축소 재사용.
  글리프가 작으므로 수 분 내 학습 완료. `E:\Highlights\ml_dataset\models\hud_digit_clf_best.pt`.
- 학습/검증 분리: **영상 단위 split**(같은 영상 글리프가 train/val 양쪽에 가면 누수).

**통합 (`hud_digit_match.py`)**:
```
match_glyph_cnn(glyph): p = softmax(cnn(normalize_glyph(glyph)))
  top1, top2 = 상위 2클래스
  if top1 == junk or p[top1] < _CNN_MIN_P(초기 0.85): return None(판독실패)
  return top1, p[top1], (top2, p[top2])   # top-2는 캐시에 저장 (HMM 대비)
```
- IoU 매칭은 폴백으로 남기되 기본 경로는 CNN. `EXCLUDE_DIGITS` 제거.
- 트리플 가드(G3)는 그대로 유지.

**수용 기준 (재캐시 전에 반드시)**:
- held-out 정확도: 0~7·9 ≥ 99%, 8 ≥ 95%, junk 거짓양성 ≤ 1%.
- 실프레임 스팟 A/B: (a) 79:51.0~79:53 / 54:15~54:20 부근 프레임에서 **8이 읽히는지**,
  (b) 기존에 잘 읽히던 0/6/9 프레임 20장에서 판독률이 IoU 대비 안 떨어지는지.

## Task B 진행 메모 (2026-07-07 Sonnet, R4 실행 중 — ROI 확정, 통합은 미완)

**ROI 확정** (`extract_game_crop_bgr` 출력 기준 비율): `gy=(0.212, 0.248)`,
`gx=(0.0, 0.165)` — 4:3 영상(`2026-03-21 00-40-56`, game 1382×1048)에서
"02:17" 전체가 깨끗이 잡힘 확인. 16:9 캘리브 영상(`2026-01-08`, game 1894×1069)
에서도 같은 비율로 타이머가 잡히긴 하나 좌우 여백이 달라 완전히 딱 맞진 않음
(4:3/16:9 별도 비율 필요할 수 있음 — 라벨 10영상은 전부 4:3이라 급하진 않음).

**마스킹이 K/D/A와 다름 — 그대로 재사용 불가**: `white_mask`(기존, thresh=155)로
바로 이진화하면 타이머 숫자는 K/D/A 빨간 숫자처럼 꽉 찬 solid fill이 아니라
**베벨/그라데이션 효과가 있는 outline에 가까운 형태**로 나옴(threshold 190에서
외곽선만, 170에서 두꺼운 outline — 어느 쪽도 기존 숫자 CNN 학습셋(solid fill)과
분포가 안 맞음). `drawContours(thickness=-1)`로 채워봐도 원래 외곽선 형태 자체가
얇아서 큰 개선 없었음 — **정면 대응 필요**: (a) 타이머 전용 소량 라벨 데이터로
CNN fine-tune, 또는 (b) 모폴로지 dilate+erode(close)를 충분히 크게 걸어 진짜
solid fill로 만든 뒤 재시도, 둘 다 미검증.

**콜론(:) 분리 미구현**: `locate_kda_glyphs`의 슬래시 검출 로직(기울기 기반)과
다른 접근 필요 — 콜론은 위아래 두 점이라 K/D/A 슬래시와 모양이 다름. 컨투어
바운딩박스의 aspect ratio(세로로 길지 않고 작은 정사각형에 가까움)로 분리하는
게 나을 듯(미검증). MM:SS 고정 폭이므로 **차라리 좌/우 절반으로 고정 분할**(콜론
건너뛰기)하는 게 더 간단하고 안전할 수 있음 — 초 단위 정밀도 불필요(스펙 확정
사항)하므로 분 두 자리만 정확히 읽으면 되고, 초 두 자리는 대충 무시해도 됨.

**다음 세션 시작점**: 위 마스킹 문제부터 해결(모폴로지 close 우선 시도) →
분 두 자리만 읽는 걸로 스코프 축소(초 자리 폭 대충 계산해서 버림) → 여러
프레임에서 스팟체크(00:00 리셋 순간 포함) → `hud_round_settle.py`에 경계
소스 추가(row_miss 방식과 병행 운용 후 대조, 전면 교체는 검증 후).

## Task B — 라운드 타이머 판독 → 경계 신호 교체

**위치**: 미니맵 바로 아래, K/D/A 행(`_KDA_BAND_Y=(0.20,0.34)`) **바로 위** —
대략 gy 0.13~0.21, gx 0.0~0.15 부근의 큰 흰 숫자 `MM:SS`(콜론 흐릿할 수 있음).
정확 좌표는 캘리브 스크립트로 확정(스크린샷 확인된 프레임: 라운드 중 `02:19`,
종료 시 `00:00`). 4:3/16:9 둘 다 확인할 것.

**판독**: 흰색 채널 이진화 → 글리프 분리 → Task A의 숫자 CNN 재사용(색만 다름,
전처리에서 그레이 정규화하면 동일 모델로 가능한지 먼저 스팟 확인; 안 되면 타이머
글리프 소량 수확해 fine-tune). 초 단위 정밀도 불필요 — **분 자리만 정확해도 충분**.

**핵심 로직 (경계 = 타이머 리셋)**:
```
timer_sec(t) 시퀀스에서:
  리셋 = 직전 유효 판독 대비 +60초 이상 점프 (예: 00:xx → 02:20)
  → 경계 시각 = 리셋이 처음 관측된 t (다음 라운드 시작)
  → 직전 라운드 세그먼트는 [이전 리셋, 이번 리셋) — 00:00 구간·배너·전광판이
    전부 "끝나는 라운드"에 자연 귀속 (그레이스 하드코딩 불필요해짐)
검증: 타이머 경계 vs 기존 row_miss+CNN 경계를 라벨 영상에서 대조 —
  일치율 확인 후 타이머를 1순위, row_miss는 타이머 미판독 구간의 폴백으로 강등
```

**캐시 스키마 확장**: `hud_sig_cache.py` reads 항목에 timer 필드 추가
(`[t, k, conf, method, timer_sec|null]`). `hud_from_cache.py` 로더 호환 유지
(길이 4 항목도 읽히게 — 구 캐시 폴백).

## Task C — 재캐시 + 측정 (A·B 합쳐서 1회)

```powershell
python -u hud_sig_cache.py --force   # ~70분, 백그라운드
python -u hud_boundary_verify.py     # 경계 검증 재생성 (참고용 유지)
python -u hud_from_cache.py && python -u _compare_hud_gt.py && python -u _tp_diff.py --compare-to r3_final
```
- 기대: +54:20(base 8 판독), +79:51(7→8 킬 판독), +14:15(타이머 경계) = **26/26**.
- 폭0 FP 6건 재검토: 8이 읽히면 "6→9 점프"들이 6→7→8→9로 풀리거나 오독으로 판명될 것.
  잔존 FP는 개별 덤프 → 분류 → 보고.
- `hud_round_settle.py`는 로직 변경 없이 그대로 (판독·경계 품질만 올라감).
  selftest 6/6 유지 확인.

## Task D — 클립 규칙 (사용자 확정 사양)

`detect_ace_hud.py` `ace_clip_window()`:
- 시작 = **첫 킬 − 4초** (`_CLIP_PRE_KILL_SEC` 14→4).
- 끝 = 3번째 킬 + 여유(잘림 절대 금지 — 라운드 종료(타이머 리셋)까지 포함해도 됨,
  `_CLIP_POST_END_SEC` 유지하되 ace_sec+12 캡이 킬을 자르지 않는지 확인).
- `_CLIP_MAX_SEC` 초과 시 뒤가 아니라 **앞을 잘라야** 함(킬은 항상 보존) — 현행
  `start = end - MAX` 방식이 이미 그렇게 동작하는지 확인만.

## 완료 시

실스캔 1회 일치 확인 → `r4_final` 베이스라인 저장 → 핸드오프 §0에 R4 절 +
이 파일 헤더 갱신 → 커밋. 배치 37~113 재개는 사용자 승인 후.

---

# R5 (2026-07-09): 오탐 12건 소탕 + recall 52.7% 원인 규명 (Fable 설계, 하위 AI 수행)

> 배경: 사용자가 배치 결과를 육안 검수해 GT를 60영상/97건으로 확장.
> 전체 recall 54.6% (53/97), precision 70.7% (53/75) — 라벨 10영상 튜닝(88.5%)이
> 일반화되지 않음. 오탐 12건은 사용자가 K/D 흐름까지 판독해줌
> (`files/_reorg_highlights.py` FP_SET 주석 참고 — 메커니즘별 분류 완료).

## 이번 세션(Fable)에서 이미 반영된 것 — 재작업 금지

1. **D/A 채널 기록**: `KRead`에 `d`/`a` 필드 추가, `collect_reads`가 저장,
   캐시 스키마 6원소로 확장(구 4원소 캐시도 로더가 하위호환으로 읽음).
2. **D-가드** (`hud_round_settle.py`): 라운드 내 지지된 D 변화(사망/관전) 시각
   이후의 킬 무효화. `d_reads` 없으면 완전 no-op. selftest 8/8 통과.
   상수: `_D_GUARD_MARGIN_SEC=2.0`(트레이드킬 허용), `_D_SUPPORT_MIN=2`.
3. **`_dump_reads_window.py`**: 캐시 없는 배치 영상의 구간 원시 K/D/A 덤프 도구.
   `python -u _dump_reads_window.py "E:\OBS\<stem>.mp4" 18:20 19:00`
4. GT 정정 4건(오탐→정답: 03-26 R49, 04-08 02-15-28 R69, 04-28 R58, 05-18 R19)
   + 클립 원위치 + FP_SET 갱신 완료.

## 절대 규칙 (기존 유지)

- ace = kills **정확히 3**, 시각 보고 M:SS, `_EV_WINDOW` 확대 금지(과거 순손실),
  8 템플릿 재설치 금지(EXCLUDE_DIGITS), CNN 폴백 재활성화 금지(R4 Task A 조건 미충족 시).
- 모든 실험은 **측정 3종 세트** 후 diff 확인: 총점만 보지 말 것.
- GT 단일 진처: `files/_compare_hud_gt.py`의 `GT` 딕셔너리 (60영상/97건).

## Task 1 — 오탐 12건 원시 판독 진단 (덤프, 코드 수정 없음)

`files/_reorg_highlights.py` FP_SET의 12건 각각에 대해 `_dump_reads_window.py`로
탐지 시각 ±40s 창을 덤프해 **+3이 어디서 만들어졌는지** 분류:

| 가설 | 시그니처 (덤프에서 볼 것) |
|------|--------------------------|
| (a) 단일 간선 +3 점프 | carry/체인값에서 중간값 없이 +3 값이 지지되어 점프 |
| (b) 팬텀 +1 | 실제 +2는 정상, 마지막 +1이 단발 conf>=0.88 오독 |
| (c) 오귀속 | 직전 라운드에서 판독 공백, 다음 라운드 첫 판독이 +3 |
| (d) 관전 | K와 D가 동시에 이상값 (D-가드가 잡을 것 — 검증만) |

각 건의 분류 결과를 이 파일에 표로 추가. **여기서 게이트 설계가 나옴 — 서두르지 말 것.**

## Task 2 — 라벨 10영상 재캐시(D 포함) + D-가드 A/B

```powershell
python -u hud_sig_cache.py --force        # 라벨 10영상, D/A 포함 재캐시 (수십 분)
python -u hud_from_cache.py; python -u _compare_hud_gt.py; python -u _tp_diff.py --compare-to r3_final
```
**합격**: 라벨 10영상에서 TP 상실 0 (r3_final 유지 23건). D-가드로 FP가 줄면 보너스.
상실 발생 시 `d_guard_dropped > 0`인 라운드를 덤프로 확인 — D 오독이 원인이면
`_D_SUPPORT_MIN` 3으로 상향 시도(그 이상은 가드 무력화 — 중단하고 보고).

## Task 3 — 오탐 12건 영상 재스캔 → FP 소멸 확인

D-가드는 실스캔에서만 활성(구 캐시엔 D 없음). FP 12건이 속한 영상 9개를 재스캔:
```powershell
python -u batch_hud_ace_pipeline.py --redo --scan-fps 4 --only "<stem>"   # 9개 각각
python -u _compare_hud_gt.py
```
**측정**: FP 12건 중 몇 건이 사라졌나 + **TP 53건 상실 0 확인** (합계가 아니라
per-video diff로). Task 1 분류상 (d)관전형만 D-가드 대상 — (a)(b)(c)는 남는 게 정상.

## Task 4 — (a)(b)(c) 게이트 설계·구현 (Task 1 데이터 기반)

Task 1 분류 결과를 보고 설계. 후보 (실측으로 취사선택):
- (a)형: 점프 간선(+2/+3)의 도착값에 "서로 다른 시각 2회 이상" 지지 요구.
  ⚠ 02-21-23 "11 conf0.90 1회" TP 실측 때문에 `_SUPPORT_SINGLE_CONF`가 존재 —
  일괄 강화하면 그 TP가 깨짐. **점프 간선에만** 강화 조건을 거는 것이 핵심.
- (b)형: kills를 3으로 만드는 마지막 전이의 도착값 지지 강화 (같은 함정 주의).
- (c)형: `_GRACE_MAX_KILLS` 3 허용 + `_GRACE_SEC` 완화 스윕 — 05-12 R30(직전 라운드
  올킬이 다음 라운드로 오귀속) 복구가 목표. ⚠ 폭0 FP와 상충 가능 — diff 필수.
- 각 변경은 **개별로** 캐시 재계산 A/B → 순이득일 때만 채택.

## Task 5 — 미탐 43건 → ✅ 완료 (2026-07-09 답변 수집 · 2026-07-10 분석 · 2026-07-11 문서화)

> ⚠ **이 Task는 끝났음. "사용자 확인 대기"가 아님 — 다시 물어보지 말 것.**
> 사용자가 `_miss_review\` 클립 43건 전부에 K/D 흐름·특이사항을 답변 완료했고,
> 그 원본 답변은 **`MISS_FEEDBACK_FORM.md`에 클립별로 전부 기록돼 있음**(빈 양식
> 아님). 그 데이터로 나온 원인 분류(8-엔드포인트/D=8오독/세이브동반/순삭)는 위
> "미탐 43건 원인 분포" 표 참고, 그 분류를 근거로 한 코드 수정(경계검증 연결,
> 0-격리, gap시각 보정 등)은 R5 전체에 이미 반영·커밋됨.
> **최종 결과**: 43건 중 12건 복구(정탐), 1건 GT 제거(오탐 확정), 30건 아직 미탐
> — 이 30건의 구체적 원인·상태는 `MISS_FEEDBACK_FORM.md`에 클립별로 적혀있음.

## 완료 시

측정 표(전체 60영상 recall/precision, FP 소멸 수, TP 상실 0 증빙) + Task 1 분류표를
이 파일과 핸드오프 §0에 기록 → 커밋. D 드라이브 162개 배치는 여전히 사용자 승인 후.

---

# R5 갱신 (2026-07-09 밤): 미탐 43건 사용자 검수 완료 — Fable 분석·수정 반영

> 사용자가 43건 전부 K/D 흐름 + 특이사항을 판독해줌 (이미지=탐색기 자연정렬 순서).
> Fable이 파일↔피드백 매핑(자연정렬 보정, D값 연대순으로 교차검증) 후 패턴 분석,
> 핵심 수정 3건을 직접 구현·selftest 통과. 하위 AI는 아래 "남은 태스크"만 수행.

## 미탐 43건 원인 분포 (Fable 분석 확정)

| 버킷 | 건수 | 내용 | 상태 |
|------|-----|------|------|
| ★8-엔드포인트 | **9** | K 시작/끝값이 8 (미판독) → ΔK 산정 구조적 불가: #1(8/8→11/8), #2(5/6→8/6), #5(5/4→8/4), #8(5/2→8/2), #13(5/1→8/1), #21(8/4→11/4), #23(8/3→11/3), #34(5/2→8/2), #39(5/7→8/7) | **Rule A/B 구현됨** — 측정 필요 |
| ★★D=8 정전 | 3 | D가 8인 구간은 트리플 가드가 모든 프레임 폐기 → 완전 판독 불가: #1, #36(7/8→10/8), #37(6/8→9/8). 완전미탐 영상 2개(05-23 02-48-06, 05-26 23-45-51)의 원인 | **더블 가드 완화 구현됨** — 재스캔 필요 |
| 세이브 동반 | ~11 | #8,14,16,23,26,28,29,31,32,38,40 — 세이브 성공 = 올킬급인데 kills<3인 특수케이스 2건(#28 6/0→7/1사망→9/1, #29 0/0→2/0) 포함 | **세이브 감지 신규 피처 필요** (아래) |
| 더블킬/멀티킬 순삭 | ~17 | 중간값 노출 극단적으로 짧음 + 라운드 막판 킬 → 최종값 지지 부족 추정 | **덤프로 검증 후** 꼬리 완화 설계 |
| 경계 특이 | 1 | #27(04-27 58:02): 마지막 라운드라 전광판 없음 → 경계 미형성 | 덤프 확인 |
| GT 재검토 | 2 | #25(04-27 22:44 '1/2→2/3 사망' — 올킬 아닌 듯), #12(이름 불일치) | **사용자 확인 대기** |

⚠ 매핑 주의: 사용자 L12에 "00-36-50_40m37s" 이름이 붙었지만 위치상 02-55-36_17m14s의
피드백(11/12→14/12)으로 판단(L29=05-07 세이브 케이스가 정확히 맞아떨어지는 것으로 검증).
사용자 확인 전까지 #12·#13 두 건은 잠정.

## Fable이 이번에 구현한 것 (재작업 금지, 측정만)

1. **더블 가드 완화** (`detect_ace_hud.collect_reads`): K 채택 조건을 "D와 A 모두"에서
   "D 또는 A 하나"로 — D=8 정전 해소. 판독 레이어 변경이므로 **캐시 무효** → 재캐시 필수.
2. **Rule A** (`hud_round_settle.py`): 이월 무효 + 라운드 9 시작 + 마지막 K≤7 → k_base=8.
3. **Rule B**: 직전 라운드 2킬·7 종료 + 현 라운드 9 시작 → 3번째 킬(7→8)을 직전 라운드로
   브리지. `_BRIDGE_8=True` 플래그로 온오프. selftest 10/10.
   ⚠ Rule B의 FP 리스크(진실이 "R에서 2킬, R+1에서 7→8 킬"인 경우 R을 가짜 ace로 완성)는
   K 채널로 원리적 구분 불가 — **97건 GT 전수 측정으로 순이득 확인 필수. 순손실이면
   `_BRIDGE_8=False`로 끄고 보고.**

## 남은 태스크 (하위 AI, 우선순위순)

### T1. 재캐시 + 라벨영상 회귀 측정 (필수 선행)
판독 레이어가 바뀌었으므로: `python -u hud_sig_cache.py --force` (라벨 10영상)
→ `hud_from_cache.py && _compare_hud_gt.py && _tp_diff.py --compare-to r3_final`
합격: TP 상실 0. 상실 시 원인 라운드 덤프 후 보고 (Rule B가 원인이면 끄고 재측정).

### T2. 미탐 관련 영상 전체 재스캔 + 97건 전수 측정
미탐 43건이 속한 영상들(약 28개) `--redo` 재스캔 → `_compare_hud_gt.py`.
기대: 8-엔드포인트 9건 중 상당수 + D=8 정전 3건 복구 → recall 54.6%에서 유의미 상승.
**per-video diff 필수** — 새 FP가 생기면 Rule A/B/더블가드 중 무엇이 유발했는지 분류.

### T3. 순삭 버킷 덤프 검증 (~17건)
`_dump_reads_window.py`로 각 GT 창 덤프 → "라운드 막판 최종값이 지지 부족(단발/저신뢰)으로
버려졌는지" 확인. 맞으면 라운드 꼬리(경계 직전 ~6s) 한정 지지 완화(`_supported_obs`에
세그먼트 끝 시각 전달)를 설계·A/B. 아니면 실제 판독 분포 보고 후 재설계.

### T4. 세이브 감지 설계 (신규 피처, 별도 승인 후 착수)
정책(사용자 확정): 세이브 성공 = kills<3이어도 올킬급 인정 (#29 유형).
후보 신호: "세이브찬스/세이브에 성공" 흰 문구(화면 중앙), MULTI KILL 로고.
기존 highlight_types CNN(save 클래스 87.5%)을 프레임 단위로 재활용하는 방안 검토.
착수 전 설계만 문서화해 사용자 승인 받을 것.

### T5. D-가드 알려진 한계 (기록)
#28(04-29 01-14-15 11:15): 사망 후 K가 계속 오르는 실측 케이스(세이브 찬스) —
현 D-가드는 이런 킬을 무효화함. 세이브 감지(T4) 전까지는 구조적 미탐. T2 측정에서
이 GT가 D-가드 때문에 안 잡히는 건 정상(기록만).

## 사용자 확인 대기 (하위 AI는 건드리지 말 것)

1. #12/#13 매핑: "11/12→14/12"가 02-55-36(17:14) 것인지 00-36-50(40:37) 것인지
2. #25(04-27 22:44-22:59): K/D상 1킬+사망 — GT에서 빼야 하는지
3. #6(01-01-04 17:15): 클립을 18:50까지 재추출해둠(`_미탐_17m15s-18m50s.mp4`) — 재확인 요청

---

# R5 T3 핵심 발견 (2026-07-09, Sonnet): 전광판 CNN 검증이 실제 배치에 연결 안 돼 있었음

**구조적 공백 발견**: `hud_boundary_verify.py`(R2 Task 1, +2 TP 검증됨)가
`hud_from_cache.py`(라벨 10영상 실험 전용 경로)에만 연결돼 있고, **실제 배치
스캔 진입점인 `scan_hud_aces`에는 한 번도 연결된 적이 없었음** — 즉 지금까지
처리한 60개 영상 전부가 이 검증 없이 스캔됨.

## 실측 재현 (2건)

| 영상 | 가짜 경계 구간 | 증거 |
|------|---------------|------|
| `2026-04-13 22-46-39` 12:33-12:56 | 12:46.50-12:51.75 (5.25s row_miss) | K=3(12:46)→3(12:52) 불변, D=1 불변 — 스코어보드 아님. 이 가짜 경계가 라운드를 R31/R32로 쪼개 킬 2개+1개로 분산 |
| `2026-04-24 00-43-29` 5:50-6:02 | ~5:58-6:17 (긴 row_miss, LONG_RUN_FRAMES 초과 추정) | 별도 원인일 수 있음 — 검증기 미적용 케이스라 확답 불가 |

`hud_boundary_verify.py` 상단 주석에 이미 "배너/사망연출 등에 의한 가짜 경계 —
GT 올킬 한복판을 쪼개는 원인"이라고 정확히 예견돼 있었음. MULTI/DOUBLE KILL
연출이 이 실패 모드의 실측 원인으로 추가 확인됨.

## 조치 (구현·검증 완료)

- `hud_boundary_verify.py`: `verify_video`(캐시 전용)에서 `verify_runs_live(video_path,
  reads, model, transform, device)` 코어를 분리 — 캐시/사전작업 없이 임의 영상에
  바로 적용 가능. `get_boundary_verifier()`로 모델 프로세스 전역 캐시(배치 루프
  재로드 방지).
- `detect_ace_hud.scan_hud_aces`: `verify_boundaries=True`(기본값)로 위 코어를
  호출 → `timeline_from_reads(boundary_verdicts=...)`에 연결. 모델 로드 실패해도
  경고만 남기고 검증 없이 진행(회귀 없음 — try/except로 감쌈).
- **스모크 테스트 통과**: `2026-04-13 22-46-39` 12:33-12:56 재스캔 결과 R31/R32가
  하나로 병합돼 `kills=3, ace=True` — GT(1/1→4/1) 정확히 일치. scan time 859s.

## 하위 AI에게: T2 실행 중 자동 반영됨 — 재작업 불필요

T2(26개 영상 `--redo` 재스캔)는 반복마다 새 `python` 프로세스라 이 수정이
**자동으로 적용됨** — 단, 맨 처음 처리된 `2026-03-26 01-26-52`만 수정 전에
끝나서 미반영. 이 영상은 미탐 버킷에 멀티킬 케이스(#4, 9:40-9:50)가 있으니
**T2 완료 후 이 영상 하나만 다시 `--only`로 재실행**할 것.

## 다음 우선순위 (T2 완료 후)

1. 97건 전수 측정 — 이 수정 하나만으로 8-엔드포인트(Rule A/B)와 별개로
   recall이 얼마나 오르는지 분리 측정 (per-video diff로 어느 게 뭘 고쳤는지 분류)
2. `04-24 00-43-29` 같은 LONG_RUN_FRAMES(10s) 초과 케이스는 검증 대상이 아니므로
   여전히 미탐 가능 — 이 임계값이 실전에서 너무 낮은지(멀티킬 연출이 10초 넘게
   HUD를 가릴 수 있는지) 사용자 확인 또는 추가 실측 필요
3. 이 발견은 **정답 GT가 없는 나머지 30여 개 영상에도 소급 적용**됨(배치 자체가
   고쳐진 것이므로) — 향후 모든 스캔에 자동 반영, 별도 조치 불요

---

# R5 T2/T3 결과 (2026-07-09, Sonnet): 26개 재스캔 완료 + per-video 분석

## 측정 결과

| 지표 | T2 이전 | **T2+경계검증 이후** |
|---|---|---|
| GT | 97건 | 97건 |
| recall | 54.6% (53/97) | **63.9% (62/97)** (+9.3pp) |
| 검출 | 75건 | 82건 |
| precision | 70.7% (53/75) | **75.6% (62/82)** (+4.9pp) |
| FP | 22 | 20 |

**recall·precision 동시 개선, FP도 순감소** — 트레이드오프 없는 순수 개선.
(⚠ `2026-03-26 01-26-52` 하나는 경계검증 수정 전에 처리돼 재재스캔 중 — 완료
시 recall이 소폭 더 오를 수 있음. `_tp_diff.py` 베이스라인을 재스캔 전에
안 남기는 실수를 해서 정확한 순수 diff는 못 냈고, per-video 수동 대조로 대체함)

## per-video 회귀 확인 (수동, 26개 재스캔 영상)

기존 100% 매칭이던 영상(`04-08 02-15-28` 2/2, `04-26 01-29-40` 2/2, `05-15 01-41-42`
2/2, `05-31 02-37-59` 2/2 등)은 재스캔 후에도 전부 유지 — **명백한 회귀 없음.**

새로 나타난 FP 2건(`05-19 01-36-22` R41·R52) 확인 결과: **D 불변(D-가드 대상 아님),
8/9 무관(Rule A/B 대상 아님), 긴 row_miss 없음(경계검증 대상 아님)** — 순수
"K가 6을 건너뛰고 5→7로 점프"하는 기존 (a)형 패턴. 오늘 수정이 유발한 게 아니라
라운드 재분할로 노출된 기존 잠재 FP로 판단 — **Task 4(a)형 게이트 대상으로 이월.**

## ★ 중요 정정: "D=8 정전" 재분류 — 진짜 원인은 스퓨리어스-0 리셋 오염

완전미탐 2건(`05-23 02-48-06`, `05-26 23-45-51`)을 실측 덤프로 직접 확인한 결과,
Fable의 1차 분석(D=8 정전)과 다른 그림이 나왔다 — **더블가드 완화 수정은
정상 작동 중**(A만 읽혀도 K 채택됨을 확인)이지만, 두 건 모두 실제 원인은:

1. `05-23 02-48-06` 45:53-46:14: K=7 안정 구간 뒤에 **고신뢰(0.69-0.71) 스퓨리어스
   `0`이 6회 연속 지지되어 DP 리셋 간선이 승리**(`resets=1` 확정) → carry_k가
   7이 아니라 0으로 오염 → 이후 K=10이 등장해도 `10-0=10 > _MAX_STEP` 이라
   carry 실패, kills=0. **이건 D=8과 무관 — D는 시종일관 `0`으로 읽힘(8 아님).**
   이 패턴은 핸드오프에 이미 여러 차례 기록된 **"G1 스퓨리어스-0 리베이스
   오염"** 문제(재시도 시 순손실 반복 확인됨)와 동일 메커니즘.
2. `05-26 23-45-51` 26:01-26:18: K가 `6`(1회) → `0`(다수, 고신뢰) 으로 읽히고
   **K=9는 한 번도 관측되지 않음.** D는 이 구간 내내 아예 미판독(`-`).
   사용자가 보고한 "6/8→9/8"과 실측 판독이 거의 안 겹침 — **D=8 자체가
   화면에 있었다면 K 판독까지 같이 오염되는 현상**일 가능성 (K·D가 같은
   행에서 슬래시로 분리되는데, 8 모양이 근처에 있으면 분리·매칭 자체가
   흔들릴 수 있음 — 가설, 미검증).

**결론**: 오늘 구현한 더블가드 완화(D 또는 A 중 하나)는 의도대로 작동하지만
(D 없어도 K는 읽힘 확인), 이 두 영상의 완전미탐은 **D=8 판독 실패가 아니라
스퓨리어스-0 리베이스 오염**이 직접 원인이다. 이 문제는 R2/R3에서 여러 번
시도했다가 다른 TP를 깨서 되돌린 이력이 있는 어려운 문제 — **Fable 판단
필요, 하위 AI가 임의로 재시도하지 말 것.**

## 다음 우선순위 (갱신)

1. **[Fable 판단 필요]** 스퓨리어스-0 리베이스 오염 — 위 실측 2건이 재현
   사례. `_RESET_PENALTY`를 캐시 기반으로 스윕하거나, "리셋 후보 자체도
   최소 지지 시간(예: N초 연속)"을 요구하는 방향 검토 여지 있음(기존
   `_D_SUPPORT_MIN`처럼 시간 분산 조건 추가) — 단, G1 이력상 라벨 10영상
   회귀 필수.
2. Task 4 (a)형 게이트 설계 — 이번에 노출된 05-19 R41/R52 포함해 실측 데이터 누적됨.
3. `2026-03-26 01-26-52` 재재스캔 완료 확인 + 최종 97건 수치 재확정.
4. D=8이 K 판독까지 오염시키는지 여부(가설) — 프레임 육안 확인으로 검증 필요.

---

# R5 Fable 판단 (2026-07-10): 스퓨리어스-0 정체 규명·해소 + 잔여 항목 판정

## 판정 결과 (Sonnet이 넘긴 4개 항목)

### 1. 스퓨리어스-0 리베이스 오염 → ★근본 원인 규명·해소 (커밋 01d20b2)

프레임 육안 검증(05-26 26:10 — 실제 K/D/A **8/9/0**인데 K가 '0' conf 0.7대로
판독)으로 **"스퓨리어스 0" = 화면의 8이 가장 닮은 0으로 오독되는 것**임을 확정.
R2부터 기록된 고신뢰 0 사례들(42:24 conf 0.95, 54:28, 45:58) 전부 킬 체인이
8을 지나는 시점과 일치 — 하나의 원인이었음.

- 글리프 레벨 분리는 **실측으로 불가 판정** (진짜 0의 8-마진이 진짜 8보다 작음
  — hud_digit_match.py 주석, 재시도 금지)
- 해소: `_quarantine_zeros` — K 단조성 도메인 규칙. "0 전후의 K가 이어지면
  가짜(격리), 진짜 리셋은 이후 K가 반드시 더 작게 재시작"
- 부수: `_GAP_WEAK_CAP`(약한 판독으로 gap 상한 — R112 유형 폭0 FP 정정),
  gap 킬 시각 first_t→s(04-24 5:50 GT 이탈 방지), k_samples 원시 기준 유지
- 검증: selftest 13/13, 라벨 캐시 회귀 유지 62/상실 0

### 2. Task 4 (a)형 게이트 → _GAP_WEAK_CAP으로 부분 대응, R41/R52는 GT 재검토行

05-19 R41(27:38)/R52(36:44)는 덤프상 "5→7 점프"인데 **먼저 온 4건(오탐→정답
정정)과 같은 패턴일 수 있음** — 사용자 클립 육안 재확인 요청 상태. 진짜 오탐으로
확정되면 그때 in-chain 점프 게이트 설계 (지금 설계하면 라벨 오류에 맞추는 꼴).

### 3. 2026-03-26 재재스캔 → 완료 (Sonnet 확인, 수치 변동 없음 — 63.9% 확정)

### 4. D=8 가설 → 정정: D=8도 '정전'이 아니라 0으로 오독됨

D-가드 영향 분석: D=8이 상수로 오독되면(라운드 내내) base 자체가 '0'으로 잡혀
변화 없음 → 무해. 라운드 중 7→8 사망 전이는 7→'0' 변화로 잡히는데 이는 진짜
사망 시점과 일치 → 의도대로 작동. **D-격리는 불필요 판정** (단 T-next 측정에서
d_guard_dropped 회귀 발견 시 재검토).

### 5. LONG_RUN_FRAMES → 40 유지 판정. 04-24 케이스는 gap 킬 시각 수정으로 해소됨.

## T-next (하위 AI): 27개 영상 재스캔 + 전수 측정

0-격리·weak-cap·gap시각은 정산 레이어라 기존 JSON에 반영 안 됨 — 재스캔 필요:
미탐 잔존 26개(기존 목록과 동일, 2026-03-26 포함) + `2026-04-23 23-41-07`(FP R06).
```powershell
# 기존 /tmp/rescan_stems.txt 목록 + 04-23 추가, --redo로 순차 실행
python -u _compare_hud_gt.py   # 완료 후
python -u _tp_diff.py --compare-to r5_pre_boundaryfix   # 상실 0 확인
```
기대 복구: 05-23 45:53(0-격리), 05-12 18:28·04-24 5:50(gap시각), 05-31 R112
FP 소멸(weak-cap), 03-30 02-55-36 등 0-오염 미탐 다수. **per-video diff 필수.**

## 사용자 확인 대기 (누적)

1. #12/#13 매핑 ("11/12→14/12"가 02-55-36 vs 00-36-50 어느 쪽?)
2. #25(04-27 22:44-22:59) GT 제외 여부 (K/D상 1킬+사망)
3. #6(01-01-04) 18:50까지 재추출한 클립 재확인
4. **[신규] 05-19 01-36-22 R41(27:38-27:48)·R52(36:44-36:50) 클립 육안 확인** —
   진짜 올킬이면 GT 추가(앞서 4건 정정과 동일 케이스), 아니면 오탐 확정

---

# R5 T-next 최종 결과 (2026-07-10, Fable): 27개 재스캔 완료

## 최종 수치 (GT 96건 — 04-27 22:44 오탐 제거 반영)

| 지표 | R5 시작 | **T-next 최종** |
|---|---|---|
| recall | 54.6% (53/97) | **66.7% (64/96)** |
| precision | 70.7% | **71.1%** (90검출 중 64 TP) |

`_tp_diff --compare-to r5_pre_boundaryfix`: **유지 62 | +획득 2 | -상실 0** (회귀 0).
- +획득: 03-29 03-12-36 7:21(R10), 03-30 02-19-18 25:14(R39) — 격리·8보정 효과
- 베이스라인 저장: `r5_final.json` (64/96)

## precision이 왜 안 올랐나 — 폭0 FP 노출 (근본적 한계 재확인)

격리 수정이 인접 라운드 병합·carry 복원을 하면서 **폭0 FP(3킬이 같은 순간)**가
15건으로 늘어 precision 상쇄. 그러나 **정탐(TP) 중에도 폭0가 6건**
(00-40-56 R54·R83, 00-42-33 R53, 03-12-36 R10, 02-15-28 R3, 00-07-24 R58 —
전부 사용자 확인 진짜 올킬):

→ **"같은 순간 3킬" 신호만으로 진짜/가짜 분리 불가**가 오늘 데이터로 재확정.
K채널 단독으로는 원리적 한계. 문서 여러 곳(R3 "구조적 결론")과 일치.

## 다음 카드 (폭0 분리 — Fable 판단: 성급한 게이트 금지)

과거 폭0 게이트 시도는 전부 라벨 10영상 회귀 유발(핸드오프 기록). 재시도 시:
1. **D채널 활용** — 이제 D가 캐시에 있음. 진짜 3킬 라운드 vs 오독 점프에서
   D 거동이 다른지 실측 분석부터 (예: 진짜 올킬은 D 불변, 오독 점프는 D도 이상).
   **→ 이게 무슨 뜻인지, 왜 이런 가설인지, 실측을 어떻게 하는지 전부**
   `HUD_ACE_HANDOFF.md`의 "★ 다음 카드 상세 설명: 폭0 FP를 D채널로 분리" 절에
   풀어씀 (2026-07-10 Fable 추가) — **다음 세션은 반드시 그 절부터 읽을 것.**
2. **carry 신뢰도** — 폭0 FP는 gap이 전부 carry에서 옴(라운드 내 중간값 0관측).
   폭0 TP는 라운드 내 실제 진행이 있었는지 대조 → 있으면 carry 의존도로 분리 가능성.
   (이것도 위 핸드오프 절 맨 아래 "대안 카드"에 설명 있음)
3. 어느 쪽이든 **라벨 10영상 회귀 0가 선결** — r5_final 대비 diff 필수.

## 남은 숙제 (우선순위)

1. **[신규] 경계 넘는 미관측 첫 킬** — 02-55-36(11→14 확정 올킬)이 `12→13→14`만
   관측(2킬), 11→12가 경계에 숨어 미탐. 직전 라운드 k_end=11 carry 필요.
   실측: R24가 kills=2로 끝남(k_samples=17, G5 통과). carry 이월 로직이 이
   케이스를 못 잇는 이유 조사 필요.
2. 폭0 분리(위 카드) — D채널/carry신뢰도, 신중히.
3. 세이브 감지(T4) — 미탐 ~11건 세이브 동반, 2건은 2킬+세이브=올킬.
4. D드라이브 162개 배치 — 미착수.

## 사용자 확인 완료 (이번 세션)

- 11/12→14/12: 02-55-36=11→14, 00-36-50=5→8 (둘 다 진짜, GT 유지)
- 04-27 22:44: 오탐(사망) → GT 제거 완료
- 01-01-04 4→7: 진짜 (재추출은 사용자가 18:50까지 요청했던 것)
- 05-19 R41/R52: 2킬(올킬 아님) → 오탐 확정, GT 미추가

---

# R6 (2026-07-13): 상단 스코어 앵커 판독 — 폭0 FP 분리의 새 독립 신호 (Fable 설계·핵심 검증 완료)

> 배경 필독: `HUD_ACE_HANDOFF.md` "2026-07-13 Fable — 실측 완료: D채널 가설 기각" 절.
> K채널·D채널·K밀도 등 **기존 캐시 신호로는 폭0 TP/FP 분리가 원리적으로 불가**함이
> 실측 확정됨. R6는 캐시에 없는 **새 채널**(상단 라이브 HUD 팀 승수)을 추가하는 카드.
> 사용자 승인 완료 (2026-07-13, "1,2번 진행시켜" → 4번 카드 실측 후 확대 결정).

## 0. 절대 규칙 (기존 + R6 추가분)

1. ace = `kills == 3` 정확히. 시각 보고 M:SS. (기존 규칙 전부 유지)
2. **측정은 3종 세트**: `hud_from_cache.py && _compare_hud_gt.py && _tp_diff.py --compare-to r5_final`
   — **TP 상실 0**이 수용 조건. 총점만 보지 말 것.
3. `hud_kda.py`의 KDA 행탐지/IoU 매칭은 건드리지 말 것.
4. **게이트는 폭0 후보에만 국소 적용** — 정상(폭>0) ace 판정 경로는 절대 건드리지 말 것.
5. 실측 없이 "그럴듯해서" 구현 금지 — Task 3(측정)이 GO/NO-GO 게이트. 분리 안 되면
   이 카드도 D채널처럼 기각하고 근거를 기록할 것.

## 1. 핵심 설계 (Fable 검증 완료 — 이 절의 수치는 전부 실측값)

### 1-1. 문제와 통찰

폭0 FP의 본질 = "판독 공백 구간에서 킬 +3이 어느 라운드 소속인지 모름".
상단 라이브 HUD의 **팀 승수 박스**(빨강|목표|파랑, 예 `001|005|002`)는 라운드가
결정되는 **그 순간** 정확히 +1 되므로, 승수 변화 시각 = 라운드 결정 시각이라는
**킬 귀속과 독립인 새 신호**가 된다.

기존 시도(고정비율 크롭)가 실패한 원인: `game_roi` ML 모델의 **프레임별 예측 지터**.
UI 자체는 절대 안 움직인다 — 흔들리는 건 크롭 기준점이었다.

### 1-2. 해법: 영상당 1회 앵커 탐지 (풀프레임 좌표, game_roi 미사용)

가운데 박스 "005"는 매치 내내 불변 텍스트 = 완벽한 랜드마크.
**풀프레임에서 직접 템플릿 매칭**으로 앵커를 잡으면 game_roi를 완전히 우회한다.

**Fable 실측 (8개 영상 × 8프레임, `scratchpad/anchor_validate.py`)**:
- 전 영상에서 앵커 위치 **정확히 (x=879, y=0), scale 1.00, 위치 표준편차 0.0px**
- 매칭 스코어 중앙값 0.91~0.95 (TM_CCOEFF_NORMED, 그레이스케일)
- 고정비율 크롭이 실패했던 4개 영상(03-12-36, 02-15-28, 00-07-24, 00-08-17) 전부 성공
- **결론: 이 OBS 코퍼스는 전 영상이 픽셀 단위로 동일 레이아웃.** 단, 하드코딩 금지 —
  미래 레이아웃 변화 대비, 반드시 영상당 앵커 탐지 후 캐시에 저장할 것.

### 1-3. 측정된 기하 상수 (scale 1.0, 앵커 좌표 (ax, ay) 기준 오프셋)

- 앵커 템플릿: `files/hud_templates/score_anchor_005.png` (159×58, 설치 완료)
- 숫자 밴드 y: `ay+7 ~ ay+45` (높이 38)
- 빨강 박스 x: `ax-142 ~ ax+1` | 가운데: `ax+8 ~ ax+151` | 파랑: `ax+158 ~ ax+301` (폭 143)
- 다른 scale s가 잡히면 오프셋에 s를 곱할 것

### 1-4. 판독기 설계 (Fable e2e 테스트에서 확인된 함정 반영 — 그대로 구현할 것)

`scratchpad/anchor_e2e.py`의 임시 판독기는 위치는 완벽했으나 값 오독 잔존. 원인 규명 완료:

- **함정 1 — 끝자리 CC 누락 = 조용한 오독**: 승수 표시는 항상 3자리 제로패딩(`002`).
  CC(connected component)가 2개만 잡히면 `00`→0으로 **조용히 틀린 값**이 나옴
  (01-01-04 21:14의 `002`가 0으로, 00-08-17의 `003`이 0으로 읽힌 원인).
  → **정확히 CC 3개가 아니면 None** (안전 미스). 이 규칙 하나가 판독기 신뢰성의 핵심.
- **함정 2 — 유효값 검증**: 승수는 0~9 (목표 5, 연장 포함 한자리) → 앞 두 자리는
  반드시 '0'. `digits[0]!=0 or digits[1]!=0`이면 None.
- **함정 3 — 빨강 박스 배경 그라데이션**: 빨간 배경 위 흰 글자가 어둡게 읽히는 프레임
  존재. `white_mask` threshold(155)로 대부분 되지만, 판독률 낮으면 이 임계값 스윕.
- 흰 숫자 템플릿 `white_0~5.png` **설치 완료** (2026-07-13 재수확분). 6~9는 승수상
  거의 안 나옴 — 필요 시에만 보충.

## 2. Task 0 — `hud_score_wins.py` 신규 모듈 + 앵커 탐지

```python
@dataclass
class ScoreAnchor:
    ax: int; ay: int; scale: float; conf: float

def find_score_anchor(video_path, n_samples=9) -> ScoreAnchor | None:
    # 영상 길이에 고르게 n_samples 프레임 → 풀프레임 그레이스케일
    # 상단 밴드 y∈[0, 0.16H], x∈[0.20W, 0.80W]에서 다중 스케일(0.70~1.22, 8단계) 매칭
    # conf>=0.75 프레임 5개 이상 + 위치 최빈값 일치 → 채택. 아니면 None(기능 비활성)
```

- `read_wins(frame_bgr, anchor) -> tuple[int|None, int|None]` — §1-3 기하 + §1-4 규칙
- selftest: 8개 검증 영상(§1-2)에서 앵커 (879, 0, 1.00) 재현 확인

## 3. Task 1 — 판독 스팟 검증 (기대 시퀀스 실측 확보됨)

아래 기대값이 재현되면 통과 (Fable이 이미 관측한 실제 시퀀스):

| 영상 | 구간 | 기대 |
|---|---|---|
| 2026-03-29 03-12-36 | 7:00~8:09 | **B: 0→1 at ~7:55** (GT 올킬 순간과 일치) |
| 2026-04-28 00-07-24 | 22:35~23:25 | **B: 4→5 at ~23:03** → **5→0 리셋** (5승=매치종료) |
| 2026-03-29 01-01-04 | 21:00~21:30 | **R: 1→2 at ~21:14, 2→3 at ~21:26** (오탐 라운드 내 2회 변화!) |
| 2026-04-10 00-08-17 | 23:40~24:30 | R: 3→4 at ~23:45, B는 003 안정 (0으로 흔들리면 함정1 미해결) |

## 4. Task 2 — 타임라인 스캐너 + 이벤트 추출 + 캐시

- `scan_score_timeline(video, scan_fps=2)` → `E:\clipai_result\score_cache\{stem}.json`
  (anchor + 원시 (t, red, blue) 배열). 영상당 ~2분.
- `score_events(timeline)` → `[(t_lo, t_hi, side, old, new)]`:
  - 지지 필터(같은 값 2회 이상)로 노이즈 제거 후 전이 추출
  - **+1 전이만 라운드 결정 이벤트**. 하락(리셋) = 매치 종료/하프타임 → 별도 'reset' 이벤트
  - t_lo = 이전값 마지막 안정 관측, t_hi = 새값 첫 안정 관측 (불확실 구간 보존)
  - **★ 하프타임 처리 (사용자 확인 도메인 사실, 2026-07-13)**: 전반→후반 교대 시
    양 팀 승수가 `000|005|000`으로 **초기화**된다 (5승 매치종료 리셋과 별개). 따라서:
    (a) **단조성/지지 필터는 하프 단위로 끊을 것** — 리셋 이벤트가 타임라인을
        분할하고, 각 분할 구간 안에서만 단조증가 가정 (매치 전체 단조 가정 금지 —
        후반 첫 이벤트들이 노이즈로 오분류됨. `hud_round_settle`의 K 0-리셋 간선과
        동일 구조의 문제).
    (b) 리셋 시각 자체도 **라운드 경계 신호**로 보존할 것 (하프타임 = 경계).
    (c) 교차검증 보너스: 스코어 리셋 시각은 K채널 리셋(하프타임 K=0) 시각과
        일치해야 정상 — 불일치하면 어느 한쪽 오독 의심 (Task 3 대조 항목에 포함).
    (d) 리셋 직후 0에서 시작하는 +1 전이(0→1)는 정상 이벤트 — 리셋과 섞어 버리지 말 것.
- 대상: 폭0 표본 영상 15개 (TP 3 + FP 12, 원본 존재분 — 목록은
  `HUD_ACE_HANDOFF.md` 2026-07-13 절 표 참고). ⚠ E:\OBS 재처리 배치가 돌고 있으면
  순차 실행으로 디스크 경합 회피.

## 5. Task 3 — ★ GO/NO-GO 측정 (구현 확대 전 필수)

폭0 후보 라운드마다 **blind gap**(직전 라운드 마지막 K 관측 ~ 이 라운드 첫 K 관측)을
계산하고, 그 안에 떨어지는 승수 이벤트 수를 TP/FP 그룹으로 대조:

- **가설**: 진짜 폭0(순간 3킬) → 이벤트는 ace 시각 **이후**(라운드 승리 순간)에만.
  가짜(병합) → blind gap **안**에 이벤트 ≥1 (킬이 경계를 넘었다는 직접 증거).
  01-01-04처럼 라운드 구간 안에 이벤트 2회면 확정적.
- 표로 정리해 분리 여부 판단. **안 갈리면 기각 + 핸드오프에 반증 기록** (D채널 전례).

## 6. Task 4 — 게이트 통합 (Task 3 통과 시에만)

- `timeline_from_reads`에서 **폭0 ace 후보에만**: score_cache 있고 blind gap 내
  이벤트 ≥1 → ace 기각(정확 규칙은 Task 3 실측으로 확정).
- score_cache 없는 영상 = 게이트 자동 비활성 (하위호환, 회귀 없음 보장).
- 측정: 3종 세트, r5_final 대비 **상실 0** + FP 감소 확인. 통과 시 라벨 10영상 외
  전 GT 영상으로 확대 측정.

## 7. Task 5 — 정리

- `HUD_ACE_HANDOFF.md`에 R6 결과 절 추가 (수치·표·기각이면 반증)
- `HANDOFF.md` 요약 갱신, 커밋 (한국어 커밋 메시지, 시각 M:SS)
