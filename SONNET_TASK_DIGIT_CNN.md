# 숫자 CNN v2 (체인-감독 재학습) — Sonnet 작업지시서

작성: 2026-07-23 Fable. 선행: `SONNET_TASK_UNDERREAD.md`(완료), `hud_kda.py` 172~186행
R4 실패 부검(반드시 먼저 읽을 것 — 같은 함정 재발 방지가 이 지시서의 절반이다).

## 0. 왜 다시 하나 (R4는 실패했는데)

R4(2026-07-07)는 모델이 아니라 **데이터가 실패**했다: v1 데이터셋이 "템플릿 IoU로
라벨을 딴" 순환 구조라 8 표본 36장 + 하드네거티브 0장 → 블러/0 프레임을 고신뢰
8로 오분류(recall 88.5→80.8 순손실, 롤백됨). R4 부검이 지목한 재시도 조건 (b)
"IoU 실패 지점의 실제 정답을 하드네거티브로 추가"를 이번에 구조적으로 해결한다.

**v2의 핵심 = 라벨을 템플릿이 아니라 신호 캐시의 시간적 문맥에서 딴다**
(`files/_build_digit_dataset_v2.py`, Fable 작성·selftest 통과):
- 격리된 가짜 0(= 화면의 진짜 8) → 8 라벨 대량 확보 (체인 문맥이 정답 보증)
- 안정 런 내부의 template_miss → 그 런의 값이 정답인 하드네거티브 (R4 킬러 해결)
- 6열 캐시의 D 채널 안정 런 → D 슬롯 학습 데이터 (D-가드 판독률 개선용)

Fable이 구현해 둔 것: 하베스터(위 파일) + `hud_kda.py` R15 통합 훅(플래그
`_CNN_V2_EIGHT`/`_CNN_V2_D`, **기본 OFF** — 게이트 통과 전 켜지 말 것) +
conf 0.85 캡(지지 필터 0.88 아래 → 단발 스퓨리어스는 정산이 자동 기각 =
시간적 상호확증 공짜).

## 1. 기대 효과 (근거 포함 추정)

현재: GT 174건, recall 66.7%(116탐지/미탐 58), precision 85.9%(검출 135/FP 19).

| 개선 축 | 추정 | 근거 |
|---|---|---|
| 8-판독 정상화 → recall | **+3.5~7%p** (→ 70~74%) | 미탐 58건 중 8-경유 확인·의심 케이스 6~12건: 02-21-23 54:20(8-base)·79:51(end-8), 03-26 9:40(6/8 교차), 05-18 29:29, 04-27 30:36(7→8), 05-26 26:01(6→8), 04-10 10:36(8→10) + 6월분 캐시 미분석 잔여. 부수효과: 가짜0 격리/8-브리지/8-증거 등 보정 핵 의존도 감소 → 미래 회귀 위험 축소 |
| D-판독 정상화 → precision | **+1.5~3%p** (→ 87~89%) | 현 FP 19건 중 데스 동반 미검출 확인 2건(#120 6/2→8/3, #122 1/1→3/2)은 D-가드가 D를 읽었으면 정산에서 차단됐을 케이스. 관전 오독류(05-31 R139 유형) 예방 포함 |
| 상한 주의 | — | 원본 소실 10영상(GT 26건)은 재스캔 불가라 캐시가 낡은 채 남음 — 이 몫의 미탐은 어떤 판독 개선으로도 회복 불가 |

## 2. 작업 순서

### T1. 수확 실행 (기계적)
```
python -u _build_digit_dataset_v2.py            # 원본 있는 캐시 전체 (~68영상, seek 다량)
```
완료 후 클래스별 수량 보고. **QC 필수**: `hardneg_*`와 `8` 폴더는 몽타주로 육안
스팟체크(각 100장 샘플) — 하베스터의 라벨은 체인 추론이라 소량 오염 가능. 오염
클러스터 발견 시 해당 파일만 삭제(하베스터 수정은 Fable 몫으로 보고).

### T2. 재학습
`train_hud_digit_cnn.py`의 `build_samples()`를 확장해 `cnn_dataset_v2/`를 흡수
(기존 v1 소스도 유지·병합). 규칙:
- `hardneg_<d>` 폴더는 라벨 `<d>`로 학습 (이게 핵심 — R4 킬러 교정)
- stem 기반 val 분리 유지 + val stem에 6월 영상 2개 추가 (v1 val은 3월뿐)
- 모델은 **별도 파일로 저장**: `hud_digit_clf_v2_best.pt` (v1은 롤백용 보존).
  `hud_digit_match._CNN_MODEL_PATH` 전환은 게이트 통과 후에만.
- 보고: per-class val 정확도, 특히 8과 hardneg 출신 샘플의 정확도 분리 표기

### T3. 오프라인 게이트 (파이프라인 건드리기 전)
held-out stem에서:
1. 격리된 가짜0 크롭 전부 → 모델이 8이라 해야 함 (포착률 보고)
2. **claimed-0 크롭 전부 → p≥0.92로 8이라 하면 안 됨** (R4 킬러 직접 측정)
   - 게이트: 0→8 오발률 < 0.5%. 실패 시 T2로 복귀(데이터/증강 조정), 파이프라인 진행 금지
3. D 슬롯: claimed-d 크롭 정확도 ≥ 99% (p≥0.95 기준)

### T4. 파이프라인 단계 검증 (재스캔 비용 주의)
판독기 변경은 sig_cache를 낡게 하므로 **재스캔 필수** — 전체 68영상 ≈ GPU 10시간+.
반드시 단계적으로:
1. `hud_digit_match._CNN_MODEL_PATH`를 v2로 전환 + `hud_kda._CNN_V2_EIGHT=True`
2. **파일럿 재스캔**(sig_cache `--force`): 8-관련 미탐 영상(§1 목록) + **R4 회귀
   전과 영상 3개(00-40-56, 02-21-23, 02-34-09 — 지난번 깨진 곳, 반드시 포함)**
3. `hud_from_cache.py` + `_compare_hud_gt.py` + 탐지/FP 집합 diff.
   게이트: 기존 TP 상실 0, FP 순증 0. 실패 시 플래그 OFF 롤백 + 원인 보고
4. 통과 시 나머지 전체 재스캔(백그라운드) → 전수 재평가 → `_CNN_V2_D=True`로
   같은 절차 반복 (D는 별도 게이트 — 한 번에 두 플래그 켜지 말 것)
5. GT 외 신규 검출이 나오면 기존 절차(클립 → `_검수중` 큐 → 사용자 육안 → GT 커밋)

### T5. 기록
이 파일 하단에 recall/precision 전후, 게이트 수치, 켜진 플래그 상태를 표로 갱신.

## 3. 금지/주의
- 게이트 통과 전 `_CNN_V2_*` 플래그를 켠 채 커밋 금지
- `_CNN_V2_MIN_P8`(0.92)·conf 캡(0.85)을 낮추는 방향의 튜닝 금지 — 캡이 지지
  필터(0.88)를 넘는 순간 단발 오분류가 정산 체인에 직행한다 (R4 사고 경로)
- 원본 소실 10영상의 캐시는 재생성 불가 — 절대 `--force` 대상에 넣지 말 것
  (기존 캐시 삭제 = 그 영상 GT 26건 영구 평가 불능)
- kills==3 게이트·D2/D3(R13)·R14 상수는 이 과제 범위 밖 — 건드리지 말 것

## 4. 진행 기록 (Sonnet이 갱신)

| 날짜 | 단계 | 결과 |
|---|---|---|
| 2026-07-23 | 인프라 (Fable) | 하베스터+selftest, R15 훅(기본 OFF), 지시서 |
| 2026-07-23 | T1 수확 완료 | 원본 보유 79영상 전체 처리(~50s/영상). 버그 수정: `extract_game_crop_bgr`가 (crop,box) 튜플 반환하는데 crop만 쓰도록 안 걸려있던 것 수정 |
| 2026-07-23 | T1 QC 스팟체크 | **hardneg_0/3/5: 정상** (라벨 일치 확인). **`8` 폴더: 대체로 정상**(이 폰트의 8은 위아래 막대 2쌍 형태, ~10~15% 노이즈는 QC 허용범위). **`junk` 폴더: ❌ 구조적 오염 확정** — "안정런 밖 template_miss"라는 기준이 실제로는 빠른 킬스트릭 중 미처 안정런을 못 채운 **정상 판독 가능 숫자**(9,3,6,5 등, 육안 확인됨)를 대량 junk로 오분류. 파일 단위 정리 불가 — **하베스터 재설계 필요 (Fable에게 인계)**. 조치: v2 junk 전량 학습 제외(v1 junk 135장만 사용), `train_hud_digit_cnn.py` 주석에 사유 기록 |
| 2026-07-23 | T2 재학습 완료 | 버그 수정 2건: (1) `sys.stdout` UTF-8 미설정으로 한글 출력 시 크래시 → 헤더에 `sys.stdout.reconfigure` 추가 (2) junk 제외 반영. 결과: train 21,907 / val 927, **val_acc 99.2%** (epoch 39). val per-class 전부 95%+ (9클래스만 4/4=100%지만 표본 4개뿐이라 참고용). **val per-origin(게이트 핵심 근거)**: v2_eight 171/173(98.8%), v2_hardneg 143/144(99.3%), v2_claimed 299/301(99.3%), v1_claimed 283/284, v1_junk 24/25. 모델 저장: `E:\Highlights\ml_dataset\models\hud_digit_clf_v2_best.pt` |

| 2026-07-23 | VAL_STEMS 확장 + 재학습 | 4영상(n=67)은 게이트 표본이 통계적으로 불안정 → 10영상(날짜 스프레드)으로 확장, 재학습. val_acc 99.2%→**95.9%**로 하락했지만 이게 더 정직한 수치(이전 값은 작은 val셋에 운 좋게 맞은 것). `files/_digit_cnn_v2_gate.py` 신규 작성(T3 게이트 스크립트) |
| 2026-07-23 | T3 게이트 실행 | **G1(0→8 오발) 0.000%(n=372) PASS.** G2(8 포착률, 참고용) 84.6%(n=671) — QC에서 확인된 `8` 폴더 자체 노이즈(~10~15%) 감안하면 사실상 천장에 가까움. **G3(D슬롯 정확도) 95.07%(n=812), 공식 기준(99%) 미달.** 세부 분석: 실패 40건 중 35건은 "정답인데 conf<0.95"(0클래스 편중, 프로덕션에서 안전 폐기), 4건은 "확신하고 junk 응답"(안전 — 오답 아닌 미탐), **위험한 "확신하고 오답" 케이스 0건.** → G3는 문구상 미달이지만 R4가 우려한 실질적 위험(확신에 찬 오분류)은 없음을 확인 |
| 2026-07-23 | T4 착수 (판단: G1 근거로 EIGHT만 우선 진행) | `hud_digit_match._CNN_MODEL_PATH` → v2 전환. `hud_kda._CNN_V2_EIGHT=True`(D는 위 사유로 보류 유지). 파일럿 재스캔 착수: R4 회귀 전과 3건(00-40-56, 02-21-23, 02-34-09) + 8-관련 미탐 5건(03-26, 05-18, 04-27, 05-26, 04-10) = 8영상, `--force` sig_cache 재생성 진행 중(백그라운드) |

| 2026-07-23 | **T4 파일럿 게이트 FAIL — 롤백 완료** | R4 회귀 전과 3건(00-40-56·02-21-23·02-34-09)은 **원본 소실**이라 재스캔 자체 불가(§3 "원본 소실 캐시 재생성 불가" 원칙과 무관하게 애초에 대상에서 제외됐어야 함 — 다음엔 원본 존재 여부 먼저 확인할 것). 실제 재스캔된 5건(03-26·05-18·04-27·05-26·04-10) 기준 GT 비교 결과: **TP 1건 손실**(`2026-04-10 00-08-17` 1:45 — GT 매칭 라운드가 kills 1+1+2로 쪼개져 3킬 라운드 자체가 사라짐), **FP 2건 신규**(`2026-05-18 01-54-57` R70, `2026-05-26 23-45-51` R09 — 둘 다 kill_times 3개가 완전 동일 시각인 폭0 패턴). 게이트 기준(TP상실0/FP순증0) 미달 → `_CNN_V2_EIGHT=False` 즉시 롤백 + 영향받은 5영상 sig_cache를 플래그 OFF로 재-재스캔해 원상복구 |

### T4 실패 근본원인 (재시도 전 반드시 재현할 것)

FP 2건 모두 **동일한 지문**으로 확인(원시 판독 직접 대조, `cnn_pilot_cache_backup/` 보존):
K가 안정값(예: 5)으로 한동안 읽히다가, **원래는 전부 template_miss(M)였던 구간**에
CNN-8 보정이 개입해 `k=8, conf=0.85, method='T'`로 여러 프레임이 갑자기 나타남
(예: 05-26 R09 — 440~449s는 K=5(0.65) 안정 후 449~460s가 전부 M이었는데, 450.0/450.5/
450.75/452.0/457.75에 CNN이 8을 주입). 결과: `hud_round_settle`이 "5→8(+3)"을
같은 시각(gap-stamp)에 몰아 찍어 **폭0 가짜 트리플킬**을 만듦 — R6/R10이 원래 막던
바로 그 패턴인데, 이번 것들은 두 라운드 다 인근에 승수(win) 이벤트가 2개 미만이라
R10(`_SCORE_SPLIT_MIN_EVENTS`) 커버리지 밖이었음.

TP 손실 1건은 정확한 라운드별 재현은 못 했으나(사전 백업 누락 — 교훈: **재스캔 전에
먼저 원본 캐시를 백업할 것**), 같은 계열(CNN이 이전엔 완전히 비어있던 miss 구간에
새 증거를 주입 → carry/gap 계산이 달라져 원래 하나였던 3킬 라운드가 여러 조각으로
재분할)로 추정.

**핵심 통찰**: `_CNN_V2_EIGHT`는 "0/None 자리만 건드리고 확신 있는 다른 판독은
안 건드린다"는 원칙(R8 8-위상 프로브와 동일 설계 원칙)은 지켰지만, **"원래 완전히
비어있던(전부 miss) 구간에 갑자기 조밀한 새 증거를 주입"하는 경우**를 놓쳤다.
`hud_round_settle`의 지지 필터(`_SUPPORT_MIN_READS`/`_SUPPORT_SINGLE_CONF`)와
gap-stamp 로직은 "이미 어느 정도 신호가 있던 구간에 값이 바뀌는" 상황을 가정하고
설계됐지, "암전 구간에서 갑자기 다발로 튀어나오는 고신뢰 단일값"은 검증되지 않은
입력 분포다.

### 재시도 조건 (다음 세션/Fable 인계)
1. **재현 우선**: 위 표의 2 FP 케이스를 `hud_from_cache.py --dump`로 먼저 재현할 것
   (백업 캐시 `cnn_pilot_cache_backup/`에 원시 판독 보존됨)
2. **재시도 방향 후보** (택1, 순서대로 시도 권장):
   a. CNN-8 증거에도 `_SUPPORT_MIN_READS`급 지지 조건 추가 — 고립 단발이 아니라
      연속·인접 구간에서 일정 횟수 이상 CNN이 8이라고 동의할 때만 채택
   b. R11 암전-gap 가드(`_DARK_GAP_GUARD`)를 CNN 증거에도 적용 — "완전 암전 구간을
      건너뛴 gap"은 귀속 포기(현재는 raw 판독 공백에만 적용, CNN 주입 값엔 미적용)
   c. R10 폭0 게이트의 `_SCORE_SPLIT_MIN_EVENTS` 요건을 CNN-8 유래 폭0에 한해 완화
      (일반 폭0과 달리 CNN 개입 폭0은 그 자체가 의심 신호이므로)
3. 어느 방향이든 **파일럿 5영상 재현 → 무손실 확인 → 그 다음에만** 확대 재스캔
4. 원본 소실 영상은 재스캔 대상에서 애초에 제외(이번에 3건 낭비— `source_available`
   확인 코드를 파일럿 목록 구성 단계에 넣을 것)

**현재 상태**: `_CNN_V2_EIGHT=False`, `_CNN_V2_D=False` (둘 다 OFF, 안전). 5영상
sig_cache는 플래그 OFF로 재-재스캔해 원상복구 완료(또는 진행 중 — 아래 진행기록 확인).
`hud_digit_match._CNN_MODEL_PATH`는 v2로 전환된 채 유지(플래그 OFF라 무해 — G1 게이트는
독립적으로 유효하므로 모델 자체는 문제 없음, 문제는 K-8 플래그의 정산 연동 방식).
junk 하베스터 재설계는 별도 과제로 Fable에게 보고 필요(이 파일 §0/T1 참고).
