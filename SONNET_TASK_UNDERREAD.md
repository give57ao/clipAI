# K 과소판독(KILLS_LOW) 미탐 해소 — Sonnet 작업지시서

작성: 2026-07-22 Fable (원인 분석 + 핵심 로직 구현 완료 상태에서 인계)
선행 문서: `HUD_ACE_HANDOFF.md`(파이프라인 구조), `SONNET_TASK.md`(기존 과제 이력)

---

## 0. 배경 — 이번에 확정된 사실 (재조사 불필요)

2026-07-22 기준 GT 147건 중 미탐 71건 전수 분석 결과 (`files/_miss_diag.py`):

| 원인 | 건수 | 의미 |
|---|---|---|
| KILLS_LOW | 65 | 라운드는 있는데 kills가 0/1/2로 과소정산 |
| ACE_FALSE | 3 | kills>=3인데 게이트 기각 (kills=4/5 오독 2건 + k_samples 미달 1건) |
| 기타 | 3 | GT 오기입(제거됨), 매처 중복 등 |

KILLS_LOW 65건 중 sig_cache 보유 33건을 원시 신호 레벨로 재분류한 결과:

| 분류 | 건수 | 뜻 |
|---|---|---|
| RAW_DK_OK | 25 | **원시 캐시에 ΔK≥3이 존재** — OCR은 다 캐놨는데 정산이 버림 |
| RAW_DK_1/2 | 6 | 원시조차 ΔK<3 — 판독(OCR/ROI) 한계 |
| K_DROP/NO_KREADS | 2 | 리셋/판독공백 |
| (캐시 없음) | 32 | 6월 중순~7월 초 영상 — sig_cache 미생성 상태라 진단 불가 |

RAW_DK_OK 25건의 유실 메커니즘 (둘이 겹침):
1. **킬 스트릭 중 가짜 경계**: 킬 배너가 HUD를 가려 row_miss 연속 구간이 생기고,
   그게 라운드 경계로 오인돼 킬 체인이 2+1로 쪼개짐.
   실측: `2026-03-29 03-12-36` 3:10·3:22·3:49 킬 사이 3:33에 경계 삽입,
   `2026-04-08 02-15-28` 26:57~28:40이 라운드 4개로 과분할.
2. **지지 필터의 빠른 전이 탈락**: 스트릭 중 K가 수 초 만에 +1씩 지나가 각 값이
   1프레임(conf 0.6~0.86)만 읽히는데, `hud_round_settle._SUPPORT_SINGLE_CONF=0.88`
   미달 단발이라 체인에서 제외.

⚠ **도메인 확정 (2026-07-22 사용자)**: 이 게임 맵은 상대가 3명 — **한 라운드 4킬은
불가능**. `kills>=4` 판독( kills=4 7건 + kills=5 1건, 전수 확인됨)은 전부 중복
카운트 오독이다. `detect_ace_hud.py`의 `r.kills == ace_kills`(정확히 3) 게이트는
버그가 아니라 이 오독의 방어선이므로 **절대 `>=`로 바꾸지 말 것.**

## 1. Fable이 구현해 둔 것 (2026-07-22)

- **`files/hud_streak_salvage.py`** (신규): 킬 스트릭 구제 정산 (R13).
  정산과 독립적으로 원시 판독에서 "+3 사다리"(안정값 p 이후 p+1→p+2→p+3 증거,
  단발 허용)를 찾고, 8중 방어선(D1~D8, 모듈 docstring 참고)을 통과하면 해당
  라운드에 ace를 스탬프. 핵심 방어선:
  - D1: 승수(win) 채널(score_cache) 없으면 전체 침묵
  - D2: 3번째 킬 직후(≤25s) win 이벤트 필수 (올킬=상대 전멸=라운드 즉시 승리)
  - D3: 킬 창 내부 win 금지 (중간에 라운드가 끝났으면 스트래들)
  - D4: 강한 경계(CNN 확인 or 승수 인접) 관통 금지
- **`files/detect_ace_hud.py`** 통합: `_STREAK_SALVAGE = True` 플래그,
  경계별 강도 수집(`boundary_strength`), `RoundTrack.salvage` 필드(JSON에 노출),
  구제 발동 시 `timeline.warnings`에 `streak_salvage_N`.
- **측정 결과** (캐시 72영상 재평가, `hud_from_cache.py` + `_compare_hud_gt.py`):
  - recall 53.7% → 55.8% (+3 TP), precision 59.4% → 60.3%
  - **TP 상실 0, 신규 FP 0** (compare 전/후 diff로 확인)
  - 신규 탐지: `04-13 22-46-39` 40:30, `05-31 21-57-14` 14:16, 137:10

측정 루프 (수정 → 수 초 재평가):
```
cd C:\clipAI\files
python -u hud_streak_salvage.py     # 셀프테스트 8케이스 — 항상 먼저
python -u hud_round_settle.py       # 기존 정산 셀프테스트 19케이스
python -u hud_from_cache.py         # 캐시 → hud_timeline 재생성 (영상 접근 0회)
python -u _compare_hud_gt.py        # GT 대조 recall/precision
```

## 2. Sonnet 과제 목록 (우선순위순)

### T1. 캐시 미보유 32건 영상의 신호 캐시 생성 (기계적, GPU 시간만 소요)

KILLS_LOW인데 sig_cache가 없어 진단·구제 불가한 영상들. 원본은 전부 `E:\OBS`에 있음.

대상 stem (17개 영상, 미탐 32건):
```
2026-06-14 00-55-19    2026-06-16 00-55-18    2026-06-18 23-25-01
2026-06-19 23-58-29    2026-06-22 02-08-51    2026-06-23 01-06-42
2026-06-24 01-55-38    2026-06-25 03-10-00    2026-06-26 23-43-25
2026-06-27 01-31-51    2026-06-28 01-42-47    2026-06-30 01-17-15
2026-06-30 22-24-03    2026-06-30 23-36-38    2026-07-03 21-42-21
2026-07-04 01-21-49    2026-07-05 02-22-22
```

절차 (영상당 순서대로):
```
python -u hud_sig_cache.py --videos "E:\OBS\<stem>.mp4"        # K/D/A 신호 캐시
python -u hud_score_wins.py --videos "E:\OBS\<stem>.mp4"       # 승수 캐시 (D1 필수!)
python -u hud_boundary_verify.py --stems "<stem>"              # 경계 CNN 검증 (있으면)
```
(정확한 CLI 인자는 각 스크립트 `--help`로 확인. score 캐시가 없으면 구제가 D1에서
침묵하므로 **score_cache 생성이 특히 중요**.)

완료 후 `hud_from_cache.py` + `_compare_hud_gt.py` 실행, 전/후 미탐 목록 diff를
기록할 것. 기대: 32건 중 상당수가 RAW_DK_OK 유형이면 구제로 탐지 전환.

### T2. 잔여 RAW_DK_OK 22건 기각 사유 추적

score_cache가 있는데도 구제 안 된 케이스들. 각각에 대해 D2~D8 중 어디서
기각됐는지(또는 사다리 탐색 자체가 실패했는지) 추적 스크립트를 짜서 분류할 것.

방법: `salvage_streak_aces()`를 복사해 각 방어선에 print를 넣은 진단판을
`files/_salvage_why.py`로 만들고, GT (stem, 시각) 목록을 돌려 기각 지점 표를 뽑는다.
분류 결과는 아래 3버킷으로:
- **튜닝 가능**: `_STEP_MAX_SEC`(50), `_WIN_AFTER_SEC`(25), `_EVID_MIN_CONF`(0.55),
  `_STABLE_*` 문턱 조정으로 잡힐 것 → T3으로
- **win 채널 문제**: 해당 시각 win 이벤트 자체가 score_cache에 없음 (OCR 미검출)
  → 건수만 기록 (score OCR 개선은 별도 과제, 임의로 D2를 약화하지 말 것)
- **근본 한계**: 사다리 증거 자체가 캐시에 없음 (예: `03-26 01-26-52` 9:40 —
  중간값 9·10·11이 전부 단발 저신뢰 + 6/8 교차 오염) → 포기 목록에 기록

### T3. 파라미터 스윕 (수용 기준 엄수)

T2에서 "튜닝 가능"으로 분류된 케이스를 잡기 위한 상수 조정. 반드시:
- 스윕 대상: `_STEP_MAX_SEC ∈ {40,50,60,75}`, `_WIN_AFTER_SEC ∈ {20,25,30}`,
  `_EVID_MIN_CONF ∈ {0.50,0.55,0.60}`, `_STABLE_SINGLE_CONF ∈ {0.85,0.88}`
- **수용 기준 (하나라도 어기면 그 조합 폐기)**:
  1. 기존 TP 상실 0 (compare 전/후 탐지 목록 diff로 증명)
  2. 신규 FP 순증 0 — 단, GT에 없는 신규 검출은 곧바로 FP가 아니라 "미검수 후보"다.
     GT에 없는 신규 검출이 나오면 → T4 절차로 사용자 검수에 올릴 것
  3. 셀프테스트 2종(8+19케이스) 전부 통과
- 결과는 조합별 (recall, precision, 신규검출수) 표로 이 파일 하단에 추가.

### T4. GT 외 신규 검출의 사용자 검수 편입

구제가 GT에 없는 올킬을 새로 찾으면(충분히 가능 — GT 자체가 미탐 위에서 만들어짐):
1. `E:\clipai_result\_검수중\`에 클립 추출 — 기존 규약: `NNN_<stem>_R<라운드>_<m>m<s>s_hud_ace.mp4`,
   번호는 `E:\clipai_result\검수목록.csv`의 max(번호)+1부터 연속
2. `검수목록.csv`에 행 추가 (번호,영상,라운드,시각_초,파일,판정(빈칸),메모)
3. 사용자에게 목록 보고 → 판정 받으면 tp는 `files/gt_aces.json` 스팬 추가 + 커밋,
   fp는 기각 기록 (2026-07-21 커밋 `98bcdf0`, `2e95b48` 선례 참고)

### T5. 수용 후 전체 재배치

T1~T3이 안정되면:
1. `hud_from_cache.py`로 캐시 보유분 hud_timeline 전체 재생성 (이미 T1에서 수행)
2. 캐시 없는 나머지 영상은 `batch_hud_ace_pipeline.py`로 재스캔 (실스캔도 동일
   `timeline_from_reads`를 타므로 구제 자동 적용)
3. `files/_build_review_ledger.py`(또는 기존 재생성 절차)로 review_ledger 갱신 —
   ⚠ 라운드 번호는 세대 간 불안정하므로 **시각 기준 매칭** 원칙 준수
   (2026-07-21 원장 재구축 때 확립된 규칙, 45s 허용오차)

## 3. 금지/주의 사항

- `r.kills == ace_kills`를 `>=`로 바꾸지 말 것 (§0 도메인 확정 — 4킬 오독 방어선)
- D2(직후 승리)·D3(창 내부 win 금지)는 구제의 정밀도 뼈대 — 완화 금지.
  완화 없이 못 잡는 케이스는 "포기 목록"으로 기록하고 넘어갈 것
- `hud_round_settle.py`의 기존 상수(_SUPPORT_SINGLE_CONF 등)는 건드리지 말 것 —
  구제는 정산 바깥에서 도는 이유가 바로 "정산 문턱을 건드리면 기존 FP 방어가
  무너지기 때문"이다 (2026-07-20 그레이스 게이트 실패 기록, hud_round_settle.py
  110~125행 주석 참고 — 재시도 전 그 표를 반드시 재현할 것)
- RAW_DK_1/2·NO_KREADS 유형(원시조차 ΔK<3)은 이 과제 범위 밖 (킬피드 OCR 등
  새 신호원이 필요 — 미래 과제로만 기록)
- 커밋은 논리 단위당 1개, 메시지에 전/후 recall·precision과 셀프테스트 결과 명기

## 4. 진행 기록 (Sonnet이 갱신)

| 날짜 | 작업 | recall | precision | 비고 |
|---|---|---|---|---|
| 2026-07-22 | R13 구제 도입 (Fable) | 53.7→55.8% | 59.4→60.3% | TP손실0/신규FP0 |
| 2026-07-22 | T1 착수 — 17영상 sig_cache 생성 (Sonnet) | - | - | 백그라운드 진행 중(영상당 ~8~11분, GPU 스트림 타임아웃 경고 간헐 발생하나 계속 진행). 완료되는 대로 T3 재검증 예정 |
| 2026-07-22 | T2 완료 (Sonnet) | - | - | `files/_salvage_why.py` 작성(D1~D7 진단 재현). 캐시+score_cache 보유 30건 재분류: tunable 26 / win채널문제 3 / 근본한계(D4·D6 등) 3 / **GT중복의심 1건**(`2026-03-29 02-43-33` 8:21 — R8이 이미 ace=True로 정탐 중인 동일 이벤트의 근접 중복 GT. 실제 미탐 아님, GT 정리 후보로 기록만 하고 임의 삭제 안 함) |
| 2026-07-22 | T3 1차 스윕 (Sonnet, **재검증 필요**) | - | - | `files/_salvage_sweep.py` 작성, 72조합 스윕. ⚠ T1과 동시 실행되며 sig_cache가 스윕 도중 늘어나 일부 조합(step≥60) 결과가 오염됨(`2026-06-14 00-55-19`/`2026-06-16 00-55-18`이 스윕 중간에 새로 캐시돼 이후 조합에만 반영) — 그 결과의 "신규 FP"는 파라미터 때문인지 신규 영상 편입 때문인지 분리 불가. **신뢰 가능한 부분만**: step=40(모든 win/conf/stable 조합, 12건)은 베이스라인(step=50)과 완전 동일(탐지·FP 집합 100% 일치) — 더 좁혀도 손실도 이득도 없음. step≥60은 T1 완료 후 재스윕 필요. |

### T3 재검증 예정 메모
T1(17영상 sig_cache) — ✅ 완료(17/17). 이후 순서 (2026-07-22 오후, 사용자 외출로 무인 진행 승인받음):
1. ~~`python -u hud_sig_cache.py --videos ...`~~ ✅ 완료 (17/17)
2. score_cache 생성 — `files/_build_score_cache.py` 작성해 진행 중 (전용 CLI 없어 `scan_score_timeline()`
   직접 호출하는 드라이버). 완료되는 대로 `hud_from_cache.py`+`_compare_hud_gt.py` 자동 체이닝
   (백그라운드 스크립트로 걸어둠, `after_scorecache.log`)
3. 이어서 `hud_boundary_verify.py --videos <17 stems>` 경계 CNN 검증 자동 체이닝
4. 그 후 `_salvage_sweep.py` 단독 재실행 (동시작업 없음 — 오염 재발 방지)
   전체 체인: `master_chain.log`에 순차 기록

**⚠ T5(review_ledger.csv 재구축)는 이 자동 체인에 포함하지 않음** — verdict 손실/BOM 사고 전례가
있어 사람 판단 없이 무인 실행하기엔 리스크가 큼. 위 1~4 끝나면 결과만 정리해두고, 실제 재구축은
사용자 복귀 후 진행.
**T4(GT 외 신규 탐지)도 자동 반영 금지** — 새 탐지 나오면 클립만 뽑아 `_검수중`에 큐잉하고
`gt_aces.json` 커밋은 사용자 육안 확인 후에만.

### 무인 체인 최종 결과 (2026-07-22, Sonnet)

| 단계 | recall | precision | 비고 |
|---|---|---|---|
| R13 도입 직후 (Fable) | 53.7% | 59.4% | 캐시 72영상 기준 |
| T1(17영상 sig_cache) 후 | 55.8% | 60.3% | 신규 영상 편입, 게이트 미작동 상태 |
| score_cache 생성 후 | 59.9% | 64.7% | R10 승수교차검증 게이트 활성화 — 신규 17영상 TP 1→6, FP 36→31 |
| 경계 CNN 검증(버그 수정 후) | **60.5%** | **65.0%** | 최종. GT 147건 / 탐지 89 / 검출 137건 |

**버그 발견·수정**: `hud_boundary_verify.py`가 6원소(d/a 포함) 캐시 스키마에서 크래시 —
`files/hud_boundary_verify.py` 커밋 `f5ec358`로 수정 (4원소/6원소 모두 지원, `hud_from_cache.py`와
동일 패턴).

**T3 클린 재스윕**: 71개 조합 **전부 베이스라인과 결과 100% 동일**(recall/precision/탐지집합
완전 일치) — 현재 기본 상수(step=50, win=25, conf=0.55, stable=0.88)가 이미 이 탐색 범위 안에서
지역 최적점. 파라미터 조정으로 더 얻을 게 없음, 이 스레드는 사실상 종결.

**남은 것(사용자 복귀 후)**:
- T5: review_ledger.csv 재구축 (17개 영상 포함, 시각기준 매칭 원칙 준수).
- 나머지 근본한계(원시 신호 자체 부족, T2에서 분류한 3건)·win채널 문제(3건)는 킬피드 OCR 등
  새 신호원 없이는 이번 세션 범위에서 추가 개선 불가 — 미래 과제로 유지.

### T4 완료 (2026-07-23, Sonnet) — GT 미등록 진짜 올킬 27건 편입

신규 17영상의 잔여 FP 31건(width0 아닌 normal 패턴, 전부 GT-근접 아님)을 `_검수중` 105~135번으로
클립 추출 → 사용자 육안 검수. 결과:
- **tp 27건** → `gt_aces.json` 편입 완료 (커밋 `0ed6f6a`). recall 60.5%→**66.7%**,
  precision 65.0%→**84.7%** (서브셋 92.1%)
- **fp 4건** — 전부 동일 패턴: 라운드 경계 근처에서 실제로는 1~2킬씩 나뉜 두 라운드를 하나의
  3킬로 합산 오판. `HANDOFF.md`의 기존 boundary_merge 계열과 같은 실패모드:
  - `2026-06-18 23-25-01 R49`(#110): 6킬이 승리 배너와 동시 판독 — 전 라운드 5→6(1킬), 이 라운드
    6→8(2킬)로 나뉨
  - `2026-06-24 01-55-38 R102`(#120): 6/2→8/3, 2킬 1데스
  - `2026-06-25 03-10-00 R28`(#122): 1/1→3/2, 2킬 1데스
  - `2026-06-26 23-43-25 R3`(#124): 전 라운드 5→6(1킬), 이 라운드 6→8(2킬)로 나뉨
  → **후속 과제 후보**: 이 4건이 공통적으로 "승리 배너 인접 + 킬 델타가 실제로는 라운드 경계를
  가로질러 나뉨" 패턴이라, `_GRACE_*`(`hud_round_settle.py`)나 D-가드 조건 정밀화로 잡을 수 있을지
  검토 가치 있음. 단 그레이스는 실측 TP로 캘리브된 규칙이라 함부로 건드리면 안 됨(§3 금지사항 참고)
  — 손대기 전 `_tp_diff` 류로 무손실 검증 필수.
- 1차 클립 4건(#106,121,125,132)은 첫 킬이 프레임 밖이라 리드타임 확장 재추출 후 최종 확인.
