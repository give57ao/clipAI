# HUD 올킬 배치 진행 기록

배치를 돌릴 때마다(완료 시) 이 파일을 갱신하고 커밋·푸시한다. `E:\OBS` 전체를 대상으로
`batch_hud_ace_pipeline.py`(인자 없음, 기본 동작)를 실행 — 이미 처리된 영상은
`E:\clipai_result\hud_timeline\<stem>.json` 캐시로 자동 스킵된다.

결과물: `E:\clipai_result\ace_clips_hud\`에 클립 전체를 평탄화 저장(영상별 하위 폴더 없음,
파일명에 원본 영상 stem이 접두사로 들어가 있어 충돌 없음 — 2026-08-30 변경).

**누적 지표 기준(2026-09-04부터)**: 원본 영상을 처리 후 `E:\OBS`에서 직접 지우는 경우가
잦아져서, "현재 OBS에 있는 영상 기준 올킬 수"는 삭제될 때마다 줄어들어 추세를 보기
어렵다. 대신 **`ace_clips_hud` 디스크 파일 개수**(원본이 지워져도 클립 자체는 남아있는 한
줄지 않음)를 누적 성과 지표로 쓴다.

## 2026-09-04 배치

- 대상: `E:\OBS` 85개 (전전 배치 이후 원본 다수 삭제 + 신규 발생분 혼재, 실행 중에도
  계속 줄어들어 최종 확인 시점엔 78개)
- 처리: 62/85 OK, 23개 자동 스킵(120초 미만)
- 신규 검출: `2026-09-03 01-34-42`(2개), `2026-09-04 00-22-43`(2개),
  `2026-09-04 01-09-06`(1개), `큐틀리3`(1개) 등
- **누적 클립(디스크 기준): 70개 → 83개 (+13)**
- 로그: `files/_batch_run_20260904.log` (세션 로컬, git 미추적)
- `2026-08-08 00-43-11`의 누락 클립(R04)은 원본이 `E:\OBS`에서 완전히 삭제되어
  재추출 불가로 확인, 포기함(2/3 클립만 영구 보존)

## 2026-09-02 배치

- 대상: `E:\OBS` 전체 105개 (직전 대비 신규 30개 발생분 포함)
- 처리: 83/105 OK (기존 캐시 재사용 포함), 22개 자동 스킵(120초 미만)
- 현재 OBS 기준 누적 결과: **올킬 68개** (43개 영상에서 검출)
- 로그: `files/_batch_run_20260902.log` (세션 로컬, git 미추적)
- ⚠ 디스크에는 클립 70개가 있음 — 그중 3개(`MSR2`, `설대세이브`,
  `마이건플러스 스왑속도 체감`)는 지금은 `E:\OBS`에 없는(삭제/이동된) 영상의 잔여
  클립이라 현재 68개 집계에서 빠짐. 또한 `2026-08-08 00-43-11`은 원래 올킬 3개
  (R04/R29/R54)였는데 지금 R04 클립 파일만 디스크에서 안 보임(파이프라인이 이번에
  이 영상을 아예 건드리지 않았으니 배치 오류는 아니고, 그 사이 수동으로 지워졌거나
  이동됐을 가능성). 필요하면 확인해서 재추출해드릴 수 있음.

## 2026-08-30 배치

- 대상: `E:\OBS` 전체 96개 (직전 대비 신규 34~44개 발생분 포함)
- 처리: 75/96 OK (기존 캐시 재사용 포함), 21개 자동 스킵(120초 미만 — 목록은 로그 참고)
- 누적 결과: **올킬 54개 / 클립 54개** (36개 영상에서 검출) — 디스크 파일 수와 JSON
  타임라인 `ace_rounds` 합계가 정확히 일치함을 확인
- 로그: `files/_batch_run_20260828.log` (세션 로컬, git 미추적)

## 2026-08-24 배치 (이전 기록)

- 대상: `E:\OBS` 전체 68개
- 처리: 62/68 OK, 6개 자동 스킵(120초 미만)
- 누적 결과: 올킬 38개 / 클립 38개 (26개 영상에서 검출)
